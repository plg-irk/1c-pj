#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Функция ПолучитьЗначениеВЕТИС(ТипТранспорта) Экспорт
	
	ИндексЗначения = Перечисления.СтатусыЗаписейСкладскогоЖурналаВЕТИС.Индекс(ТипТранспорта);
	
	Результат = Число(Метаданные.Перечисления.СтатусыЗаписейСкладскогоЖурналаВЕТИС.ЗначенияПеречисления[ИндексЗначения].Комментарий);
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьЗначение(Знач СтатусЗаписи) Экспорт
	
	СтатусЗаписи = Строка(СтатусЗаписи);
	
	Результат = Неопределено;
	
	НайденноеЗначение = Неопределено;
	
	ЗначенияПеречисления = Метаданные.Перечисления.СтатусыЗаписейСкладскогоЖурналаВЕТИС.ЗначенияПеречисления;
	Для Каждого ЗначениеПеречисления Из ЗначенияПеречисления Цикл
		Если ЗначениеПеречисления.Комментарий = СтатусЗаписи Тогда
			НайденноеЗначение = ЗначениеПеречисления;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если Не НайденноеЗначение = Неопределено Тогда
		ИндексЗначения = Метаданные.Перечисления.СтатусыЗаписейСкладскогоЖурналаВЕТИС.ЗначенияПеречисления.Индекс(НайденноеЗначение);
		Результат = Перечисления.СтатусыЗаписейСкладскогоЖурналаВЕТИС[ИндексЗначения];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецЕсли