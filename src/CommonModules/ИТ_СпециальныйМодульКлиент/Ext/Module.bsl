Процедура ПодключитьВнешнююФормуАРМПроизводства() Экспорт
	
	
	ИмяОбр = ИТ_СпециальныйМодульСервер.ПолучитьИмяВнешнейАРМПроизводства();
	
	Если ИмяОбр = "" Тогда 
		Возврат ;
	КонецЕсли;
	//
	ПараметрыОткрытия = Новый Структура("КлючВарианта", "");
	ФормаАРМПроизводства =  ОткрытьФорму("ВнешняяОбработка." + ИмяОбр + ".Форма", , Неопределено);
	ФормаАРМПроизводства.Активизировать();
		
КонецПроцедуры 