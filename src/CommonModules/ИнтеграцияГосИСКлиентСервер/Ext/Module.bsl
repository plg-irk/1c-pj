
#Область ТекстыСтандартныхСообщений

Функция ТекстКомандаНеМожетБытьВыполнена() Экспорт
	Возврат НСтр("ru = 'Команда не может быть выполнена для указанного объекта.'");
КонецФункции

// Добавляет в свойство структуры сообщения текст ошибки
//
// Параметры:
//  Сообщение    - Структура - сообщение, в которое добавляется текст ошибки.
//  ТекстОшибки  - Строка - добавляемый текст ошибки.
//
Процедура ДобавитьТекстОшибки(Сообщение, ТекстОшибки) Экспорт
	
	Если Сообщение.Ошибки.Получить(ТекстОшибки) <> Неопределено Тогда
		Возврат;
	Иначе
		Сообщение.Ошибки.Вставить(ТекстОшибки, Истина);
	КонецЕсли;
	
	Если Сообщение.ТекстОшибки = "" Тогда
		Сообщение.ТекстОшибки = ТекстОшибки;
	Иначе
		Сообщение.ТекстОшибки = Сообщение.ТекстОшибки + Символы.ПС + ТекстОшибки;
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьЗначениеСвойстваИзОбъектаXDTO(ОбъектXDTO, Знач ИмяСвойства, Знач ПустоеЗначение = Неопределено, ИмяСвойстваДоп = Неопределено) Экспорт
	
	Если ОбъектXDTO = Неопределено Тогда
		Возврат ПустоеЗначение;
	КонецЕсли;
	
	СвойстваОбъекта = ОбъектXDTO.Свойства();
	
	СвойствоОбъекта = СвойстваОбъекта.Получить(ИмяСвойства);
	Если СвойствоОбъекта = Неопределено И (НЕ ИмяСвойстваДоп = Неопределено) Тогда
		СвойствоОбъекта = СвойстваОбъекта.Получить(ИмяСвойстваДоп);
		ИмяСвойства = ИмяСвойстваДоп;
	КонецЕсли;
	
	Если СвойствоОбъекта = Неопределено Тогда
		Возврат ПустоеЗначение;
	КонецЕсли;
	
	Результат = ОбъектXDTO[ИмяСвойства];
	Если ТипЗнч(Результат) = Тип("ОбъектXDTO") И Результат.Свойства().Количество() = 0 Тогда
		Возврат ПустоеЗначение;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти