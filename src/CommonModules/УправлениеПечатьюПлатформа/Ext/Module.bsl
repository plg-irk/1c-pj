// Общий модуль "Управление печатью (платформа,сервер)"


////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

// Определяет объекты, в которых есть процедура ДобавитьКомандыПечати().
//
// Параметры:
//  СписокОбъектов - Массив - список менеджеров объектов.
//
Процедура ПриОпределенииОбъектовСКомандамиПечати(СписокОбъектов) Экспорт
	
	ВсеОбъектыПечати = УправлениеПечатьюПовтИсп.ОбъектыСКомандамиПечати();
	
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(СписокОбъектов, ВсеОбъектыПечати);
	
КонецПроцедуры // ПриОпределенииОбъектовСКомандамиПечати()

// Дополняет список команд печати внешними печатными формами.
//
// Параметры:
//   КомандыПечати - ТаблицаЗначений - См. УправлениеПечатью.СоздатьКоллекциюКомандПечати().
//   ИмяОбъекта    - Строка          - Полное имя объекта метаданных, для которого необходимо получить список
//                                     команд печати.
//
Процедура ПриПолученииКомандПечати(КомандыПечати, ИмяОбъекта) Экспорт
	
	// Определим какое количество реальных команд печати добавлено
	КоличествоКомандПечати = КомандыПечати.Количество();
	
	// Получим объект метаданных
	МетаданныеОбъекта = Метаданные.НайтиПоПолномуИмени(ИмяОбъекта);
	
	// Команда формирования реестра для документов и справочников
	Если Метаданные.Справочники.Содержит(МетаданныеОбъекта) ИЛИ Метаданные.Документы.Содержит(МетаданныеОбъекта) ИЛИ Метаданные.ЖурналыДокументов.Содержит(МетаданныеОбъекта) Тогда
		УправлениеПечатьюПлатформа.ДобавитьКоманду(КомандыПечати, Неопределено, "ПечатьРеестра", НСтр("ru = 'Печать реестра'"), МетаданныеОбъекта.Имя, "УправлениеДиалогомКлиент.ОбработкаКомандыПечати",,, "ФормаСписка,ФормаВыбора",, ЛОЖЬ,,,,,,, 90);
	КонецЕсли;
	
	// Если печатных форм нет, то прочие пункты не нужны
	Если КоличествоКомандПечати = 0 Тогда
		Возврат;
	КонецЕсли;
	
	// Команды настройки будем собирать в одной группе
	УправлениеПечатьюПлатформа.ДобавитьГруппуКоманд(КомандыПечати, "ПодменюПараметрыПечати", НСтр("ru = 'Настройки'"), ЛОЖЬ,, 90);
	
	// Если используются дополнительные реквизиты, то проверим настройки параметров печати.
	Если ЕстьРеквизит(МетаданныеОбъекта,, "ДополнительныеРеквизиты") Тогда
		
		Если Метаданные.ЖурналыДокументов.Содержит(МетаданныеОбъекта) Тогда
			ЕстьРеквизитыПечати = Истина;
		Иначе
			ЕстьРеквизитыПечати = Ложь;
			
			МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени(МетаданныеОбъекта.ПолноеИмя());
			СсылкаНаОбъект  = МенеджерОбъекта.ПустаяСсылка();
			
			// Получение списка основного набора свойств
			НаборыСвойств = УправлениеСвойствамиСлужебный.ПолучитьОсновнойНаборСвойствДляОбъекта(СсылкаНаОбъект);
			
			// Получение списка доступных наборов дополнительных реквизитов со свойством только для печати.
			Запрос = Новый Запрос;
			Запрос.Текст = "
			|ВЫБРАТЬ ПЕРВЫЕ 1
			|	ИСТИНА КАК ЕстьРеквизитыПечати
			|ИЗ
			|	Справочник.НаборыДополнительныхРеквизитовИСведений.ДополнительныеРеквизиты КАК ДополнительныеРеквизиты
			|ГДЕ
			|	ДополнительныеРеквизиты.Свойство.ТолькоДляПечати = Истина И 
			|	ДополнительныеРеквизиты.Ссылка = &НаборыСвойств
			|";
			
			Запрос.УстановитьПараметр("НаборыСвойств", НаборыСвойств);
			РезультатЗапроса = Запрос.Выполнить();
			ЕстьРеквизитыПечати = (НЕ РезультатЗапроса.Пустой());
			
		КонецЕсли;
		
		// Если есть дополнительные реквизиты печати, добавим команду печати "Параметры печати".
		Если ЕстьРеквизитыПечати Тогда
			УправлениеПечатьюПлатформа.ДобавитьКоманду(КомандыПечати, Неопределено, "ПараметрыПечати", НСтр("ru = 'Параметры печати'"),МетаданныеОбъекта.Имя, "УправлениеДиалогомКлиент.ОбработкаКомандыПечати", "ПодменюПараметрыПечати",,,,,,,,,,, 90, "ИспользоватьДополнительныеРеквизитыИСведения");
		КонецЕсли;
		
	КонецЕсли;
	
	// Команда настройки комплекта печати
	Если Метаданные.Документы.Содержит(МетаданныеОбъекта) Тогда
		УправлениеПечатьюПлатформа.ДобавитьКоманду(КомандыПечати, Неопределено, "НастройкаКомплекта", НСтр("ru='Настройка комплекта печати'"),МетаданныеОбъекта.Имя, "УправлениеСпискомДокументаКлиент.ОбработкаКомандыПечатиКомплекта", "ПодменюПараметрыПечати",,,,,,,,,,, 95);
	КонецЕсли;
	
КонецПроцедуры // ПриПолученииКомандПечати()

// Процедура добавления команды печати в комплект
//
Процедура ДобавитьКоманду(КомандыПечати, МенеджерПечати, Идентификатор, Представление, Объект = "", Обработчик="", МестоРазмещения=Неопределено, Картинка=Неопределено, СписокФорм="",
                          ЗаголовокФормы="", ПроверкаПроведенияПередПечатью=Неопределено, ПроверкаИсключенияЗапретаРедактирования = Неопределено, СразуНаПринтер=Ложь, ФорматСохранения=Неопределено, ПереопределитьПользовательскиеНастройкиКоличества=Ложь,
                          ДополнитьКомплектВнешнимиПечатнымиФормами=Истина, ФиксированныйКомплект=Ложь, НеВыполнятьЗаписьВФорме=Неопределено, Порядок=Неопределено, ФункциональныеОпции="", ТипыОбъектовПечати=Неопределено) Экспорт
	
	Если КомандыПечати=Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	// !!!_alalek Временно! удалить после разнесения правок в ТП3 и А6
	Если Найти(Объект, ".") > 0 Тогда
		ДобавитьКоманду(КомандыПечати, МенеджерПечати, Идентификатор, Представление,, Объект ,Обработчик, МестоРазмещения, Картинка, СписокФорм,ЗаголовокФормы,
		ПроверкаПроведенияПередПечатью, ПроверкаИсключенияЗапретаРедактирования, СразуНаПринтер, ФорматСохранения, ПереопределитьПользовательскиеНастройкиКоличества,ДополнитьКомплектВнешнимиПечатнымиФормами, 
		ФиксированныйКомплект, НеВыполнятьЗаписьВФорме, Порядок, ФункциональныеОпции);
		Возврат;
	КонецЕсли;
	// !!!_alalek Временно! удалить после разнесения правок в ТП3 и А6
	

	СтрокаКоманды = КомандыПечати.Добавить();
	СтрокаКоманды.ЭтоГруппа      = ЛОЖЬ;
	СтрокаКоманды.МенеджерПечати = МенеджерПечати;
	СтрокаКоманды.Идентификатор  = Идентификатор;
	СтрокаКоманды.Представление  = Представление;
	СтрокаКоманды.Обработчик     = Обработчик;
	
	Если ЗначениеЗаполнено(МестоРазмещения) Тогда
		СтрокаКоманды.МестоРазмещения = МестоРазмещения;
	КонецЕсли;

	Если ЗначениеЗаполнено(Картинка) Тогда
		СтрокаКоманды.Картинка = Картинка;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СписокФорм) Тогда
		СтрокаКоманды.СписокФорм = СписокФорм;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ФункциональныеОпции) Тогда
		СтрокаКоманды.ФункциональныеОпции = ФункциональныеОпции;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЗаголовокФормы) Тогда
		СтрокаКоманды.ЗаголовокФормы = ЗаголовокФормы;
	Иначе
		СтрокаКоманды.ЗаголовокФормы = Представление;
	КонецЕсли;
	
	Если ПроверкаПроведенияПередПечатью <> Неопределено Тогда
		СтрокаКоманды.ПроверкаПроведенияПередПечатью = ПроверкаПроведенияПередПечатью;
	Иначе
		СтрокаКоманды.ПроверкаПроведенияПередПечатью = (НЕ ПравоПользователя("ПечатьНепроведенных",Объект));
	КонецЕсли;
	
	Если ПроверкаИсключенияЗапретаРедактирования <> Неопределено Тогда
		СтрокаКоманды.ПроверкаИсключенияЗапретаРедактирования = ПроверкаИсключенияЗапретаРедактирования;
	Иначе
		СтрокаКоманды.ПроверкаИсключенияЗапретаРедактирования = (ПравоПользователя("ИсключениеЗапретаРедактирования",Объект));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СразуНаПринтер) Тогда
		СтрокаКоманды.СразуНаПринтер = СразуНаПринтер;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ФорматСохранения) Тогда
		СтрокаКоманды.ФорматСохранения = ФорматСохранения;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПереопределитьПользовательскиеНастройкиКоличества) Тогда
		СтрокаКоманды.ПереопределитьПользовательскиеНастройкиКоличества = ПереопределитьПользовательскиеНастройкиКоличества;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДополнитьКомплектВнешнимиПечатнымиФормами) Тогда
		СтрокаКоманды.ДополнитьКомплектВнешнимиПечатнымиФормами = ДополнитьКомплектВнешнимиПечатнымиФормами;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ФиксированныйКомплект) Тогда
		СтрокаКоманды.ФиксированныйКомплект = ФиксированныйКомплект;
	КонецЕсли;
	
	Если СтрокаКоманды.ПроверкаПроведенияПередПечатью Тогда
		СтрокаКоманды.НеВыполнятьЗаписьВФорме = ЛОЖЬ;
	ИначеЕсли НеВыполнятьЗаписьВФорме <> Неопределено Тогда
		СтрокаКоманды.НеВыполнятьЗаписьВФорме = НеВыполнятьЗаписьВФорме;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Порядок) Тогда
		СтрокаКоманды.Порядок = Порядок;
	Иначе
		СтрокаКоманды.Порядок = КомандыПечати.Количество()+1;
	КонецЕсли;
	
	Если ТипыОбъектовПечати<>Неопределено Тогда
		СтрокаКоманды.ТипыОбъектовПечати = ТипыОбъектовПечати;
	КонецЕсли;
	
КонецПроцедуры // ДобавитьКоманду()

Процедура ДобавитьГруппуКоманд(КомандыПечати, Идентификатор, Представление, ЭтоПодменю, МестоРазмещения=Неопределено, Порядок=Неопределено) Экспорт
	
	Если КомандыПечати=Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СтрокаКоманды = КомандыПечати.Добавить();
	СтрокаКоманды.ЭтоГруппа        = ИСТИНА;
	СтрокаКоманды.Идентификатор    = Идентификатор;
	СтрокаКоманды.Представление    = Представление;
	СтрокаКоманды.ВидГруппыПодменю = ЭтоПодменю;
	
	Если ЗначениеЗаполнено(МестоРазмещения) Тогда
		СтрокаКоманды.МестоРазмещения = МестоРазмещения;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Порядок) Тогда
		СтрокаКоманды.Порядок = Порядок;
	Иначе
		СтрокаКоманды.Порядок = КомандыПечати.Количество()+1;
	КонецЕсли;
	
КонецПроцедуры // ДобавитьГруппуКоманд()

Процедура СоздатьГруппыКомандНаФорме(Форма, КомандыПечати, МестоРазмещенияКомандПоУмолчанию) Экспорт
	
	// Определим имя группы подменю печати по умолчанию
	Если МестоРазмещенияКомандПоУмолчанию=Неопределено Тогда
		ИмяГруппыРодителяПоУмолчанию = "КоманднаяПанель";
	Иначе
		ИмяГруппыРодителяПоУмолчанию = МестоРазмещенияКомандПоУмолчанию.Имя;
	КонецЕсли;
	
	// Произведем добавление кнопки подменю Печать на командной панели формы
	Если Форма.Элементы[ИмяГруппыРодителяПоУмолчанию].Вид=ВидГруппыФормы.Подменю ИЛИ КомандыПечати.Количество()=1 Тогда
		// Добавление специального подменю не требуется
	Иначе
		МестоРазмещенияКомандПоУмолчанию = ЗащищенныеФункцииСервер.ДобавитьГруппуКоманд(Форма, ИмяГруппыРодителяПоУмолчанию + "ПодменюПечать", ИмяГруппыРодителяПоУмолчанию, ИСТИНА, НСтр("ru = 'Печать'"), БиблиотекаКартинок.Печать,НСтр("ru = 'Подменю печати'"),, ОтображениеКнопки.Картинка);
		ИмяГруппыРодителяПоУмолчанию = ИмяГруппыРодителяПоУмолчанию + "ПодменюПечать";
	КонецЕсли;
	
	// Обработаем все команды, которые описывают группы
	Для каждого ГруппаКоманд Из КомандыПечати.НайтиСтроки(Новый Структура("ЭтоГруппа", ИСТИНА)) Цикл
		
		// Определим где должна располагаться текущая группа
		Если ЗначениеЗаполнено(ГруппаКоманд.МестоРазмещения) И (НЕ Форма.Элементы.Найти(ГруппаКоманд.МестоРазмещения)=Неопределено) Тогда
			ИмяГруппыРодителя = ГруппаКоманд.МестоРазмещения;
		Иначе
			ИмяГруппыРодителя = ИмяГруппыРодителяПоУмолчанию;
		КонецЕсли;
		
		// Определим имя группы с которым оно будет добавлено на форму
		ИмяГруппы = ИмяГруппыРодителя+"Группа"+ГруппаКоманд.Идентификатор;
		
		// Производим создание элемента формы
		ЗащищенныеФункцииСервер.ДобавитьГруппуКоманд(Форма, ИмяГруппы, ИмяГруппыРодителя, ГруппаКоманд.ВидГруппыПодменю, ГруппаКоманд.Представление, Новый Картинка,,, ОтображениеКнопки.Текст);
		
		// Производим корректировку имен мест размещения вложенных команд печати
		Для каждого КомандаПечати Из КомандыПечати.НайтиСтроки(Новый Структура("МестоРазмещения", ГруппаКоманд.Идентификатор)) Цикл
			КомандаПечати.МестоРазмещения = ИмяГруппы;
		КонецЦикла;
		
		// Текущая группа обработана, исключим ее из общего списка
		КомандыПечати.Удалить(ГруппаКоманд);
		
	КонецЦикла;
	
КонецПроцедуры // СоздатьГруппыКомандНаФорме()

// Приводит макет печатной формы в соответствие с данными документа
//
// Параметры:
// ЭтотОбъект - ДокументОбъект - документ, для которого производится действие
// Макет	 - макет печатной формы.
//
// Возвращаемое значение:
// Область шапки таблицы с заполненными параметрами.
//
Функция ПривестиМакетПечатнойФормы(Макет, Объект, ТабличныеЧасти=Неопределено) Экспорт
	// получим нужные области
	ОбластьТовар = Макет.Область("Товар");
	
	// определим, нужны ли скидки
	ЕстьСкидка = Ложь;
	
	Если ТабличныеЧасти = Неопределено Тогда
		ТабличныеЧасти = Новый Массив;
		ТабличныеЧасти.Добавить("Товары");
	КонецЕсли;
	
	Для Каждого ТабличнаяЧасть Из ТабличныеЧасти Цикл
		
		ЕстьСкидка = ЕстьРеквизит(Объект, "СуммаСкидки", ТабличнаяЧасть) И (Объект[ТабличнаяЧасть].Итог("СуммаСкидки") <> 0);
		ЕстьСкидка = ЕстьСкидка ИЛИ (ЕстьРеквизит(Объект,"СуммаСкидкиСтроки", ТабличнаяЧасть) И (Объект[ТабличнаяЧасть].Итог("СуммаСкидкиСтроки") <> 0));
		Если ЕстьСкидка Тогда
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если НЕ ЕстьСкидка И  Макет.Области.Найти("Скидка") <> Неопределено Тогда
		// удалим область
		ОбластьСкидка = Макет.Область("Скидка");
		ОбластьТовар.ШиринаКолонки = ОбластьТовар.ШиринаКолонки + ОбластьСкидка.ШиринаКолонки;
		Макет.УдалитьОбласть(ОбластьСкидка,ТипСмещенияТабличногоДокумента.ПоВертикали);
	КонецЕсли;
	
	// проверим нужно ли выводить код в печатных формах
	ЕстьКод = ПравоПользователя("ВыводитьКодВПечатныхФормах");
	Если НЕ ЕстьКод И  Макет.Области.Найти("Код") <> Неопределено Тогда
		ОбластьКод = Макет.Область("Код");
		ОбластьТовар.ШиринаКолонки = ОбластьТовар.ШиринаКолонки + ОбластьКод.ШиринаКолонки;
		Макет.УдалитьОбласть(ОбластьКод,ТипСмещенияТабличногоДокумента.ПоВертикали);
	КонецЕсли;
	
	// теперь запишем параметры шапки
	ОбластьМакета = Макет.ПолучитьОбласть("ШапкаТаблицы");
	Если ЕстьКод Тогда
		КолонкаКода = ПолучитьПараметрыРежимаВыводаКодаВДокументах();
		ОбластьМакета.Параметры.ИмяКолонкиКод = КолонкаКода.Синоним;
	КонецЕсли;
	
	// заполняем заголовок колонки НДС по типу цен
	Параметры = Новый Структура("НДС");
	Параметры.НДС = "НДС";
	Если ЕстьРеквизит(Объект, "ТипЦен") И Объект.ТипЦен.ЦенаВключаетНДС Тогда	// Если НДС включен
		Организация = Неопределено;
		ПодразделениеКомпании = Неопределено;
		ДатаСреза = Неопределено;
		
		Если ЕстьРеквизит(Объект,"Организация") Тогда
			Организация = Объект.Организация;
		КонецЕсли;
		
		Если ЕстьРеквизит(Объект,"ПодразделениеКомпании") Тогда
			ПодразделениеКомпании = Объект.ПодразделениеКомпании;
		КонецЕсли;
		
		Если ЕстьРеквизит(Объект, "Дата") Тогда
			ДатаСреза = Объект.Дата;
		КонецЕсли;
		
		Если НЕ ОсвобожденОтУплатыНДС(Организация, ПодразделениеКомпании, ДатаСреза) Тогда
			Параметры.НДС = НСтр("ru = 'в т.ч. НДС'");
		КонецЕсли; 
	КонецЕсли;
	ОбластьМакета.Параметры.Заполнить(Параметры);
	
	Возврат ОбластьМакета;
КонецФункции

// Функция возвращает объект метаданных по текущему значению права "режим вывода кода в документах".
//
Функция ПолучитьПараметрыРежимаВыводаКодаВДокументах() Экспорт
	
	РежимыВыводаКодаВДокументах = ПолучитьФункциональнуюОпцию("ИспользоватьАртикул");
	Если НЕ ЗначениеЗаполнено(РежимыВыводаКодаВДокументах) Тогда
		РежимыВыводаКодаВДокументах = Перечисления.РежимыВыводаКодаВДокументах.Артикул;
	КонецЕсли;
	
	Индекс = Перечисления.РежимыВыводаКодаВДокументах.Индекс(РежимыВыводаКодаВДокументах);
	МД = Метаданные.Перечисления.РежимыВыводаКодаВДокументах;
	
	Возврат МД.ЗначенияПеречисления[Индекс];
КонецФункции

// Процедура возвращает значение колонки код из номенклатуры
//
// Параметры:
//	Номенклатура - Справочник "Номенклатура". Номенклатура для получения значений.
//
// Возвращаемое значение - Строка. Значение колонки код.
//
Функция ПолучитьЗначениеКолонкиКода(Номенклатура) Экспорт
	// получим значение права
	Если НЕ ЗначениеЗаполнено(Номенклатура) Тогда
		Возврат "";
	КонецЕсли;
	
	РежимВыводаКода = ПолучитьПараметрыРежимаВыводаКодаВДокументах().Имя;
	
	Если РежимВыводаКода = "КодИАртикул" Тогда
			КодНоменклатуры = Номенклатура["Код"]+" / "+Номенклатура["Артикул"];
	Иначе
		КодНоменклатуры = Номенклатура[РежимВыводаКода];
	КонецЕсли;
	
	Возврат КодНоменклатуры;
КонецФункции

// Процедура формирования наименования документа по ссылке
//
// Параметры:
//	Документ - Ссылка на документ для которого готовится представление
//	АльтернативноеНазвание - название документа которое будет использовано вместо синонима
//								при формировании представления.
//
//	Возвращаемое значение - Строка. Представление документа.
//
Функция ПолучитьПредставлениеДокумента(Документ, АльтернативноеНазвание="") Экспорт
	
	Если НЕ ПустаяСтрока(АльтернативноеНазвание) Тогда
		ПредставлениеДокумента = АльтернативноеНазвание;
		
	ИначеЕсли ЕстьРеквизит(Документ, "ХозОперация") Тогда
		ПредставлениеДокумента = Строка(Документ.ХозОперация);
		
	Иначе
		ПредставлениеДокумента = УправлениеДиалогомСервер.ПредставлениеОбъект(Документ);
	КонецЕсли;
	
	Возврат СокрЛП(ПредставлениеДокумента) + " № " + ПолучитьНомерДляПечати(Документ) + " от " + Формат(Документ.Дата,"ДФ = dd.MM.yyyy");
	
КонецФункции

// Функция формирования номера для печати(убраны лидирующие нули).
//
// Параметры:
//	Документ - Ссылка на документ , номер которого используется;
//	СокращенныйНомер - признак сокращенного номера.
//
// Возвращаемое значение - Строка. Номер для печати.
//
Функция ПолучитьНомерДляПечати(Документ, СокращенныйНомер=Неопределено) Экспорт
	
	НомерДокумента = Документ.Номер;
	
	Если СокращенныйНомер=ЛОЖЬ ИЛИ (СокращенныйНомер=Неопределено И Константы.ПолныйНомерДокументаВПечатныхФормах.Получить()) Тогда
		Возврат НомерДокумента;
	КонецЕсли;
	
	// удалим лидирующие нули
	Пока Лев(НомерДокумента, 1) = "0" Цикл
		НомерДокумента = Сред(НомерДокумента,2);
	КонецЦикла;
	
	Возврат НомерДокумента;
	
КонецФункции // ПолучитьНомерДляПечати()

Функция ПолучитьРеквизитыОрганизации(ЮрФизЛицо)
	
	Реквизиты = Новый Структура;
	Реквизиты.Вставить("Наименование", "");
	Реквизиты.Вставить("ФормаСобственности", Неопределено);
	Реквизиты.Вставить("ИНН", "");
	Реквизиты.Вставить("КПП", "");
	Реквизиты.Вставить("КодПоОКПО", "");
	Реквизиты.Вставить("КодОКВЭД", "");
	Реквизиты.Вставить("НомерСчета", "");
	
	Запрос = Новый Запрос;
	Запрос.Текст = "
	|ВЫБРАТЬ
	|	Организации.Наименование,
	|	Организации.ФормаСобственности,
	|	Организации.ИНН,
	|	Организации.КПП,
	|	Организации.КодПоОКВЭД КАК КодОКВЭД,
	|	Организации.КодПоОКПО КАК КодПоОКПО,
	|	Организации.ОсновнойБанковскийСчет.НомерСчета КАК НомерСчета
	|ИЗ
	|	Справочник.Организации КАК Организации
	|ГДЕ
	|	Организации.Ссылка = &ЮрФизЛицо
	|";
	
	Запрос.УстановитьПараметр("ЮрФизЛицо", ЮрФизЛицо);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Реквизиты, Выборка);
	КонецЕсли;
	
	Возврат Реквизиты;
	
КонецФункции

Функция ПолучитьРеквизитыКонтрагента(ЮрФизЛицо)
	
	Реквизиты = Новый Структура;
	Реквизиты.Вставить("Наименование", "");
	Реквизиты.Вставить("ФормаСобственности", Неопределено);
	Реквизиты.Вставить("ИНН", "");
	Реквизиты.Вставить("КПП", "");
	Реквизиты.Вставить("КодПоОКПО", "");
	Реквизиты.Вставить("КодОКВЭД", "");
	Реквизиты.Вставить("НомерСчета", "");
	
	Запрос = Новый Запрос;
	Запрос.Текст = "
	|ВЫБРАТЬ
	|	Контрагенты.Наименование,
	|	Контрагенты.ФормаСобственности,
	|	Контрагенты.ИНН,
	|	Контрагенты.КПП,
	|	"""" КАК КодОКВЭД,
	|	Контрагенты.КодПоОКПО КАК КодПоОКПО,
	|	Контрагенты.Имя,
	|	Контрагенты.Отчество,
	|	Контрагенты.Фамилия,
	|	Контрагенты.ОсновнойБанковскийСчет.НомерСчета КАК НомерСчета
	|ИЗ
	|	Справочник.Контрагенты КАК Контрагенты
	|ГДЕ
	|	Контрагенты.Ссылка = &ЮрФизЛицо
	|";
	
	Запрос.УстановитьПараметр("ЮрФизЛицо", ЮрФизЛицо);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Реквизиты, Выборка);
	КонецЕсли;
	
	Возврат Реквизиты;
	
КонецФункции

Функция ПолучитьРеквизитыПодразделения(ЮрФизЛицо)
	
	Реквизиты = Новый Структура;
	Реквизиты.Вставить("Наименование", "");
	Реквизиты.Вставить("ФормаСобственности", Неопределено);
	Реквизиты.Вставить("ИНН", "");
	Реквизиты.Вставить("КПП", "");
	Реквизиты.Вставить("КодПоОКПО", "");
	Реквизиты.Вставить("КодОКВЭД", "");
	Реквизиты.Вставить("НомерСчета", "");
	Реквизиты.Вставить("Организация", Неопределено);
	
	Запрос = Новый Запрос;
	Запрос.Текст = "
	|ВЫБРАТЬ
	|	ПодразделенияКомпании.Наименование,
	|	ПодразделенияКомпании.Организация КАК Организация,
	|	ПодразделенияКомпании.Организация.ФормаСобственности КАК ФормаСобственности,
	|	ПодразделенияКомпании.Организация.ИНН КАК ИНН,
	|	ПодразделенияКомпании.КПП,
	|	ПодразделенияКомпании.Организация.КодПоОКВЭД КАК КодОКВЭД,
	|	ПодразделенияКомпании.Организация.КодПоОКПО КАК КодПоОКПО,
	|	ПодразделенияКомпании.Организация.ОсновнойБанковскийСчет.НомерСчета КАК НомерСчета
	|ИЗ
	|	Справочник.ПодразделенияКомпании КАК ПодразделенияКомпании
	|ГДЕ
	|	ПодразделенияКомпании.Ссылка = &ЮрФизЛицо
	|";
	
	Запрос.УстановитьПараметр("ЮрФизЛицо", ЮрФизЛицо);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Реквизиты, Выборка);
	КонецЕсли;
	
	Возврат Реквизиты;
	
КонецФункции

// Функция формирует сведения об указанном юридическом или физическом лице.
// К сведениям относятся: наименование, адрес, номер телефона, банковские реквизиты.
//
// Параметры: 
//  ЮрФизЛицо   - организация или физическое лицо, о котором собираются сведения.
//  ДатаПериода - дата, на которую выбираются сведения о ЮрФизЛице.
//  ДляФизЛицаТолькоИнициалы - Для физ. лица выводить только инициалы имени и отчества.
//  БанковскийСчет - Банковский счет, если счет не основной.
//
// Возвращаемое значение:
//  Сведения - собранные сведения.
//
Функция СведенияОЮрФизЛице(ЮрФизЛицо, ДатаПериода, ДляФизЛицаТолькоИнициалы = Истина, Знач БанковскийСчет = Неопределено) Экспорт

	Сведения = Новый Структура("ЮрФизЛицо, Представление, ПолноеНаименование, КодПоОКПО, КодОКВЭД, ИНН, КПП, Телефоны, ЮридическийАдрес, Банк, БИК, КоррСчет, НомерСчета, Фамилия, Имя, Отчество");

	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") И (НЕ ЗначениеЗаполнено(ЮрФизЛицо)) Тогда
		Возврат Сведения;
	КонецЕсли;
	
	ДанныеПодразделения = Ложь;
	ДанныеОрганизации   = Ложь;
	ДанныеКонтрагента   = Ложь;
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.ПодразделенияКомпании") Тогда
		ДанныеПодразделения = Истина;
	ИначеЕсли ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") Тогда
		ДанныеОрганизации = Истина;
	ИначеЕсли ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Контрагенты") Тогда
		ДанныеКонтрагента = Истина;
	КонецЕсли;
	
	Если ДанныеОрганизации Тогда
		Реквизиты = ПолучитьРеквизитыОрганизации(ЮрФизЛицо);
	ИначеЕсли ДанныеПодразделения Тогда
		Реквизиты = ПолучитьРеквизитыПодразделения(ЮрФизЛицо);
	ИначеЕсли ДанныеКонтрагента Тогда
		Реквизиты = ПолучитьРеквизитыКонтрагента(ЮрФизЛицо);
	КонецЕсли;
	
	Сведения.Вставить("ПолноеНаименование",      Реквизиты.Наименование);
	Сведения.Вставить("ОфициальноеНаименование", Реквизиты.Наименование);
	Сведения.Вставить("ЮрФизЛицо", Реквизиты.ФормаСобственности);
	Сведения.Вставить("ИНН", СтрЗаменить(Реквизиты.ИНН," ",""));
	Сведения.Вставить("КПП", Реквизиты.КПП);
	
	Если НЕ ЮрФизЛицо.ФормаСобственности = Перечисления.ФормыСобственности.ЮридическоеЛицо Тогда
		Сведения.Вставить("КПП", "");
		Сведения.Вставить("КодОКВЭД", "");
		Сведения.Вставить("КодПоОКПО", "");
	КонецЕсли;
	
	ЗначениеПараметра = "";
	Если ДанныеПодразделения Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо,Справочники.ВидыКонтактнойИнформации.ТелефонПодразделенияКомпании);
		Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
			ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Реквизиты.Организация,Справочники.ТелефонОрганизации);
		КонецЕсли;
	ИначеЕсли ДанныеОрганизации Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо,Справочники.ВидыКонтактнойИнформации.ТелефонОрганизации);
	ИначеЕсли ДанныеКонтрагента Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо,Справочники.ВидыКонтактнойИнформации.ТелефонКонтрагента);
	КонецЕсли;
	
	Сведения.Вставить("Телефоны", ЗначениеПараметра);
	
	//Сведения.Вставить("НомерСчета", 	  РеквизитыСчета.НомерСчета);
	//Сведения.Вставить("Банк", 			  РеквизитыСчета.Банк);
	//Сведения.Вставить("БИК", 			  РеквизитыСчета.БИК);
	//Сведения.Вставить("КоррСчет", 		  РеквизитыСчета.КоррСчет);
	//Сведения.Вставить("АдресБанка", 	  РеквизитыСчета.АдресБанка);
	
	ЗначениеПараметра = "";
	Если ДанныеПодразделения Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо, Справочники.ВидыКонтактнойИнформации.ЮрАдресПодразделенияКомпании);
		Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
			ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Реквизиты.Организация, Справочники.ВидыКонтактнойИнформации.ЮрАдресОрганизации);
		КонецЕсли;
	ИначеЕсли ДанныеОрганизации Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо,Справочники.ВидыКонтактнойИнформации.ЮрАдресОрганизации);
	ИначеЕсли ДанныеКонтрагента Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо,Справочники.ВидыКонтактнойИнформации.ЮрАдресКонтрагента);
	КонецЕсли;
	
	Сведения.Вставить("ЮридическийАдрес", ЗначениеПараметра);
	
	ЗначениеПараметра = "";
	Если ДанныеПодразделения Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо, Справочники.ВидыКонтактнойИнформации.ФактАдресПодразделенияКомпании);
		Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
			ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Реквизиты.Организация, Справочники.ВидыКонтактнойИнформации.ФактАдресОрганизации);
		КонецЕсли;
	ИначеЕсли ДанныеОрганизации Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо,Справочники.ВидыКонтактнойИнформации.ФактАдресОрганизации);
	ИначеЕсли ДанныеКонтрагента Тогда
		ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ЮрФизЛицо,Справочники.ВидыКонтактнойИнформации.ФактАдресКонтрагента);
	КонецЕсли;
	
	Сведения.Вставить("ФактическийАдрес", ЗначениеПараметра);
	
	Возврат Сведения;

КонецФункции // СведенияОЮрФизЛице()

// Формирование строкового представления справочника
Функция ПолучитьПредставлениеСправочника(Объект, Знач СтруктураПараметров=Неопределено, ДляПечати=Ложь, РасчетныйСчетДокумента=Неопределено, НаДату = Неопределено, ДополнительныеПараметры = Неопределено) Экспорт
	
	// Проверим подходит ли объект под тип.
	ТипОбъекта = ТипЗнч(Объект);
	Если ТипОбъекта <> Тип("СправочникСсылка.Организации")
		И ТипОбъекта <> Тип("СправочникСсылка.ПодразделенияКомпании")
		И ТипОбъекта <> Тип("СправочникСсылка.СкладыКомпании")
		И ТипОбъекта <> Тип("СправочникСсылка.Контрагенты") Тогда
			Возврат НСтр("ru = 'Неправильный тип объекта.'");
	КонецЕсли;
	
	Если НЕ ТипЗнч(ДополнительныеПараметры) = Тип("Структура") Тогда
		ДополнительныеПараметры = Новый Структура;
	КонецЕсли;
	
	Представление = "";
	// Если не указана структура параметров, работаем по умолчанию.
	Если СтруктураПараметров = Неопределено Тогда
		СтруктураПараметров=Новый Структура;
		СтруктураПараметров.Вставить("Наименование",     "");
		СтруктураПараметров.Вставить("ИНН",              "ИНН ");
		СтруктураПараметров.Вставить("КПП",              "КПП ");
		СтруктураПараметров.Вставить("АдресЮридический", "");
		СтруктураПараметров.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПараметров.Вставить("ОКПО",             "Код по ОКПО ");
	КонецЕсли;
	
	// объявим часто используемые переменные
	ТипСклад = Тип("СправочникСсылка.СкладыКомпании");
	ТипПодразделение = Тип("СправочникСсылка.ПодразделенияКомпании");
	// Если у подразделения документа стоит флаг "ИспользоватьКИПодразделенияПриПечати", то КИ выводим для подразделения.
	ИспользоватьКИПодразделенияПриПечати = (ДополнительныеПараметры.Свойство("ПодразделениеКомпании") И ДополнительныеПараметры.ПодразделениеКомпании.ИспользоватьКИПодразделенияПриПечати);
	
	Для Каждого Параметр Из СтруктураПараметров Цикл
		
		ЗначениеПараметра = Неопределено;
		
		Если ВРЕГ(Параметр.Ключ)="НАИМЕНОВАНИЕ" Тогда
			
			Если ДляПечати И ТипОбъекта = ТипПодразделение Тогда
				ЗначениеПараметра = ПолучитьНаименованиеСправочника(Объект.Организация, НаДату);
			ИначеЕсли ТипОбъекта <> ТипСклад Тогда
				ЗначениеПараметра = ПолучитьНаименованиеСправочника(Объект, НаДату);
			Иначе
				ЗначениеПараметра = ПолучитьНаименованиеСправочника(Объект);
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="ИНН" Тогда
			
			Если ТипОбъекта = ТипПодразделение ИЛИ ТипОбъекта = ТипСклад Тогда
				ЗначениеПараметра = СокрЛП(Объект.Организация.ИНН);
			ИначеЕсли ТипОбъекта=Тип("СправочникСсылка.Контрагенты")
				И Объект.ФормаСобственности=Перечисления.ФормыСобственности.ОбособленноеПодразделение Тогда
				ИННОбъекта = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект, "ГоловнойКонтрагент.ИНН");
				ЗначениеПараметра = СокрЛП(ИННОбъекта);
			Иначе
				ЗначениеПараметра = СокрЛП(Объект.ИНН);
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="КПП" Тогда
			
			Если ТипОбъекта = ТипСклад Тогда
				ЗначениеПараметра = ?(ЗначениеЗаполнено(НаДату), СокрЛП(Справочники.Организации.КППНаДату(Объект.Организация, НаДату)), СокрЛП(Объект.Организация.КПП));
			ИначеЕсли ТипОбъекта = ТипПодразделение И НЕ ЗначениеЗаполнено(Объект.КПП) Тогда
				ЗначениеПараметра = ?(ЗначениеЗаполнено(НаДату), СокрЛП(Справочники.Организации.КППНаДату(Объект.Организация, НаДату)), СокрЛП(Объект.Организация.КПП));
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Контрагенты")
				И (Объект.ФормаСобственности = Перечисления.ФормыСобственности.ЮридическоеЛицо
				ИЛИ Объект.ФормаСобственности=Перечисления.ФормыСобственности.ОбособленноеПодразделение) Тогда
				ЗначениеПараметра = ?(ЗначениеЗаполнено(НаДату), СокрЛП(Справочники.Контрагенты.КППНаДату(Объект, НаДату)), СокрЛП(Объект.КПП));
			ИначеЕсли ТипОбъекта = ТипПодразделение Тогда
				ЗначениеПараметра = ?(ЗначениеЗаполнено(НаДату), СокрЛП(Справочники.ПодразделенияКомпании.КППНаДату(Объект, НаДату)), СокрЛП(Объект.КПП));
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Организации") Тогда 
				ЗначениеПараметра = ?(ЗначениеЗаполнено(НаДату), СокрЛП(Справочники.Организации.КППНаДату(Объект, НаДату)), СокрЛП(Объект.КПП));
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="ОКПО" Тогда
			
			Если ТипОбъекта = ТипСклад ИЛИ ТипОбъекта = ТипПодразделение Тогда
				ЗначениеПараметра = СокрЛП(Объект.Организация.КодПоОКПО);
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Контрагенты")
				И (Объект.ФормаСобственности = Перечисления.ФормыСобственности.ЮридическоеЛицо
				ИЛИ Объект.ФормаСобственности=Перечисления.ФормыСобственности.ОбособленноеПодразделение) Тогда
				ЗначениеПараметра = СокрЛП(Объект.КодПоОКПО);
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Организации") Тогда
				ЗначениеПараметра = СокрЛП(Объект.КодПоОКПО);
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="АДРЕСЮРИДИЧЕСКИЙ" ИЛИ ВРег(Параметр.Ключ)="АДРЕСФАКТИЧЕСКИЙ" Тогда
			
			ВидКИ = ?(ВРег(Параметр.Ключ)="АДРЕСЮРИДИЧЕСКИЙ", "ЮрАдрес", "ФактАдрес");
			Если ТипОбъекта = ТипПодразделение Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект, Справочники.ВидыКонтактнойИнформации[ВидКИ+"ПодразделенияКомпании"]);
				Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект.Организация,Справочники.ВидыКонтактнойИнформации[ВидКИ+"Организации"]);
				КонецЕсли;
			ИначеЕсли ТипОбъекта = ТипСклад Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект.Организация,Справочники.ВидыКонтактнойИнформации[ВидКИ+"Организации"]);
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Организации") Тогда
				// Если у подразделения документа стоит флаг "ИспользоватьКИПодразделенияПриПечати",
				// то КИ выводим для подразделения.
				Если ИспользоватьКИПодразделенияПриПечати Тогда
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ДополнительныеПараметры.ПодразделениеКомпании, Справочники.ВидыКонтактнойИнформации[ВидКИ+"ПодразделенияКомпании"]);
				Иначе
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации[ВидКИ+"Организации"]);
				КонецЕсли;
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Контрагенты") Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации[ВидКИ+"Контрагента"]);
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="ТЕЛЕФОНРАБОЧИЙ" Тогда
			
			Если ТипОбъекта = ТипПодразделение Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации.ТелефонПодразделенияКомпании);
				Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект.Организация,Справочники.ВидыКонтактнойИнформации.ТелефонОрганизации);
				КонецЕсли;
			ИначеЕсли ТипОбъекта = ТипСклад Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект.Организация,Справочники.ВидыКонтактнойИнформации.ТелефонОрганизации);
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Организации") Тогда
				// Если у подразделения документа стоит флаг "ИспользоватьКИПодразделенияПриПечати",
				// то КИ выводим для подразделения.
				Если ИспользоватьКИПодразделенияПриПечати Тогда
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ДополнительныеПараметры.ПодразделениеКомпании, Справочники.ВидыКонтактнойИнформации.ТелефонПодразделенияКомпании);
				Иначе
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации.ТелефонОрганизации);
				КонецЕсли;
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Контрагенты") Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации.ТелефонКонтрагента);
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="ФАКС" Тогда
			
			Если ТипОбъекта = ТипПодразделение Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации.ФаксПодразделенияКомпании);
				Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект.Организация,Справочники.ВидыКонтактнойИнформации.ФаксОрганизации);
				КонецЕсли;
			ИначеЕсли ТипОбъекта = ТипСклад Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект.Организация,Справочники.ВидыКонтактнойИнформации.ФаксОрганизации);
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Организации") Тогда
				// Если у подразделения документа стоит флаг "ИспользоватьКИПодразделенияПриПечати",
				// то КИ выводим для подразделения.
				Если ИспользоватьКИПодразделенияПриПечати Тогда
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ДополнительныеПараметры.ПодразделениеКомпании, Справочники.ВидыКонтактнойИнформации.ФаксПодразделенияКомпании);
				Иначе
					ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации.ФаксОрганизации);
				КонецЕсли;
			ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Контрагенты") Тогда
				ЗначениеПараметра = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Объект,Справочники.ВидыКонтактнойИнформации.ФаксКонтрагента);
			КонецЕсли;
	
		ИначеЕсли ВРег(Параметр.Ключ)="БАНКОВСКИЙСЧЕТ" Тогда
			
			Если ЗначениеЗаполнено(РасчетныйСчетДокумента) Тогда
				ЗначениеПараметра = СокрЛП(РасчетныйСчетДокумента.НомерСчета);
			Иначе
				Если ЕстьРеквизит(Объект, "ОсновнойБанковскийСчет") Тогда
					ЗначениеПараметра = СокрЛП(Объект.ОсновнойБанковскийСчет.НомерСчета);
					Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) И ТипОбъекта=ТипПодразделение Тогда
						ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.НомерСчета);
					КонецЕсли;
				Иначе
					ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.НомерСчета);
				КонецЕсли;
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="БАНК" Тогда
			
			Если ЗначениеЗаполнено(РасчетныйСчетДокумента) Тогда
				ЗначениеПараметра = СокрЛП(РасчетныйСчетДокумента.Банк);
			Иначе
				Если ЕстьРеквизит(Объект, "ОсновнойБанковскийСчет") Тогда
					ЗначениеПараметра = СокрЛП(Объект.ОсновнойБанковскийСчет.Банк);
					Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) И ТипОбъекта=ТипПодразделение Тогда
						ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.Банк);
					КонецЕсли;
				Иначе
					ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.Банк);
				КонецЕсли;
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="БИК" Тогда
			
			Если ЗначениеЗаполнено(РасчетныйСчетДокумента) Тогда
				ЗначениеПараметра = СокрЛП(РасчетныйСчетДокумента.Банк.Код);
			Иначе
				Если ЕстьРеквизит(Объект, "ОсновнойБанковскийСчет") Тогда
					ЗначениеПараметра = СокрЛП(Объект.ОсновнойБанковскийСчет.Банк.Код);
					Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) И ТипОбъекта=ТипПодразделение Тогда
						ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.Банк.Код);
					КонецЕсли;
				Иначе
					ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.Банк.Код);
				КонецЕсли;
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="КОРРСЧЕТ" Тогда
			
			Если ЗначениеЗаполнено(РасчетныйСчетДокумента) Тогда
				ЗначениеПараметра = СокрЛП(РасчетныйСчетДокумента.Банк.КоррСчет);
			Иначе
				Если ЕстьРеквизит(Объект, "ОсновнойБанковскийСчет") Тогда
					ЗначениеПараметра = СокрЛП(Объект.ОсновнойБанковскийСчет.Банк.КоррСчет);
					Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) И ТипОбъекта=ТипПодразделение Тогда
						ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.Банк.КоррСчет);
					КонецЕсли;
				Иначе
					ЗначениеПараметра = СокрЛП(Объект.Организация.ОсновнойБанковскийСчет.Банк.КоррСчет);
				КонецЕсли;
			КонецЕсли;
			
		ИначеЕсли ВРег(Параметр.Ключ)="ПЕЧАТЬЗНАЧЕНИЯПАРАМЕТРА" Тогда
			
			ЗначениеПараметра = "ПЕЧАТЬЗНАЧЕНИЯПАРАМЕТРА";
			
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
			Продолжить;
		КонецЕсли; 
		Если НЕ ПустаяСтрока(Представление) Тогда
			Представление = Представление + ", ";
		КонецЕсли;
		Если Параметр.Значение<>Неопределено Тогда
			Представление = Представление + Параметр.Значение;
		КонецЕсли; 
		Если ЗначениеПараметра<>"ПЕЧАТЬЗНАЧЕНИЯПАРАМЕТРА" Тогда
			Представление = Представление + ЗначениеПараметра;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Представление;
	
КонецФункции //ПолучитьПредставлениеСправочника()

// Полное наименование справочника, если есть.
Функция ПолучитьНаименованиеСправочника(Объект, НаДату = Неопределено) Экспорт
	Если Объект = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	Если ЕстьРеквизит(Объект,"НаименованиеПолное") Тогда
		Если ЗначениеЗаполнено(Объект.НаименованиеПолное) Тогда
			НаименованиеПолное = Объект.НаименованиеПолное;
			
			Если ЗначениеЗаполнено(НаДату) Тогда
				СтруктураНаименований = ОбщегоНазначения.ПолучитьНаименованияНаДату(Объект, НаДату);
				НаименованиеПолное = СтруктураНаименований.НаименованиеПолное;
			КонецЕсли;
				
			Возврат НаименованиеПолное;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Объект.Наименование;
КонецФункции

// Строковое представление ставки НДС
Функция ПолучитьПредставлениеСтавкиНДС(СтавкаНДС, Процент="") Экспорт
	
	Если СтавкаНДС = ПредопределенноеЗначение("Справочник.СтавкиНДС.БезНДС") Тогда
		
		Возврат НСтр("ru = 'Без НДС'");
		
	ИначеЕсли СтавкаНДС = ПредопределенноеЗначение("Справочник.СтавкиНДС.ОсновнаяСтавкаНДСРасчетная")
		ИЛИ СтавкаНДС = ПредопределенноеЗначение("Справочник.СтавкиНДС.РасчетнаяСтавкаНДС10") Тогда
		
		Возврат Строка(СтавкаНДС) + Процент;
		
	КонецЕсли;
	
	Возврат СокрЛП(СтавкаНДС.Ставка) + Процент;
	
КонецФункции

// Функция заполнения структуры печатной строки по строке табличной части
Функция ЗаполнитьПредставлениеДанныхТоварнойСтроки(СтрокаТЧ, Документ, ИмяТабличнойЧасти="Товары") Экспорт
	СтруктураСтроки = Новый Структура;
	
	// заполнение номенклатуры и характеристики
	ТоварНаименование = ПолучитьНаименованиеСправочника(СтрокаТЧ.Номенклатура);
	
	Если ЕстьРеквизит(Документ,"ХарактеристикаНоменклатуры",ИмяТабличнойЧасти) Тогда
		Если ЗначениеЗаполнено(СтрокаТЧ.ХарактеристикаНоменклатуры) Тогда
			ТоварНаименование = ТоварНаименование + "," + ПолучитьНаименованиеСправочника(СтрокаТЧ.ХарактеристикаНоменклатуры);
		КонецЕсли;
	КонецЕсли;
	
	СтруктураСтроки.Вставить("НомерСтроки",СтрокаТЧ.НомерСтроки);
	СтруктураСтроки.Вставить("ТоварНаименование",ТоварНаименование);
	СтруктураСтроки.Вставить("Номенклатура",СтрокаТЧ.Номенклатура);
	
	// получение кода
	СтруктураСтроки.Вставить("Код", ПолучитьЗначениеКолонкиКода(СтрокаТЧ.Номенклатура));
	
	ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
	ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
	
	// если количество не равно базовому, корректируем вывод
	Если ЕстьРеквизит(Документ, "КоличествоБазовое", ИмяТабличнойЧасти) Тогда
		
		Если Окр(СтрокаТЧ.Количество * СтрокаТЧ.Коэффициент,3) <> СтрокаТЧ.КоличествоБазовое Тогда
			Количество        = СтрокаТЧ.КоличествоБазовое;
			Если ЕстьРеквизит(СтрокаТЧ.Номенклатура,"БазоваяЕдиницаИзмерения") Тогда
				ЕдиницаИзмерения  = СтрокаТЧ.Номенклатура.БазоваяЕдиницаИзмерения;
				Коэффициент       = 1;
			Иначе
				ЕдиницаИзмерения  = СтрокаТЧ.ЕдиницаИзмерения;
				Коэффициент       = СтрокаТЧ.ЕдиницаИзмерения.Коэффициент;
			КонецЕсли;
		Иначе
			Количество        = СтрокаТЧ.Количество;
			ЕдиницаИзмерения  = СтрокаТЧ.ЕдиницаИзмерения;
			Коэффициент       = СтрокаТЧ.ЕдиницаИзмерения.Коэффициент;
		КонецЕсли;
		
		СтруктураСтроки.Вставить("Количество"      , Формат(Количество, ФорматВыводаКоличества));
		СтруктураСтроки.Вставить("ЕдиницаИзмерения", ЕдиницаИзмерения);
		
	Иначе
		
		Если ЕстьРеквизит(Документ, "Количество", ИмяТабличнойЧасти) Тогда
			Количество = СтрокаТЧ.Количество;
		Иначе
			Количество = 0;
		КонецЕсли;
		
		Если ЕстьРеквизит(Документ, "ЕдиницаИзмерения" , ИмяТабличнойЧасти) Тогда
			ЕдиницаИзмерения  = СтрокаТЧ.ЕдиницаИзмерения;
			Коэффициент       = СтрокаТЧ.ЕдиницаИзмерения.Коэффициент;
		Иначе
			ЕдиницаИзмерения = "-";
			Если ЕстьРеквизит(Документ, "Коэффициент", ИмяТабличнойЧасти) Тогда
				Коэффициент = СтрокаТЧ.Коэффициент;
			Иначе
				Коэффициент = 0;
			КонецЕсли;
		КонецЕсли;
		
		СтруктураСтроки.Вставить("Количество"      , Формат(Количество, ФорматВыводаКоличества));
		СтруктураСтроки.Вставить("ЕдиницаИзмерения", ЕдиницаИзмерения);
	КонецЕсли;
	
	// Масса брутто
	МассаБрутто = 0;
	Если ЕстьРеквизит(Документ, "ЕдиницаИзмерения" , ИмяТабличнойЧасти) И ЕстьРеквизит(Документ, "Количество" , ИмяТабличнойЧасти) Тогда
		МассаБрутто = СтрокаТЧ.ЕдиницаИзмерения.Вес * СтрокаТЧ.Количество;
	КонецЕсли;
	СтруктураСтроки.Вставить("МассаБрутто", МассаБрутто);
		
	// цена
	Если ЕстьРеквизит(Документ, "Цена", ИмяТабличнойЧасти) Тогда
		СтруктураСтроки.Вставить("Цена", Формат(СтрокаТЧ.Цена, ФорматВыводаСуммы));
	КонецЕсли;
	
	// ставка НДС
	Если ЕстьРеквизит(Документ,"СтавкаНДС", ИмяТабличнойЧасти) Тогда
		СтруктураСтроки.Вставить("СтавкаНДС", ПолучитьПредставлениеСтавкиНДС(СтрокаТЧ.СтавкаНДС));
	КонецЕсли;
	
	// сумма НДС
	ЕстьСуммаНДС = ЕстьРеквизит(Документ, "СуммаНДС", ИмяТабличнойЧасти);
	Если ЕстьСуммаНДС Тогда
		СтруктураСтроки.Вставить("СуммаНДС",Формат(СтрокаТЧ.СуммаНДС, ФорматВыводаСуммы));
		Если СтрокаТЧ.СтавкаНДС = Справочники.СтавкиНДС.БезНДС Тогда // вместо пустой суммы выведем прочерк
			СтруктураСтроки.СуммаНДС = "-";
		КонецЕсли;
	КонецЕсли;
	
	// сумма всего
	ЕстьСуммаВсего = ЕстьРеквизит(Документ, "СуммаВсего", ИмяТабличнойЧасти);
	Если ЕстьСуммаВсего Тогда
		СтруктураСтроки.Вставить("СуммаВсего", Формат(СтрокаТЧ.СуммаВсего, ФорматВыводаСуммы));
	КонецЕсли;
	
	// сумма без НДС
	Если ЕстьСуммаНДС И ЕстьСуммаВсего Тогда
		СуммаБезНДС = СтрокаТЧ.СуммаВсего - СтрокаТЧ.СуммаНДС;
	ИначеЕсли ЕстьСуммаВсего Тогда
		СуммаБезНДС = СтрокаТЧ.СуммаВсего;
	ИначеЕсли ЕстьРеквизит(Документ, "Сумма", ИмяТабличнойЧасти) Тогда
		СуммаБезНДС = СтрокаТЧ.Сумма;
	Иначе
		СуммаБезНДС = 0;
	КонецЕсли;
	СтруктураСтроки.Вставить("СуммаБезНДС", Формат(СуммаБезНДС, ФорматВыводаСуммы));
	
	// цена без НДС
	Количество = Количество * Коэффициент;
	ЦенаБезНДС = ?(Количество = 0, 0, СуммаБезНДС / Количество);
	СтруктураСтроки.Вставить("ЦенаБезНДС", Формат(ЦенаБезНДС, ФорматВыводаСуммы));
	
	// розница
	Если ЕстьРеквизит(Документ, "ЦенаРозничная", ИмяТабличнойЧасти) Тогда
		СтруктураСтроки.Вставить("ЦенаРозничная", Формат(СтрокаТЧ.ЦенаРозничная,ФорматВыводаСуммы));
	КонецЕсли;
	
	Если ЕстьРеквизит(Документ, "СуммаРозничная", ИмяТабличнойЧасти) Тогда
		СтруктураСтроки.Вставить("СуммаРозничная", Формат(СтрокаТЧ.СуммаРозничная,ФорматВыводаСуммы));
	КонецЕсли;
	
	// скидки
	ПредставлениеСкидки = 0;
	Если ЕстьРеквизит(Документ, "СуммаСкидки", ИмяТабличнойЧасти) Тогда
		ПредставлениеСкидки = ПредставлениеСкидки + СтрокаТЧ.СуммаСкидки;
	КонецЕсли;
	
	Если ЕстьРеквизит(Документ, "СуммаСкидкиСтроки", ИмяТабличнойЧасти) Тогда
		ПредставлениеСкидки = ПредставлениеСкидки + СтрокаТЧ.СуммаСкидкиСтроки;
	КонецЕсли;
	
	СтруктураСтроки.Вставить("ПредставлениеСкидки", Формат(ПредставлениеСкидки, ФорматВыводаСуммы));
	
	Возврат СтруктураСтроки;
КонецФункции

// Проверка на возможность вывода области, если не удалось - выводится горизонтальный разделитель.
Функция ВывестиГоризонтальнуюОбласть(ТабличныйДокумент, Область, ОбластьШапкаТаблицы=Неопределено, ОбластьПодвал=Неопределено, НомерСтраницы=1, СтруктураИтоговПоСтранице=Неопределено, Документ, мсвДопОбластиПодвала=Неопределено) Экспорт
	
	ИспользоватьРазбиениеНаСтраницы = ПравоПользователя("ИспользоватьРазбиениеНаСтраницы", Документ);
	
	// если не разбиваем, выводим и все
	Если Не ИспользоватьРазбиениеНаСтраницы Тогда
		ТабличныйДокумент.Вывести(Область);
		Возврат НомерСтраницы;
	КонецЕсли;
	
	// запишем их в один массив
	МассивОбластейДляПроверки = Новый Массив;
	МассивОбластейДляПроверки.Добавить(Область);
	Если ОбластьПодвал <> Неопределено Тогда
		МассивОбластейДляПроверки.Добавить(ОбластьПодвал);
	КонецЕсли;
	Если мсвДопОбластиПодвала <> Неопределено Тогда
		Для Каждого ТекОбласть Из мсвДопОбластиПодвала Цикл
			МассивОбластейДляПроверки.Добавить(ТекОбласть);
		КонецЦикла;
	КонецЕсли;
	
	
	// проверим, помещаются ли на странице
	РезультатПроверки = ТабличныйДокумент.ПроверитьВывод(МассивОбластейДляПроверки);
	
	// если не помещаются, то ...
	Если НЕ РезультатПроверки Тогда
		ВывестиИтогиПоСтранице(ТабличныйДокумент,ОбластьПодвал,СтруктураИтоговПоСтранице,Документ); //выводим итог по странице
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц(); //переходим на следующую страницу
		НомерСтраницы = НомерСтраницы + 1;
		Если ОбластьШапкаТаблицы <> Неопределено Тогда
			ТабличныйДокумент.Вывести(ОбластьШапкаТаблицы); //выводим шапку таблицы
		КонецЕсли;
	КонецЕсли;
	
	// выводим текущую область
	ТабличныйДокумент.Вывести(Область);
	
	Возврат НомерСтраницы;
	
КонецФункции

// инкремент структуры итогов
Процедура ДобавитьИтогиПоСтранице(СтрокаТЧ,СтруктураИтоговПоСтранице) Экспорт
	Для Каждого ЭлементСтруктуры Из СтруктураИтоговПоСтранице Цикл
		Если ЭлементСтруктуры.Ключ = "ВалютаДокумента" Тогда
			Продолжить;
		КонецЕсли;
		НовоеЗначение = ЭлементСтруктуры.Значение + СтрокаТЧ[ЭлементСтруктуры.Ключ];
		Если ЭлементСтруктуры.Ключ = "СуммаСкидки" Тогда
			НовоеЗначение = НовоеЗначение + ПолучитьЗначениеПараметраСтруктуры(СтрокаТЧ,"СуммаСкидкиСтроки",0);
		КонецЕсли;
		СтруктураИтоговПоСтранице.Вставить(ЭлементСтруктуры.Ключ,НовоеЗначение);
	КонецЦикла;
КонецПроцедуры

// вывод строки итогов по странице
Процедура ВывестиИтогиПоСтранице(ТабличныйДокумент, ОбластьПодвал, СтруктураИтоговПоСтранице, Документ) Экспорт
	Если ОбластьПодвал <> Неопределено Тогда
		Если СтруктураИтоговПоСтранице <> Неопределено Тогда
			ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
			ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
			Для Каждого ЭлементИтога Из СтруктураИтоговПоСтранице Цикл
				Если СтрНайти(ЭлементИтога.Ключ,"Кол") <> 0 Тогда
					ЗначениеПараметра = Формат(ЭлементИтога.Значение,ФорматВыводаКоличества);
				Иначе
					ЗначениеПараметра = Формат(ЭлементИтога.Значение,ФорматВыводаСуммы);
				КонецЕсли; 
				Попытка
					ОбластьПодвал.Параметры[ЭлементИтога.Ключ] = ЗначениеПараметра;
				Исключение
				КонецПопытки; 
			КонецЦикла;
		КонецЕсли;
		
		ТабличныйДокумент.Вывести(ОбластьПодвал);
	КонецЕсли;
КонецПроцедуры

// вывод штрих-кода или удаление его с формы
Процедура ОбработатьШтрихКодОбъекта(Область, Документ) Экспорт
	РисунокШК = Неопределено;
	
	// получим рисунок
	Для Каждого Рисунок Из Область.Рисунки Цикл
		Если Рисунок.Имя = "ШК" Тогда
			РисунокШК = Рисунок;
		КонецЕсли;
	КонецЦикла;
	
	Если РисунокШК = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если  Метаданные.РегистрыСведений.ШтрихКоды.Измерения.Объект.Тип.СодержитТип(ТипЗнч(Документ)) Тогда
		// получим штрих код документа
		Запрос = Новый Запрос(
		"ВЫБРАТЬ
		|	ШтрихКоды.ШтрихКод
		|ИЗ
		|	РегистрСведений.ШтрихКоды КАК ШтрихКоды
		|ГДЕ
		|	ШтрихКоды.Объект = &Объект
		|	И ШтрихКоды.Запрет = ЛОЖЬ");
		Запрос.УстановитьПараметр("Объект", Документ);
		
		РезультатЗапроса = Запрос.Выполнить();
	Иначе
		РезультатЗапроса = Неопределено;
	КонецЕсли;
	
	Если РезультатЗапроса = Неопределено ИЛИ РезультатЗапроса.Пустой() Тогда
		// удаляем рисунок
		Область.Рисунки.Удалить(РисунокШК);
	Иначе
		Выборка = РезультатЗапроса.Выбрать();
		Выборка.Следующий();
		
		ПараметрыШтрихкода = Новый Структура;
		ПараметрыШтрихкода.Вставить("Штрихкод",        Выборка.ШтрихКод);
		ПараметрыШтрихкода.Вставить("Ширина",          239); 
		ПараметрыШтрихкода.Вставить("Высота",          100);  
		ПараметрыШтрихкода.Вставить("ТипКода",         99);
		ПараметрыШтрихкода.Вставить("ОтображатьТекст", Истина);
		ПараметрыШтрихкода.Вставить("РазмерШрифта",    24);
		
		Попытка
			РисунокШК.Картинка = МенеджерОборудованияВызовСервера.ПолучитьКартинкуШтрихкода(ПараметрыШтрихкода);
		Исключение
			ЗаписьЖурналаРегистрации(НСтр("ru = 'Ошибка получения изображения штрихкода'", ОбщегоНазначенияКлиентСервер.КодОсновногоЯзыка()), УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		КонецПопытки;
		
	КонецЕсли;
КонецПроцедуры

// вывод строки свойств в параметр области
Процедура ЗаполнитьСвойствамиОбъекта(Область, Документ, ИмяПараметраВОбласти="СтрокаСвойств") Экспорт
	
	ПолучатьДопРеквизиты = УправлениеСвойствами.ИспользоватьДопРеквизиты(Документ);
	ПолучатьДопСведения  = УправлениеСвойствами.ИспользоватьДопСведения (Документ);
	
	Если НЕ(ПолучатьДопРеквизиты ИЛИ ПолучатьДопСведения) Тогда
		Возврат;
	КонецЕсли;
	
	// получим свойства документа
	Свойства = УправлениеСвойствами.ПолучитьЗначенияСвойств(Документ.Ссылка, ПолучатьДопРеквизиты, ПолучатьДопСведения);
	
	// получим строку
	СтрокаСвойств = "";
	Для Каждого Свойство Из Свойства Цикл
		Если НЕ ЗначениеЗаполнено(Свойство.Значение) Тогда
			Продолжить;
		КонецЕсли;
		
		Если НЕ ПустаяСтрока(СтрокаСвойств) Тогда
			СтрокаСвойств = СтрокаСвойств + Символы.ПС;
		КонецЕсли;
		
		СтрокаСвойств = СтрокаСвойств + Строка(Свойство.Свойство) + " : """ + Строка(Свойство.Значение) + """";
	КонецЦикла;
	
	Область.Параметры[ИмяПараметраВОбласти] = СтрокаСвойств;
	
КонецПроцедуры

// получение ответственного лица
Функция ПолучитьТипОтветственногоЛица(ВидОтветственногоЛица)
	
	Если ТипЗнч(ВидОтветственногоЛица) <> Тип("Строка") Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если ВидОтветственногоЛица = "ГлавныйБухгалтер" Тогда
		Возврат Перечисления.ВидыОбъектовСведений.ГлавныйБухгалтер;
	ИначеЕсли ВидОтветственногоЛица = "Руководитель" Тогда
		Возврат Перечисления.ВидыОбъектовСведений.Руководитель;
	ИначеЕсли ВидОтветственногоЛица = "Кассир" Тогда
		Возврат Перечисления.ВидыОбъектовСведений.Кассир;
	ИначеЕсли ВидОтветственногоЛица = "МОЛ" Тогда
		Возврат Перечисления.ВидыОбъектовСведений.МОЛ;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции // ПолучитьТипОтветственногоЛица()

// получение данных отв. лица документа
Функция ДанныеОтветственногоЛица(Документ, ВидОтветственногоЛица, ВыводНаПечать="", Знач Организация=Неопределено) Экспорт
	ОтветственноеЛицо = Неопределено;
	
	
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		Организация = Документ.Организация;
	КонецЕсли;
	
	стрВидОтветственногоЛица = ?(ТипЗнч(ВидОтветственногоЛица) = Тип("Строка"), ВидОтветственногоЛица, Метаданные.Перечисления.ВидыОбъектовСведений.ЗначенияПеречисления[Перечисления.ВидыОбъектовСведений.Индекс(ВидОтветственногоЛица)].Имя);
	
	Если ПустаяСтрока(ВыводНаПечать) Тогда
		ВыводНаПечать = стрВидОтветственногоЛица;
	КонецЕсли;
	
	// пытаемся найти заполненное значение
	Свойства = УправлениеСвойствами.ПолучитьЗначенияСвойств(Документ.Ссылка,ЕстьРеквизит(Документ, , "ДополнительныеРеквизиты"));
	
	Попытка
		СвойствоПВХ = ?(ТипЗнч(ВидОтветственногоЛица) = Тип("Строка"), ПланыВидовХарактеристик.ДополнительныеРеквизитыИСведения[ВидОтветственногоЛица], ВидОтветственногоЛица);
	Исключение
		СвойствоПВХ = ПланыВидовХарактеристик.ДополнительныеРеквизитыИСведения.ПустаяСсылка();
	КонецПопытки;
	
	СтрокаСвойства = Свойства.Найти(СвойствоПВХ, "Свойство");
	Если СтрокаСвойства <> Неопределено Тогда
		ОтветственноеЛицо = СтрокаСвойства.Значение;
	КонецЕсли;
	
	// +Рарус Для корректной работы функции при вызове из справочника "Подтверждающие документы".
	Если Метаданные.Документы.Содержит(Документ.Метаданные()) Тогда
		Дата = Документ.Дата; 
	Иначе
		Дата = ТекущаяДатаСеанса();
	КонецЕсли;
	// -Рарус
	
	Если НЕ ЗначениеЗаполнено(ОтветственноеЛицо) Тогда
		Если ЕстьРеквизит(Документ, стрВидОтветственногоЛица) И ЗначениеЗаполнено(Документ[стрВидОтветственногоЛица]) Тогда
			ОтветственноеЛицо = Документ[стрВидОтветственногоЛица];
		Иначе
			// +Рарус Для корректной работы функции при вызове из справочника "Подтверждающие документы"
			//Если Метаданные.Документы.Содержит(Документ.Метаданные()) Тогда
			//	Дата = Документ.Дата; 
			//Иначе
			//	Дата = ТекущаяДатаСеанса();
			//КонецЕсли;
			
			ТипОтветственногоЛица = ПолучитьТипОтветственногоЛица(стрВидОтветственногоЛица);
			
			Если ТипОтветственногоЛица <> Неопределено Тогда
				СтруктураСведений = РегистрыСведений.СведенияКомпании.ПолучитьПоследнее(Дата, Новый Структура("Организация,Объект", Организация, ТипОтветственногоЛица));
				// -Рарус
				ОтветственноеЛицо = СтруктураСведений.Значение;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	
	// положим все в структуру
	СтруктураВозврата = Новый Структура;
	Если ЕстьРеквизит(ОтветственноеЛицо,"Должность") Тогда
		СтруктураВозврата.Вставить(ВыводНаПечать + "Должность", ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОтветственноеЛицо,"Должность"));
	Иначе
		СтруктураВозврата.Вставить(ВыводНаПечать + "Должность",);
	КонецЕсли;
	// +Рарус
	СтруктураВозврата.Вставить(ВыводНаПечать + "Представление", ?(ЗначениеЗаполнено(ОтветственноеЛицо),"/ ","") +  ПолучитьНаименованиеСправочника(ОтветственноеЛицо, Дата) + ?(ЗначениеЗаполнено(ОтветственноеЛицо)," /",""));
	// -Рарус
	СтруктураВозврата.Вставить(ВыводНаПечать, ОтветственноеЛицо);
	
	Возврат СтруктураВозврата;
	
КонецФункции

// набросок для торгов
Функция ПолучитьДанныеДокументаДляПечати(Объект) Экспорт
	ДанныеОбъекта     = Новый Структура();
	МетаданныеОбъекта = Объект.Метаданные();
	
	Для Каждого Реквизит Из МетаданныеОбъекта.Реквизиты Цикл
		ДанныеОбъекта.Вставить(Реквизит.Имя, Объект[Реквизит.Имя]);
	КонецЦикла;
	
	Для Каждого ТабличнаяЧасть Из МетаданныеОбъекта.ТабличныеЧасти Цикл
		ДанныеОбъекта.Вставить("ТабличнаяЧасть"+ТабличнаяЧасть.Имя, Объект[ТабличнаяЧасть.Имя].Выгрузить());
	КонецЦикла;
	
	Для Каждого РеквизитОбщий Из Метаданные.ОбщиеРеквизиты Цикл
		Если ЕстьРеквизит(Объект, РеквизитОбщий.Имя) Тогда
			ДанныеОбъекта.Вставить(РеквизитОбщий.Имя, Объект[РеквизитОбщий.Имя]);
		КонецЕсли;
	КонецЦикла;
	
	Возврат ДанныеОбъекта;
КонецФункции

// возвращает формат вывода количества
Функция ПолучитьФорматВыводаКоличества(Объект) Экспорт
	
	Формат = Константы.ФорматВыводаКоличества.Получить();
	
	Если НЕ ЗначениеЗаполнено(Формат) Тогда
		Формат = "ЧН=0";
	КонецЕсли;
	
	Возврат Формат;
	
КонецФункции

// возвращает формат вывода суммы
Функция ПолучитьФорматВыводаСуммы(Объект) Экспорт
	
	Формат = Константы.ФорматВыводаСуммы.Получить();
	
	Если НЕ ЗначениеЗаполнено(Формат) Тогда
		Формат = "ЧДЦ=2; ЧН=0,00";
	КонецЕсли;
	
	Возврат Формат;
	
КонецФункции

// Возвращает правильное склонение слова "день" по количеству дней.
//
// Параметры:
//  КолДней   - Число  - количество дней.
//
// Возвращаемое значение:
// Строка - Слово в нужном склонении.
//
Функция ПолучитьПредставлениеДня(Знач КолДней) Экспорт
	КолДней = Макс(КолДней, -КолДней); 
	ОстатокОтДеления = КолДней%10; 
	Если ОстатокОтДеления = 0 ИЛИ ОстатокОтДеления >=5 И ОстатокОтДеления <=9 
		ИЛИ КолДней%100 >= 10 И КолДней%100 <= 19 Тогда
		Возврат "дней";	
	ИначеЕсли ОстатокОтДеления = 1 Тогда
		Возврат "день";
	ИначеЕсли ОстатокОтДеления >=2 И ОстатокОтДеления <=4 Тогда
		Возврат "дня";
	КонецЕсли;		
КонецФункции //ПолучитьПредставлениеДня()

// Процедура добавляет команду параметров печати.
// 
Процедура ДобавитьКомандуПараметровПечати(Форма, МестоРазмещенияКоманд) Экспорт
	
	// Если печатных форм нет, то настройки параметров печати не нужны
	Если МестоРазмещенияКоманд.ПодчиненныеЭлементы.Количество()<2 Тогда
		Возврат;
	КонецЕсли;
	
	// Если не используются дополнительные реквизиты, то настройки параметров печати не нужны.
	Если НЕ Форма.ПолучитьФункциональнуюОпциюФормы("ИспользоватьДополнительныеРеквизитыИСведения") Тогда
		Возврат;
	КонецЕсли;
	
	МетаданныеОбъекта = УправлениеДиалогомСервер.ПолучитьМетаданныеОбъектаФормы(Форма);
	
	Если Метаданные.ЖурналыДокументов.Содержит(МетаданныеОбъекта) Тогда
		ЕстьРеквизитыПечати = Истина;
	Иначе
		МенеджерОбъекта  = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени(Форма.ИмяФормы);
		СсылкаНаОбъект = МенеджерОбъекта.ПустаяСсылка();
		
		ЕстьРеквизитыПечати = Ложь;
		Если ЕстьРеквизит(СсылкаНаОбъект,, "ДополнительныеРеквизиты") Тогда
			
			// Получение списка основного набора свойств
			НаборыСвойств = УправлениеСвойствамиСлужебный.ПолучитьОсновнойНаборСвойствДляОбъекта(СсылкаНаОбъект);
		
			// Получение списка доступных наборов дополнительных реквизитов со свойством только для печати.
			Запрос = Новый Запрос;
			Запрос.Текст = "
			|ВЫБРАТЬ ПЕРВЫЕ 1
			|	ИСТИНА КАК ЕстьРеквизитыПечати
			|ИЗ
			|	Справочник.НаборыДополнительныхРеквизитовИСведений.ДополнительныеРеквизиты КАК ДополнительныеРеквизиты
			|ГДЕ
			|	ДополнительныеРеквизиты.Свойство.ТолькоДляПечати = Истина И 
			|	ДополнительныеРеквизиты.Ссылка = &НаборыСвойств
			|";
			
			Запрос.УстановитьПараметр("НаборыСвойств", НаборыСвойств);
			РезультатЗапроса = Запрос.Выполнить();
			ЕстьРеквизитыПечати = (НЕ РезультатЗапроса.Пустой());
			
		КонецЕсли;
	КонецЕсли;
	
	// Если есть дополнительные реквизиты печати, добавим команду печати "Параметры печати".
	Если ЕстьРеквизитыПечати Тогда
		Если Форма.Элементы.Найти("ПодменюПараметрыПечати") = Неопределено Тогда
			ЗащищенныеФункцииСервер.ДобавитьГруппуКоманд(Форма,"ПодменюПараметрыПечати", МестоРазмещенияКоманд.Имя,Ложь ,"Настройки",Новый Картинка);
		КонецЕсли;
		ЗащищенныеФункцииСервер.ДобавитьКоманду(Форма, "ПараметрыПечати", "ПодменюПараметрыПечати",, НСтр("ru='Параметры печати'"), НСтр("ru='Настройка дополнительных параметров печати'"),ЛОЖЬ);
	КонецЕсли;
	
КонецПроцедуры // ДобавитьКомандуПараметровПечати()

// Процедура добавляет команду параметров печати.
// 
Процедура ДобавитьКомандуНастройкиПечатиКомплекта(Форма, МестоРазмещенияКоманд) Экспорт
	
	// Если печатных форм нет, то настройки параметров печати не нужны
	Если МестоРазмещенияКоманд.ПодчиненныеЭлементы.Количество()=0  Тогда
		Возврат;
	КонецЕсли;
	
	ЗащищенныеФункцииСервер.ДобавитьГруппуКоманд(Форма,"ПодменюПараметрыПечати", МестоРазмещенияКоманд.Имя,Ложь ,"Настройки",Новый Картинка);
	ЗащищенныеФункцииСервер.ДобавитьКоманду(Форма, "НастройкаКомплекта","ПодменюПараметрыПечати",, НСтр("ru='Настройка комплекта печати'"), НСтр("ru='Настройка комплекта печати'"),ЛОЖЬ);
	
КонецПроцедуры // ДобавитьКомандуПечатиКомплекта()

// Процедура добавляет команду параметров печати.
// 
Процедура ДобавитьКомандыПечатиКомплекта(Форма, МестоРазмещенияКоманд) Экспорт
	
	ИмяФормы = Форма.ИмяФормы;
	
	// Получим адрес хранения описания команд печати
	АдресКомандПечати = Форма.Команды.Найти("АдресКомандПечатиВоВременномХранилище");
	
	// Проверим использование печатных форм для данной формы
	Если АдресКомандПечати=Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ТипыОбъектовКомплекта = Новый Массив;
	ОбъектМетаданных = Метаданные.НайтиПоПолномуИмени(ИмяФормы);
	Если ОбъектМетаданных <> Неопределено Тогда
		ОбъектМетаданных = ОбъектМетаданных.Родитель();
	КонецЕсли;
	
	Если ОбъектМетаданных = Неопределено Тогда
		Возврат;
	Иначе
		Если ОбщегоНазначения.ЭтоЖурналДокументов(ОбъектМетаданных) Тогда
			Для каждого Документ Из ОбъектМетаданных.РегистрируемыеДокументы Цикл
				ТипыОбъектовКомплекта.Добавить("Документ." + Документ.Имя);
			КонецЦикла;
		ИначеЕсли Метаданные.Документы.Содержит(ОбъектМетаданных) Тогда
			ТипыОбъектовКомплекта.Добавить(Форма.ПолноеИмяОбъекта);
		КонецЕсли;
	КонецЕсли;
	
	Если ТипыОбъектовКомплекта.Количество() > 0 Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	НастройкаПечатиКомплекта.Ссылка КАК Ссылка,
		|	НастройкаПечатиКомплекта.Код КАК Код,
		|	НастройкаПечатиКомплекта.Наименование КАК Наименование
		|ПОМЕСТИТЬ ВозможныеКомплекты
		|ИЗ
		|	Справочник.НастройкаПечатиКомплекта КАК НастройкаПечатиКомплекта
		|ГДЕ
		|	НастройкаПечатиКомплекта.СписокПечатныхФорм.ТипДокумента В(&ТипДокумента)
		|	И НастройкаПечатиКомплекта.ПометкаУдаления = ЛОЖЬ
		|	И (НастройкаПечатиКомплекта.ТипДоступа = ЗНАЧЕНИЕ(Перечисление.ТипыДоступаКВариантуОтчета.БезОграничения)
		|			ИЛИ НастройкаПечатиКомплекта.ТипДоступа = ЗНАЧЕНИЕ(Перечисление.ТипыДоступаКВариантуОтчета.ПоПользователю)
		|				И НастройкаПечатиКомплекта.ОбъектДоступа = &Администратор
		|			ИЛИ НастройкаПечатиКомплекта.ТипДоступа = ЗНАЧЕНИЕ(Перечисление.ТипыДоступаКВариантуОтчета.ПоПодразделениюКомпании)
		|				И НастройкаПечатиКомплекта.ОбъектДоступа = &ПодразделениеКомпании
		|			ИЛИ НастройкаПечатиКомплекта.ТипДоступа = ЗНАЧЕНИЕ(Перечисление.ТипыДоступаКВариантуОтчета.ПоГруппеПользователей)
		|				И НастройкаПечатиКомплекта.ОбъектДоступа В
		|					(ВЫБРАТЬ
		|						ГруппыПользователейСостав.Ссылка
		|					ИЗ
		|						Справочник.ГруппыПользователей.Состав КАК ГруппыПользователейСостав
		|					ГДЕ
		|						ГруппыПользователейСостав.Пользователь = &Администратор))
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	НастройкаПечатиКомплектаСписокПечатныхФорм.ТипДокумента КАК ТипДокумента,
		|	ВозможныеКомплекты.Код КАК Код,
		|	ВозможныеКомплекты.Наименование КАК Наименование
		|ИЗ
		|	ВозможныеКомплекты КАК ВозможныеКомплекты
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.НастройкаПечатиКомплекта.СписокПечатныхФорм КАК НастройкаПечатиКомплектаСписокПечатныхФорм
		|		ПО ВозможныеКомплекты.Ссылка = НастройкаПечатиКомплектаСписокПечатныхФорм.Ссылка
		|
		|СГРУППИРОВАТЬ ПО
		|	НастройкаПечатиКомплектаСписокПечатныхФорм.ТипДокумента,
		|	ВозможныеКомплекты.Код,
		|	ВозможныеКомплекты.Наименование
		|ИТОГИ ПО
		|	Код";
		
		Запрос.УстановитьПараметр("ТипДокумента",ТипыОбъектовКомплекта);
		Запрос.УстановитьПараметр("ПодразделениеКомпании",ПараметрыСеанса.ПодразделениеКомпании);
		Запрос.УстановитьПараметр("Администратор",ПараметрыСеанса.ТекущийПользователь);
		Выборка = Запрос.Выполнить();
		
		// Получим список всех использующихся команд печати в форме
		КомандыПечати = ПолучитьИзВременногоХранилища(АдресКомандПечати.Действие);
		
		// Команды комплекта будем собирать в одной группе
		Если НЕ Выборка.Пустой() Тогда
			ЗащищенныеФункцииСервер.ДобавитьГруппуКоманд(Форма,"ПодменюКомплектыПечати", МестоРазмещенияКоманд.Имя,Ложь,НСтр("ru = 'Настройки'"),Новый Картинка,НСтр("ru = 'Настройки комплекта'"),"ПодменюПечатьГруппаПодменюПараметрыПечати");
		КонецЕсли;
		
		Выборка = Выборка.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
		Пока Выборка.Следующий() Цикл
			ИмяКоманды = "ПечатьКомплекта" + Выборка.Код;
			НоваяКоманда = КомандыПечати.Добавить();
			ВыборкаТипов = Выборка.Выбрать();
			ПредставлениеКоманды = ИмяКоманды;
			Пока ВыборкаТипов.Следующий() Цикл
				НоваяКоманда.ТипыОбъектовПечати.Добавить(Тип(СтрЗаменить(ВыборкаТипов.ТипДокумента,"Документ", "ДокументСсылка")));
				ПредставлениеКоманды = ВыборкаТипов.Наименование;
			КонецЦикла;
			НоваяКоманда.Идентификатор = ИмяКоманды;
			НоваяКоманда.ПроверкаПроведенияПередПечатью = Истина;
			НоваяКоманда.ПроверкаИсключенияЗапретаРедактирования = Ложь;
			НоваяКоманда.Представление = ПредставлениеКоманды;
			НоваяКоманда.ЭлементФормы = ИмяКоманды;
			НоваяКоманда.ПроверкаПроведенияПередПечатью = (НЕ ПравоПользователя("ПечатьНепроведенных",ОбъектМетаданных.Имя));
			НоваяКоманда.ПроверкаИсключенияЗапретаРедактирования = (ПравоПользователя("ИсключениеЗапретаРедактирования",ОбъектМетаданных.Имя));
			ЗащищенныеФункцииСервер.ДобавитьКоманду(Форма,ИмяКоманды,"ПодменюКомплектыПечати",, ПредставлениеКоманды, НСтр("ru='Печать комплекта'"),ЛОЖЬ,,);
		КонецЦикла;
	КонецЕсли;

КонецПроцедуры // ДобавитьКомандуПечатиКомплекта()

// Устанавливает для табличного документа стандартные параметры печати.
//
Процедура УстановитьСтандартныеПараметрыПечати(ИмяПараметровПечати,НастраиваемыйТабличныйДокумент) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	ПараметрыПечати = ХранилищеСистемныхНастроек.Загрузить(ИмяПараметровПечати+"_Стандартный",,,"Стандартный");
	Если НЕ ПараметрыПечати = Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастраиваемыйТабличныйДокумент,ПараметрыПечати);
		Если НЕ НастраиваемыйТабличныйДокумент.ИмяПараметровПечати = "" Тогда
			ХранилищеСистемныхНастроек.Удалить("Общее/НастройкиПечатиТабличногоДокумента/"+ИмяПараметровПечати,Неопределено,Строка(Пользователи.ТекущийПользователь()));	
		КонецЕсли;	
	КонецЕсли;
	
КонецПроцедуры //УстановитьСтандартныеПараметрыПечати()

// Сохраняет параметры печати в хранилище.
//
Процедура СохранитьТекущиеПараметрыКакСтандартные(ИмяПараметровПечати,НастраиваемыйТабличныйДокумент) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	// Подготовим структуру параметров
	ПараметрыПечати = Новый Структура();
	ПараметрыПечати.Вставить("АвтоМасштаб");
	ПараметрыПечати.Вставить("РазмерСтраницы");
	ПараметрыПечати.Вставить("ОриентацияСтраницы");
	ПараметрыПечати.Вставить("МасштабПечати");
	ПараметрыПечати.Вставить("ПолеСверху");
	ПараметрыПечати.Вставить("ПолеСлева");
	ПараметрыПечати.Вставить("ПолеСнизу");
	ПараметрыПечати.Вставить("ПолеСправа");
	ПараметрыПечати.Вставить("РазмерКолонтитулаСверху");
	ПараметрыПечати.Вставить("РазмерКолонтитулаСнизу");
	ПараметрыПечати.Вставить("ЭкземпляровНаСтранице");
	ПараметрыПечати.Вставить("ЧередованиеРасположенияСтраниц");
	ПараметрыПечати.Вставить("ДвусторонняяПечать");
	ПараметрыПечати.Вставить("ЧерноБелаяПечать");	
	
	ЗаполнитьЗначенияСвойств(ПараметрыПечати,НастраиваемыйТабличныйДокумент);
	ХранилищеСистемныхНастроек.Сохранить(НастраиваемыйТабличныйДокумент.ИмяПараметровПечати+"_Стандартный",,ПараметрыПечати,,"Стандартный");
	
КонецПроцедуры //СохранитьТекущиеПараметрыКакСтандартные()


