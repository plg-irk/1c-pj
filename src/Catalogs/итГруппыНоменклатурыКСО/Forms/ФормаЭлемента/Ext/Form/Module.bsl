
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Попытка
		ДД = Новый ДвоичныеДанные(Объект.КартинкаГруппы);	
		АдресКартинки = ПоместитьВоВременноеХранилище(ДД);
	Исключение
	КонецПопытки;
	
КонецПроцедуры
