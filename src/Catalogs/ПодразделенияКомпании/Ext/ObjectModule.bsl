// Модуль объекта справочника "Подразделения компании"

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда


////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

// Рекурсивная функция по поиску балансовых родителей у элемента.
//
// Параметры:
//  Ссылка - "СправочникСсылка.ПодразделенияКомпании" - текущее подразделение компании.
//
// Возвращаемое значение:
//   Ссылка - ссылка на балансового родителя.
// 
Функция НайтиБалансовогоРодителя(ТекущийРодитель)
	
	Если ТекущийРодитель.Пустая() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если ТекущийРодитель.Балансовое Тогда
		Возврат ТекущийРодитель;
	Иначе
		Возврат НайтиБалансовогоРодителя(ТекущийРодитель.Родитель);
	КонецЕсли;
	
КонецФункции // НайтиБалансовогоРодителя()

// Рекурсивная функция по поиску балансовых подразделений, подчиненных текущему
//
// Параметры:
//  Ссылка - "СправочникСсылка.ПодразделенияКомпании" - текущее подразделение.
//
// Возвращаемое значение:
//   Массив - массив подчиненных подразделений, по которым ведется балансовый учет.
// 
Функция НайтиБалансовыхДетей(Ссылка)
	
	Массив = Новый Массив;
	Если Ссылка.Пустая() Тогда
		Возврат Массив;
	КонецЕсли;
	
	Выборка = Справочники.ПодразделенияКомпании.ВыбратьИерархически(Ссылка);
	Пока Выборка.Следующий() Цикл
		Если Выборка.Балансовое Тогда
			Массив.Добавить(Строка(Выборка));
		КонецЕсли;
	КонецЦикла;
	
	Возврат Массив;
	
КонецФункции // НайтиБалансовыхДетей()


////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ ОСНОВНЫХ СОБЫТИЙ ОБЪЕКТА

// Обработчик события заполнения объекта как при создании нового, так и при вводе на основании существующего.
//
// Параметры:
//  ДанныеЗаполнения     - Произвольный - Значение, которое используется как основание для заполнения.
//  ТекстЗаполнения      - Строка       - Текст, используемый для формирования наименования/кода нового элемента.
//  СтандартнаяОбработка - Булево       - В данный параметр передается признак выполнения системной обработки события.
//
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка) Экспорт
	
	// Вызываем общий обработчик события
	Если НЕ ОбработкаСобытийСправочникаСервер.ОбработкаЗаполнения(ЭтотОбъект, ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка) Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры // ОбработкаЗаполнения()

// Обработчик события заполнения объекта копированием.
//
// Параметры:
//  ОбъектКопирования - СправочникОбъект - Исходный объект, который является источником копирования.
//
Процедура ПриКопировании(ОбъектКопирования)
	
	// Вызываем общий обработчик события
	Если НЕ ОбработкаСобытийСправочникаСервер.ПриКопировании(ЭтотОбъект, ОбъектКопирования) Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры // ПриКопировании()

// Обработчик события возникающего при необходимости проверки заполнения реквизитов при записи объекта.
//
// Параметры:
//  Отказ                - Булево - Признак отказа от продолжения работы после выполнения проверки заполнения.
//  ПроверяемыеРеквизиты - Массив - Массив путей к реквизитам, для которых будет выполнена проверка заполнения.
//
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	// Вызываем общий обработчик события
	Если НЕ ОбработкаСобытийСправочникаСервер.ОбработкаПроверкиЗаполнения(ЭтотОбъект, Отказ, ПроверяемыеРеквизиты) Тогда
		Возврат;
	КонецЕсли;
	
	Если ЭтотОбъект.Ссылка=Справочники.ПодразделенияКомпании.ОсновноеПодразделение И (НЕ ЭтотОбъект.Балансовое) Тогда
		ВывестиСообщение(НСтр("ru='Корневое подразделение не может быть небалансовым.'"),ЭтотОбъект,"Балансовое",,Отказ);
	КонецЕсли;
	
	// Строгое правило. В одной ветке может быть только одно балансовое подразделение.
	Если (НЕ ЭтотОбъект.Ссылка=Справочники.ПодразделенияКомпании.ОсновноеПодразделение) И ЭтотОбъект.Балансовое Тогда
		
		// Сначала поднимаемся вверх
		БалансовыйРодитель = НайтиБалансовогоРодителя(Родитель);
		Если (БалансовыйРодитель<>Неопределено) И (БалансовыйРодитель<>Справочники.ПодразделенияКомпании.ОсновноеПодразделение) Тогда
			ВывестиСообщение(НСтр("ru='У подразделения, по которому ведется балансовый учет, не может быть родительских подразделений, по которым также ведется балансовый учет (кроме корневого подразделения). Балансовое подразделение: %1'"),ЭтотОбъект,"Балансовое",,Отказ,БалансовыйРодитель);
		КонецЕсли;
		
		// Теперь опускаемся вниз.
		МассивБалансовыхДетей = НайтиБалансовыхДетей(ЭтотОбъект.Ссылка);
		Если МассивБалансовыхДетей.Количество()<>0 Тогда
			ПредставлениеБалансовыхДетей = "";
			Для каждого ТекПодразделение Из МассивБалансовыхДетей Цикл
				ПредставлениеБалансовыхДетей = ПредставлениеБалансовыхДетей + ", " + ТекПодразделение;
			КонецЦикла;
			ВывестиСообщение(НСтр("ru='У подразделения, по которому ведется балансовый учет, не может быть родительских подразделений, по которым также ведется балансовый учет (кроме корневого подразделения). Балансовое подразделение: %1'"),ЭтотОбъект,"Балансовое",,Отказ, Сред(ПредставлениеБалансовыхДетей, 3));
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЭтотОбъект.ОсновнойБанковскийСчет) Тогда
		ВладелецСчета = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЭтотОбъект.ОсновнойБанковскийСчет, "Владелец");
		Если НЕ (ВладелецСчета = ЭтотОбъект.Ссылка ИЛИ ВладелецСчета = ЭтотОбъект.Организация) Тогда
			ВывестиСообщение(НСтр("ru = 'Введен основной банковский счет, который не принадлежит данному подразделению/организации подразделения.'"), ЭтотОбъект,"ОсновнойБанковскийСчет",, Отказ);
		КонецЕсли;
	КонецЕсли;
	
	// Проверим корректность КПП
	ОшибкаКПП = "";
	Если НЕ РегламентированныеДанныеКлиентСервер.КППСоответствуетТребованиям(КПП,ОшибкаКПП) Тогда
		ВывестиСообщение(ОшибкаКПП);
	КонецЕсли;
	
КонецПроцедуры // ОбработкаПроверкиЗаполнения()

// Обработчик события возникающего после начала транзакции записи, но до начала записи объекта.
//
// Параметры:
//  Отказ - Булево - Признак отказа от совершения операции.
//
Процедура ПередЗаписью(Отказ)
	
	// Вызываем общий обработчик события
	Если НЕ ОбработкаСобытийСправочникаСервер.ПередЗаписью(ЭтотОбъект, Отказ) Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтотОбъект.Ссылка=Справочники.ПодразделенияКомпании.ОсновноеПодразделение Тогда
		
		// На нулевом уровне может находится только одно подразделение - "Вся компания"
		Если НЕ ЗначениеЗаполнено(ЭтотОбъект.Родитель) Тогда
			//ЭтотОбъект.Родитель = Справочники.ПодразделенияКомпании.ОсновноеПодразделение;
		КонецЕсли;
		
	КонецЕсли;
	
	ЭтотОбъект.ДополнительныеСвойства.Вставить("СтароеЗначениеРодителя",Ссылка.Родитель);
	РедактированиеИстории.ПередЗаписью(ЭтотОбъект);
	
КонецПроцедуры // ПередЗаписью()

// Обработчик события возникающего после записи объекта в базу данных, но до окончания транзакции записи.
//
// Параметры:
//  Отказ - Булево - Признак отказа от совершения операции.
//
Процедура ПриЗаписи(Отказ)
	
	// Вызываем общий обработчик события
	Если НЕ ОбработкаСобытийСправочникаСервер.ПриЗаписи(ЭтотОбъект, Отказ) Тогда
		Возврат;
	КонецЕсли;
	
	//Мальцев А.Ю. +++
	//Если ЭтотОбъект.ДополнительныеСвойства.СтароеЗначениеРодителя <> Родитель Тогда
	//	
	//	// При смене родителя или создании нового элемента необходимо обновить структуру иерархии для ограничения доступа.
	//	РегистрыСведений.ГруппыЗначенийДоступа.ОбновитьСтруктуруИерархииДоступаПодразделений();	
	//	РегистрыСведений.СтруктураИерархииПодразделений.ОбновитьСтруктуруИерархииПодразделений(Ссылка);
	//	
	//КонецЕсли;
	Если ЭтотОбъект.ДополнительныеСвойства.СтароеЗначениеРодителя <> Родитель Тогда	
		// При смене родителя или создании нового элемента необходимо обновить структуру иерархии для ограничения доступа.
		РегистрыСведений.ГруппыЗначенийДоступа.ОбновитьСтруктуруИерархииДоступаПодразделений();			
	КонецЕсли;
	РегистрыСведений.СтруктураИерархииПодразделений.ОбновитьСтруктуруИерархииПодразделений(Ссылка);	
	//Мальцев А.Ю. ---
	
КонецПроцедуры // ПриЗаписи()

// Обработчик события возникающего в транзакции удаления перед непосредственным удалением объекта из базы данных.
//
// Параметры:
//  Отказ - Булево - Признак отказа от совершения операции.
//
Процедура ПередУдалением(Отказ)
	
	// Вызываем общий обработчик события
	Если НЕ ОбработкаСобытийСправочникаСервер.ПередУдалением(ЭтотОбъект, Отказ) Тогда
		Возврат;
	КонецЕсли;
	
	// Почистим регистры сведений от удаляемого объекта
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	сфпЗаписиТелефонныхПереговоров.ИдентификаторЗвонка КАК ИдентификаторЗвонка,
	               |	сфпЗаписиТелефонныхПереговоров.ИдентификаторЗаписи КАК ИдентификаторЗаписи
	               |ИЗ
	               |	РегистрСведений.сфпЗаписиТелефонныхПереговоров КАК сфпЗаписиТелефонныхПереговоров
	               |ГДЕ
	               |	сфпЗаписиТелефонныхПереговоров.Контакт = &Ссылка";
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	УстановитьПривилегированныйРежим(Истина);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		МенеджерЗаписи = РегистрыСведений.сфпЗаписиТелефонныхПереговоров.СоздатьМенеджерЗаписи();
		ЗаполнитьЗначенияСвойств(МенеджерЗаписи, Выборка);
		МенеджерЗаписи.Удалить();
	КонецЦикла;
	
КонецПроцедуры // ПередУдалением()


////////////////////////////////////////////////////////////////////////////////
// ИСПОЛНЯЕМАЯ ЧАСТЬ МОДУЛЯ

#КонецЕсли