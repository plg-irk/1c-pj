
//Функция ЗаписатьСкидки(ТаблицаСкидок) Экспорт
//	
//	Запрос = Новый Запрос("ВЫБРАТЬ МАКСИМУМ(Идентификатор) КАК Идентификатор ИЗ РегистрСведений.СкидкиВОборудовании");
//	ПоследнийИдентификатор = 0;
//	Выборка=Запрос.Выполнить().Выбрать();
//	Если Выборка.Следующий() Тогда
//		ПоследнийИдентификатор=?(Выборка.Идентификатор=NULL,0,Выборка.Идентификатор);
//	КонецЕсли;
//	
//	СкидкаНаШтрихкод = Справочники.ТипыСкидок.СкидкаНаШтрихкодТовара;
//	
//	СкидкиНаШтрихкода = Новый Массив;
//	
//	ПоследнийИдентификатор=ПоследнийИдентификатор+1;
//	Для Каждого СтрокаСкидки Из ТаблицаСкидок Цикл
//		Если НЕ СтрокаСкидки.Пометка Тогда
//			Продолжить;
//		КонецЕсли;
//		
//		НоваяЗапись=РегистрыСведений.СкидкиВОборудовании.СоздатьМенеджерЗаписи();
//		НоваяЗапись.ПодразделениеКомпании=СтрокаСкидки.ПодразделениеКомпании;
//		НоваяЗапись.Номенклатура=СтрокаСкидки.Объект;
//		НоваяЗапись.НачалоДействия=СтрокаСкидки.НачалоДействия;
//		НоваяЗапись.КонецДействия=СтрокаСкидки.КонецДействия;
//		НоваяЗапись.НачВремя=СтрокаСкидки.НачВремя;
//		НоваяЗапись.КонВремя=СтрокаСкидки.КонВремя;
//		НоваяЗапись.ДниНедели=СтрокаСкидки.ДниНедели;
//		НоваяЗапись.ДисконтнаяКарта=СтрокаСкидки.ДисконтнаяКарта;
//		НоваяЗапись.СуммаЧека=СтрокаСкидки.ОтСуммыЧека;
//		НоваяЗапись.Количество=СтрокаСкидки.ОтКоличества;
//		НоваяЗапись.Правило=СтрокаСкидки.Правило;
//		НоваяЗапись.Скидка=СтрокаСкидки.Скидка;
//		НоваяЗапись.СуммаНакопления=СтрокаСкидки.ОтСуммыНакопленияНаКарте;
//		НоваяЗапись.РучнаяСкидка=СтрокаСкидки.РучнаяСкидка;
//		НоваяЗапись.Свойство=СтрокаСкидки.Свойство;
//		НоваяЗапись.Прочитать();
//		Если Не ЗначениеЗаполнено(НоваяЗапись.Идентификатор) Тогда
//			НоваяЗапись.ПодразделениеКомпании=СтрокаСкидки.ПодразделениеКомпании;
//			НоваяЗапись.Номенклатура=СтрокаСкидки.Объект;
//			НоваяЗапись.НачалоДействия=СтрокаСкидки.НачалоДействия;
//			НоваяЗапись.КонецДействия=СтрокаСкидки.КонецДействия;
//			НоваяЗапись.НачВремя=СтрокаСкидки.НачВремя;
//			НоваяЗапись.КонВремя=СтрокаСкидки.КонВремя;
//			НоваяЗапись.ДниНедели=СтрокаСкидки.ДниНедели;
//			НоваяЗапись.ДисконтнаяКарта=СтрокаСкидки.ДисконтнаяКарта;
//			НоваяЗапись.СуммаЧека=СтрокаСкидки.ОтСуммыЧека;
//			НоваяЗапись.Количество=СтрокаСкидки.ОтКоличества;
//			НоваяЗапись.Правило=СтрокаСкидки.Правило;
//			НоваяЗапись.Скидка=СтрокаСкидки.Скидка;
//			НоваяЗапись.СуммаНакопления=СтрокаСкидки.ОтСуммыНакопленияНаКарте;
//			НоваяЗапись.РучнаяСкидка=СтрокаСкидки.РучнаяСкидка;
//			НоваяЗапись.Свойство=СтрокаСкидки.Свойство;
//			НоваяЗапись.Идентификатор=ПоследнийИдентификатор;
//			ПоследнийИдентификатор=ПоследнийИдентификатор+1;
//		КонецЕсли;
//		НоваяЗапись.Наименование=СтрокаСкидки.Наименование;
//		НоваяЗапись.ЗначениеСкидки=СтрокаСкидки.ЗначениеСкидки;
//		НоваяЗапись.СпособВычисления=СтрокаСкидки.СпособВычисления;
//		НоваяЗапись.Действует=СтрокаСкидки.Действует;
//		НоваяЗапись.КонтрольнаяСуммаНабора=СтрокаСкидки.КонтрольнаяСуммаНабора;
//		НоваяЗапись.МоментИзменения=ТекущаяДата();
//		НоваяЗапись.ФлагВытеснения=СтрокаСкидки.ФлагВытеснения;
//		НоваяЗапись.ИдентификаторСкидкиВОборудовании=СтрокаСкидки.ИдентификаторСкидкиВОборудовании;
//		
//		Если СтрокаСкидки.Скидка = СкидкаНаШтрихкод Тогда
//			СкидкиНаШтрихкода.Добавить(СтрокаСкидки);
//		КонецЕсли;
//		
//		Если ЗначениеЗаполнено(СтрокаСкидки.КонтрольнаяСуммаНабора) И ЗначениеЗаполнено(СтрокаСкидки.Регистратор) Тогда
//			ЗапросНаборов = Новый Запрос;
//			ЗапросНаборов.УстановитьПараметр("КонтрольнаяСуммаНабора", СтрокаСкидки.КонтрольнаяСуммаНабора);
//			ЗапросНаборов.УстановитьПараметр("Регистратор",            СтрокаСкидки.Регистратор);
//			ЗапросНаборов.Текст = "
//			|ВЫБРАТЬ ПЕРВЫЕ 1
//			|	СкидкиСтроки.СоставНабора
//			|ИЗ
//			|	РегистрСведений.СкидкиСтроки КАК СкидкиСтроки
//			|ГДЕ
//			|	СкидкиСтроки.Регистратор = &Регистратор И 
//			|	СкидкиСтроки.КонтрольнаяСуммаНабора = &КонтрольнаяСуммаНабора
//			|";
//			Выборка = ЗапросНаборов.Выполнить().Выбрать();
//			Если Выборка.Следующий() Тогда
//				НоваяЗапись.СоставНабора = Выборка.СоставНабора;
//			КонецЕсли;
//		КонецЕсли;
//		
//		НоваяЗапись.Записать(Истина);
//	КонецЦикла;
//	
//	ТаблицаСкидок = Неопределено;
//	
//	
//	
//КонецФункции
