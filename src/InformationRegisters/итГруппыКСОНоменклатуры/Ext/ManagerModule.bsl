Процедура ДобавитьЗаписьГруппы(Номенклатура, Группа) Экспорт
	
	МЗ = РегистрыСведений.итГруппыКСОНоменклатуры.СоздатьМенеджерЗаписи();
	МЗ.Период = ТекущаяДата();
	МЗ.Номенклатура = Номенклатура;
	МЗ.ГруппаКСО = Группа;
	МЗ.Записать();		
	
	//Запрос = Новый Запрос;
	//Запрос.Текст = 
	//"ВЫБРАТЬ
	//|	итГруппыКСОНоменклатурыСрезПоследних.ГруппаКСО КАК ГруппаКСО
	//|ИЗ
	//|	РегистрСведений.итГруппыКСОНоменклатуры.СрезПоследних(, Номенклатура = &Номенклатура) КАК итГруппыКСОНоменклатурыСрезПоследних
	//|ГДЕ
	//|	итГруппыКСОНоменклатурыСрезПоследних.Удалена = ЛОЖЬ";
	//
	//Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	//РезультатЗапроса = Запрос.Выполнить();
	//
	//Если РезультатЗапроса.Пустой() Тогда
	//	МЗ = РегистрыСведений.итГруппыКСОНоменклатуры.СоздатьМенеджерЗаписи();
	//	МЗ.Период = ТекущаяДата();
	//	МЗ.Номенклатура = Номенклатура;
	//	МЗ.ГруппаКСО = Группа;
	//	МЗ.Записать();		
	//Иначе
	//	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	//	ВыборкаДетальныеЗаписи.Следующий();
	//	Если ВыборкаДетальныеЗаписи.ГруппаКСО <> Группа Тогда
	//		МЗ = РегистрыСведений.итГруппыКСОНоменклатуры.СоздатьМенеджерЗаписи();
	//		МЗ.Период = ТекущаяДата();
	//		МЗ.Номенклатура = Номенклатура;
	//		МЗ.ГруппаКСО = Группа;
	//		МЗ.Записать();
	//	КонецЕсли;
	//КонецЕсли;
	
КонецПроцедуры