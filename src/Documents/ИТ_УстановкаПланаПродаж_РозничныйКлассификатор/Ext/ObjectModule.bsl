
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Если Не(ЗначениеЗаполнено(ПериодПлана)) Тогда 
		ПериодПлана = НачалоМесяца(ДобавитьМесяц(ТекущаяДата(), 1));
	КонецЕсли;	
	
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Движения.ИТ_ПланПродаж_РозничныйКлассификатор.Записывать = Истина;
	Движения.ИТ_ПланПродаж_РозничныйКлассификатор.Загрузить(ПланПродаж.Выгрузить());
	
КонецПроцедуры
