
//&НаСервере
//Процедура ПоискВСД()
//	
//	Запрос = Новый Запрос;
//	
//	ТекстЗапроса = "";
//	Если ЗначениеЗаполнено(НомерТТН) И ЗначениеЗаполнено(ДатаТТН) Тогда
//		ТекстЗапроса = "
//		|ВЫБРАТЬ
//		|	МеркурийВСД.Ссылка КАК ВСД
//		|ИЗ
//		|	Документ.МеркурийВСД.СвязанныеДокументы КАК МеркурийВСД
//		|ГДЕ
//		|	МеркурийВСД.Номер = &НомерТТН И 
//		|	МеркурийВСД.Дата = &ДатаТТН И 
//		|	(МеркурийВСД.Ссылка.Статус = ЗНАЧЕНИЕ(Перечисление.МеркурийСтатусыВСД.Погашен) ИЛИ 
//		|	МеркурийВСД.Ссылка.Статус = ЗНАЧЕНИЕ(Перечисление.МеркурийСтатусыВСД.Оформлен))
//		|";
//		Запрос.УстановитьПараметр("НомерТТН", НомерТТН);
//		Запрос.УстановитьПараметр("ДатаТТН",  ДатаТТН);
//	ИначеЕсли ЗначениеЗаполнено(НомерТТН) Тогда
//		ТекстЗапроса = "
//		|ВЫБРАТЬ
//		|	МеркурийВСД.Ссылка КАК ВСД
//		|ИЗ
//		|	Документ.МеркурийВСД.СвязанныеДокументы КАК МеркурийВСД
//		|ГДЕ
//		|	МеркурийВСД.Номер = &НомерТТН И 
//		|	(МеркурийВСД.Ссылка.Статус = ЗНАЧЕНИЕ(Перечисление.МеркурийСтатусыВСД.Погашен) ИЛИ 
//		|	МеркурийВСД.Ссылка.Статус = ЗНАЧЕНИЕ(Перечисление.МеркурийСтатусыВСД.Оформлен))
//		|";
//		Запрос.УстановитьПараметр("НомерТТН",  НомерТТН);
//	ИначеЕсли ЗначениеЗаполнено(ДатаТТН) Тогда
//		ТекстЗапроса = "
//		|ВЫБРАТЬ
//		|	МеркурийВСД.Ссылка КАК ВСД
//		|ИЗ
//		|	Документ.МеркурийВСД.СвязанныеДокументы КАК МеркурийВСД
//		|ГДЕ
//		|	МеркурийВСД.Дата = &ДатаТТН И 
//		|	(МеркурийВСД.Ссылка.Статус = ЗНАЧЕНИЕ(Перечисление.МеркурийСтатусыВСД.Погашен) ИЛИ 
//		|	МеркурийВСД.Ссылка.Статус = ЗНАЧЕНИЕ(Перечисление.МеркурийСтатусыВСД.Оформлен))
//		|";
//		Запрос.УстановитьПараметр("ДатаТТН", ДатаТТН);
//	КонецЕсли;
//	
//	УстанавливатьОтбор = Ложь;
//	ДоступныеВСД.Очистить();
//	Если НЕ ТекстЗапроса = "" Тогда
//		
//		УстанавливатьОтбор = Истина;
//		
//		Запрос.Текст = ТекстЗапроса;
//		
//		МассивВСД = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("ВСД");
//		
//		ДоступныеВСД.ЗагрузитьЗначения(МассивВСД);
//		
//	КонецЕсли;
//	
//	ОбновитьОтборПоВСД(УстанавливатьОтбор);
//	
//КонецПроцедуры


////Процедура ОтправительПриИзменении(Элемент)
////	
////	СписокВСД.Отбор.Отправитель.Использование = ЗначениеЗаполнено(ЭлементыФормы.Отправитель.Значение);
////	Если ЗначениеЗаполнено(ДатаТТН) ИЛИ ЗначениеЗаполнено(НомерТТН) Тогда
////		ПоискВСД();
////	КонецЕсли;
////	
////КонецПроцедуры

////Процедура ПредприятиеОтправительПриИзменении(Элемент)
////	
////	СписокВСД.Отбор.ПредприятиеОтправитель.Использование = ЗначениеЗаполнено(ЭлементыФормы.ПредприятиеОтправитель.Значение);
////	Если ЗначениеЗаполнено(ДатаТТН) ИЛИ ЗначениеЗаполнено(НомерТТН) Тогда
////		ПоискВСД();
////	КонецЕсли;
////	
////КонецПроцедуры

////Процедура ПолучательПриИзменении(Элемент)
////	
////	СписокВСД.Отбор.Получатель.Использование = ЗначениеЗаполнено(ЭлементыФормы.Получатель.Значение);
////	Если ЗначениеЗаполнено(ДатаТТН) ИЛИ ЗначениеЗаполнено(НомерТТН) Тогда
////		ПоискВСД();
////	КонецЕсли;
////	
////КонецПроцедуры

////Процедура ПредприятиеПолучательПриИзменении(Элемент)
////	
////	СписокВСД.Отбор.ПредприятиеПолучатель.Использование = ЗначениеЗаполнено(ЭлементыФормы.ПредприятиеПолучатель.Значение);
////	Если ЗначениеЗаполнено(ДатаТТН) ИЛИ ЗначениеЗаполнено(НомерТТН) Тогда
////		ПоискВСД();
////	КонецЕсли;
////	
////КонецПроцедуры

////Процедура НомерТТНПриИзменении(Элемент)
////	
////	ПоискВСД();
////	
////КонецПроцедуры

////Процедура ДатаТТНПриИзменении(Элемент)
////	
////	ПоискВСД();
////	
////КонецПроцедуры

////Процедура СписокВСДПередНачаломДобавления(Элемент, Отказ, Копирование)
////	
////	Если Копирование Тогда
////		Возврат;
////	КонецЕсли;
////	
////	Отказ = Истина;
////	
////	ВСДОбъект = Документы.МеркурийВСД.СоздатьДокумент();
////	ВСДОбъект.Заполнить(Неопределено);
////	ВСДОбъект.Дата                   = ТекущаяДата();
////	ВСДОбъект.Отправитель            = Элементыформы.Отправитель.Значение;
////	ВСДОбъект.Контрагент             = Элементыформы.Отправитель.Значение.Контрагент;
////	ВСДОбъект.ПредприятиеОтправитель = Элементыформы.ПредприятиеОтправитель.Значение;
////	ВСДОбъект.Получатель             = Элементыформы.Получатель.Значение;
////	ВСДОбъект.ПредприятиеПолучатель  = Элементыформы.ПредприятиеПолучатель.Значение;
////	ВСДОбъект.НомерТТН               = Элементыформы.НомерТТН.Значение;
////	ВСДОбъект.ДатаТТН                = Элементыформы.ДатаТТН.Значение;
////	ВСДОбъект.ВидПродукта            = Элементыформы.ВидПродукта.Значение;
////	ВСДОбъект.Продукт                = Элементыформы.Продукт.Значение;
////	ВСДОбъект.Статус                 = Перечисления.МеркурийСтатусыВСД.Оформлен;
////	ВСДОбъект.ХозОперация            = Справочники.ХозОперации.МеркурийВходящийВСД;
////	ВСДОбъект.ТипТТН                 = 1;
////	//ВСДОбъект.СтранаПроисхождения    = 
////	//ВСДОбъект.РольПроизводителя
////	
////	ФормаВСД = ВСДОбъект.ПолучитьФорму(,Элемент);
////	ФормаВСД.Открыть();
////	
////КонецПроцедуры

////Процедура ПредприятиеОтправительНачалоВыбора(Элемент, СтандартнаяОбработка)
////	
////	глВнешнийМодульОбменаМеркурий.МеркурийПредприятиеПолучательНачалоВыбора(ЭтаФОрма, ЭлементыФормы.Отправитель.Значение, ЭлементыФормы.ПредприятиеОтправитель.Значение, Элемент, СтандартнаяОбработка);
////	
////КонецПроцедуры

////Процедура ПредприятиеПолучательНачалоВыбора(Элемент, СтандартнаяОбработка)
////	
////	глВнешнийМодульОбменаМеркурий.МеркурийПредприятиеПолучательНачалоВыбора(ЭтаФОрма, ЭлементыФормы.Получатель.Значение, ЭлементыФормы.ПредприятиеПолучатель.Значение, Элемент, СтандартнаяОбработка);
////	
////КонецПроцедуры

////Процедура СписокВСДПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
////	
////	Если (НЕ Товары.Найти(ДанныеСтроки.Ссылка, "ВСД") = Неопределено) Тогда
////		ОформлениеСтроки.ЦветТекста = Новый Цвет(0,128,0);
////	КонецЕсли;
////	
////КонецПроцедуры

////Процедура СписокВСДВыбор(Элемент, ВыбраннаяСтрока, Колонка, СтандартнаяОбработка)
////	
////	//ТекущиеДанные = ЭлементыФормы.СписокВСД.ТекущиеДанные;
////	//
////	//Если ТекущиеДанные = Неопределено Тогда
////	//	Возврат;
////	//КонецЕсли;
////	//
////	//СтандартнаяОбработка = Ложь;
////	//ПоискЗначения = Товары.Найти(ТекущиеДанные.Ссылка, "Ссылка");
////	//Если ПоискЗначения = Неопределено Тогда
////	//	ПоискЗначения = Товары.Добавить();
////	//КонецЕсли;
////	//
////	//ЗаполнитьЗначенияСвойств(ПоискЗначения, ТекущиеДанные);
////	
////КонецПроцедуры

//Процедура ОсновныеДействияФормыДействиеОК(Кнопка)
//	
//	Закрыть(Товары);
//	
//КонецПроцедуры

//Процедура ПодобранныеВСДПередНачаломДобавления(Элемент, Отказ, Копирование)
//	
//	Отказ = Истина;
//	
//КонецПроцедуры

//Процедура ПодобранныеВСДПередУдалением(Элемент, Отказ)
//	
//	Отказ = Истина;
//	
//КонецПроцедуры

//Процедура ПодобранныеВСДПриПолученииДанных(Элемент, ОформленияСтрок)
//	
//	глВнешнийМодульОбменаМеркурий.ГашениеВСДФормаТоварыПриПолученииДанных(ДокументОбъект, ЭтаФорма, Элемент, ОформленияСтрок, Товары);
//	
//КонецПроцедуры

//Процедура СписокВСДПриАктивизацииСтроки(Элемент)
//	
//	ТекущиеДанные = ЭлементыФормы.СписокВСД.ТекущаяСтрока;
//	Если ТекущиеДанные = Неопределено тогда
//		Возврат;
//	КонецЕсли;
//	
//	ТекущаяСтрока = Товары.Найти(ТекущиеДанные.Ссылка, "ВСД");
//	Если НЕ ТекущаяСтрока = Неопределено Тогда
//		ЭлементыФормы.Товары.ТекущаяСтрока = ТекущаяСтрока;
//	КонецЕсли;
//	
//КонецПроцедуры

//Функция ПолучитьПризнакУстановкиОбора()
//	
//	УстанавливатьОтбор = Ложь;
//	Если ЗначениеЗаполнено(НомерТТН) И ЗначениеЗаполнено(ДатаТТН) Тогда
//		УстанавливатьОтбор = Истина;
//	ИначеЕсли ЗначениеЗаполнено(НомерТТН) Тогда
//		УстанавливатьОтбор = Истина;
//	ИначеЕсли ЗначениеЗаполнено(ДатаТТН) Тогда
//		УстанавливатьОтбор = Истина;
//	КонецЕсли;
//	
//	Возврат УстанавливатьОтбор;
//	
//КонецФункции

//Процедура ОбновитьОтборПоВСД(Знач УстанавливатьОтбор = Неопределено)
//	
//	Если УстанавливатьОтбор = Неопределено Тогда
//		УстанавливатьОтбор = ПолучитьПризнакУстановкиОбора();
//	КонецЕсли;
//	
//	ОтборПоВСД = Новый СписокЗначений;
//	Для Каждого ТекСтрока Из ДоступныеВСД Цикл
//		Если НЕ СкрыватьПодобранные Тогда
//			ОтборПоВСД.Добавить(ТекСтрока.Значение);
//		ИначеЕсли ВыбранныеВСД.НайтиПоЗначению(ТекСтрока.Значение) = Неопределено Тогда
//			ОтборПоВСД.Добавить(ТекСтрока.Значение);
//		КонецЕсли;
//	КонецЦикла;
//	
//	Если ОтборПоВСД.Количество()>0 Тогда
//		СписокВСД.Отбор.Ссылка.Использование = Истина;
//		СписокВСД.Отбор.Ссылка.ВидСравнения  = ВидСравнения.ВСписке;
//		СписокВСД.Отбор.Ссылка.Значение      = ОтборПоВСД;
//	ИначеЕсли СкрыватьПодобранные И ВыбранныеВСД.Количество()>0 Тогда
//		СписокВСД.Отбор.Ссылка.Использование = Истина;
//		СписокВСД.Отбор.Ссылка.ВидСравнения  = ВидСравнения.НеВСписке;
//		СписокВСД.Отбор.Ссылка.Значение      = ВыбранныеВСД;
//	Иначе
//		СписокВСД.Отбор.Ссылка.Использование = УстанавливатьОтбор;
//	КонецЕсли;
//	
//	СписокВСД.Обновить();
//	
//КонецПроцедуры

//Процедура СкрыватьПодобранныеПриИзменении(Элемент)
//	
//	ОбновитьОтборПоВСД();
//	
//КонецПроцедуры

//Процедура СписокВСДНачалоПеретаскивания(Элемент, ПараметрыПеретаскивания, Выполнение)
//	
//	ТекущиеДанные = Элемент.ТекущиеДанные;
//	Структура = Новый Структура;
//	Структура.Вставить("ВСД",           Элемент.ТекущаяСтрока.Ссылка);
//	Структура.Вставить("ТекущаяСтрока", Элемент.ТекущаяСтрока);
//	Структура.Вставить("Товары",        Ложь);
//	
//	ПараметрыПеретаскивания.Значение = Структура;
//	ПараметрыПеретаскивания.ДопустимыеДействия = ДопустимыеДействияПеретаскивания.Копирование;
//	
//	ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Копирование;
//	
//КонецПроцедуры

//Процедура ПодобранныеВСДПроверкаПеретаскивания(Элемент, ПараметрыПеретаскивания, СтандартнаяОбработка, Строка, Колонка)
//	
//	Если ПараметрыПеретаскивания.Значение = Неопределено Тогда
//		ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Отмена;
//	ИначеЕсли ПараметрыПеретаскивания.Значение.Товары = Ложь Тогда
//		ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Копирование;
//	Иначе 
//		ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Отмена;
//	КонецЕсли;
//	
//	СтандартнаяОбработка = Ложь;
//	
//КонецПроцедуры

//Процедура ПодобранныеВСДПеретаскивание(Элемент, ПараметрыПеретаскивания, СтандартнаяОбработка, Строка, Колонка)
//	
//	СтандартнаяОбработка = Ложь;
//	
//	Если ПараметрыПеретаскивания.Значение = Неопределено Тогда
//		Возврат;
//	КонецЕсли;
//	
//	Если Строка = Неопределено Тогда
//		Возврат;
//	КонецЕсли;
//	
//	СтрокаИсточник = ПараметрыПеретаскивания.Значение;
//	
//	ПодобранноеКоличество = 0;
//	НайденныеСтроки = Товары.НайтиСтроки(Новый Структура("ВСД", СтрокаИсточник.ВСД));
//	Для Каждого ТекущаяСтрока Из НайденныеСтроки Цикл
//		ПодобранноеКоличество = ПодобранноеКоличество + ТекущаяСтрока.Вес;
//	КонецЦикла;
//	
//	КоличествоПоВСД      = СтрокаИсточник.ВСД.Количество;
//	ДопустимоеКоличество = КоличествоПоВСД - Мин(КоличествоПоВСД, ПодобранноеКоличество);
//	Если ДопустимоеКоличество = 0 Тогда
//		Возврат;
//	КонецЕсли;
//	
//	ИзмененСоставПодобранныхВСД = Ложь;
//	СтрокаТоваров = Строка;
//	Если ЗначениеЗаполнено(Строка.ВСД) Тогда
//		Если Строка.ВСД.Количество<Строка.Вес Тогда
//			Если Товары.Индекс(Строка) = Товары.Количество()-1 Тогда
//				СтрокаТоваров = Товары.Добавить();
//			Иначе
//				СтрокаТоваров = Товары.Вставить(Товары.Индекс(Строка)+1);
//			КонецЕсли;
//			ЗаполнитьЗначенияСвойств(СтрокаТоваров, Строка);
//			
//			ВесНетто = Строка.Номенклатура.ОсновнаяЕдиницаИзмерения.ВесНетто;
//			Вес      = Строка.Номенклатура.ОсновнаяЕдиницаИзмерения.Вес;
//			Если ЗначениеЗаполнено(ВесНетто) Тогда
//				Строка.Количество = Строка.ВСД.Количество/ВесНетто;
//			ИначеЕсли ЗначениеЗаполнено(Вес) Тогда
//				Строка.Количество = Строка.ВСД.Количество/Вес;
//			Иначе
//				Строка.Количество = Строка.ВСД.Количество;
//			КонецЕсли;
//			Строка.Вес               = Строка.ВСД.Количество;
//			СтрокаТоваров.Количество = СтрокаТоваров.Количество - Строка.Количество;
//			СтрокаТоваров.Вес        = СтрокаТоваров.Вес - Строка.Вес;
//			
//		ИначеЕсли Товары.НайтиСтроки(Новый Структура("ВСД", Строка.ВСД)).Количество() = 1 Тогда
//			
//			НайденноеВСД = ВыбранныеВСД.НайтиПоЗначению(Строка.ВСД);
//			Если НЕ НайденноеВСД = Неопределено Тогда
//				ВыбранныеВСД.Удалить(НайденноеВСД);
//				ИзмененСоставПодобранныхВСД = Истина;
//			КонецЕсли;
//		КонецЕсли;
//	КонецЕсли;
//	
//	СтрокаТоваров.ВСД = СтрокаИсточник.ВСД;
//	
//	Если ДопустимоеКоличество<=СтрокаТоваров.Вес Тогда
//		ВыбранныеВСД.Добавить(СтрокаИсточник.ВСД);
//		ИзмененСоставПодобранныхВСД = Истина;
//	КонецЕсли;
//	
//	Если СкрыватьПодобранные И ИзмененСоставПодобранныхВСД Тогда
//		ОбновитьОтборПоВСД();
//	КонецЕсли;
//	
//КонецПроцедуры

//Процедура ТоварыНачалоПеретаскивания(Элемент, ПараметрыПеретаскивания, Выполнение)
//	
//	ТекущиеДанные = Элемент.ТекущиеДанные;
//	Структура = Новый Структура;
//	Структура.Вставить("ВСД",           Элемент.ТекущаяСтрока.ВСД);
//	Структура.Вставить("ТекущаяСтрока", Элемент.ТекущаяСтрока);
//	Структура.Вставить("Товары",        Истина);
//	
//	ПараметрыПеретаскивания.Значение = Структура;
//	ПараметрыПеретаскивания.ДопустимыеДействия = ДопустимыеДействияПеретаскивания.Копирование;
//	
//	ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Копирование;
//	
//КонецПроцедуры

//Процедура СписокВСДПроверкаПеретаскивания(Элемент, ПараметрыПеретаскивания, СтандартнаяОбработка, Строка, Колонка)
//	
//	Если ПараметрыПеретаскивания.Значение = Неопределено Тогда
//		ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Отмена;
//	ИначеЕсли ПараметрыПеретаскивания.Значение.Товары = Истина Тогда
//		ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Копирование;
//	Иначе
//		ПараметрыПеретаскивания.Действие = ДействиеПеретаскивания.Отмена;
//	КонецЕсли;
//	
//	СтандартнаяОбработка = Ложь;
//	
//КонецПроцедуры

//Процедура СписокВСДПеретаскивание(Элемент, ПараметрыПеретаскивания, СтандартнаяОбработка, Строка, Колонка)
//	
//	СтандартнаяОбработка = Ложь;
//	
//	Если ПараметрыПеретаскивания.Значение = Неопределено Тогда
//		Возврат;
//	КонецЕсли;
//	
//	ИзмененСоставПодобранныхВСД = Ложь;
//	
//	СтрокаИсточник = ПараметрыПеретаскивания.Значение;
//	
//	ТекущаяСтрока = СтрокаИсточник.ТекущаяСтрока;
//	ВСД = СтрокаИсточник.ВСД;
//	ТекущаяСтрока.ВСД = Неопределено;
//	
//	Если Товары.НайтиСтроки(Новый Структура("ВСД", ВСД)).Количество() = 0 Тогда
//		НайденноеВСД = ВыбранныеВСД.НайтиПоЗначению(ВСД);
//		Если НЕ НайденноеВСД = Неопределено Тогда
//			ВыбранныеВСД.Удалить(НайденноеВСД);
//			ИзмененСоставПодобранныхВСД = Истина;
//		КонецЕсли;
//	КонецЕсли;
//	
//	НайденныеСтроки = Товары.НайтиСтроки(Новый Структура("ИдентификаторСтроки", ТекущаяСтрока.ИдентификаторСтроки));
//	ИндСтроки = НайденныеСтроки.Количество()-1;
//	Пока ИндСтроки>=0 Цикл
//		СтрокаПоиска = НайденныеСтроки[ИндСтроки];
//		Если НЕ СтрокаПоиска = ТекущаяСтрока Тогда
//			СтрокаПоиска.Количество = СтрокаПоиска.Количество + ТекущаяСтрока.Количество;
//			СтрокаПоиска.Вес        = СтрокаПоиска.Вес + ТекущаяСтрока.Вес;
//			Товары.Удалить(ТекущаяСтрока);
//			Прервать;
//		КонецЕсли;
//		
//		ИндСтроки = ИндСтроки - 1;
//		
//	КонецЦикла;
//	
//	Если СкрыватьПодобранные И ИзмененСоставПодобранныхВСД Тогда
//		ОбновитьОтборПоВСД();
//	КонецЕсли;
//	
//КонецПроцедуры


//&НаСервере
//Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
//	
//	СкрыватьПодобранные = Истина;
//	
//		ВыполнитьПоиск = Ложь;
//		
//		Если НачальноеЗначениеВыбора.Свойство("Отправитель") И ЗначениеЗаполнено(НачальноеЗначениеВыбора.Отправитель) Тогда
//			ЭлементыФормы.Отправитель.Значение = НачальноеЗначениеВыбора.Отправитель;
//			СписокВСД.Отбор.Отправитель.Использование = Истина;
//		КонецЕсли;
//		
//		Если НачальноеЗначениеВыбора.Свойство("Получатель") И ЗначениеЗаполнено(НачальноеЗначениеВыбора.Получатель) Тогда
//			ЭлементыФормы.Получатель.Значение = НачальноеЗначениеВыбора.Получатель;
//			СписокВСД.Отбор.Получатель.Использование = Истина;
//		КонецЕсли;
//		
//		Если НачальноеЗначениеВыбора.Свойство("ПредприятиеОтправитель") И ЗначениеЗаполнено(НачальноеЗначениеВыбора.ПредприятиеОтправитель) Тогда
//			ЭлементыФормы.ПредприятиеОтправитель.Значение = НачальноеЗначениеВыбора.ПредприятиеОтправитель;
//			СписокВСД.Отбор.ПредприятиеОтправитель.Использование = Истина;
//		КонецЕсли;
//		
//		Если НачальноеЗначениеВыбора.Свойство("ПредприятиеПолучатель") И ЗначениеЗаполнено(НачальноеЗначениеВыбора.ПредприятиеПолучатель) Тогда
//			ЭлементыФормы.ПредприятиеПолучатель.Значение = НачальноеЗначениеВыбора.ПредприятиеПолучатель;
//			СписокВСД.Отбор.ПредприятиеПолучатель.Использование = Истина;
//		КонецЕсли;
//		
//		Если НачальноеЗначениеВыбора.Свойство("НомерТТН") И ЗначениеЗаполнено(НачальноеЗначениеВыбора.НомерТТН) Тогда
//			НомерТТН = НачальноеЗначениеВыбора.НомерТТН;
//			ВыполнитьПоиск = Истина;
//		КонецЕсли;
//		
//		Если НачальноеЗначениеВыбора.Свойство("ДатаТТН") И ЗначениеЗаполнено(НачальноеЗначениеВыбора.ДатаТТН) Тогда
//			ДатаТТН = НачальноеЗначениеВыбора.ДатаТТН;
//			ВыполнитьПоиск = Истина;
//		КонецЕсли;
//		
//		Если НачальноеЗначениеВыбора.Свойство("ТекущийЭлемент") И ЗначениеЗаполнено(НачальноеЗначениеВыбора.ТекущийЭлемент) Тогда
//			ЭлементыФормы.СписокВСД.ТекущаяСтрока = НачальноеЗначениеВыбора.ТекущийЭлемент;
//		КонецЕсли;
//		
//		Если НачальноеЗначениеВыбора.Свойство("ДокументОбъект") Тогда
//			
//			ДокументОбъект = НачальноеЗначениеВыбора.ДокументОбъект;
//			
//			Для Каждого ТекущееВСД Из ДокументОбъект.Товары Цикл
//				НоваяСтрока = Товары.Добавить();
//				ЗаполнитьЗначенияСвойств(НоваяСтрока, ТекущееВСД);
//			КонецЦикла;
//		КонецЕсли;
//		
//		Для Каждого ТекущаяСтрока Из Товары Цикл
//			Если ЗначениеЗаполнено(ТекущаяСтрока.ВСД) И ВыбранныеВСД.НайтиПоЗначению(ТекущаяСтрока.ВСД) = Неопределено Тогда
//				ВыбранныеВСД.Добавить(ТекущаяСтрока.ВСД);
//			КонецЕсли;
//		КонецЦикла;
//		
//		ПоискВСД();
//		
//	ОтборСтатус = СписокВСД.Отбор.Найти("Статус");
//	Если ОтборСтатус = Неопределено Тогда
//		ОтборСтатус = СписокВСД.Отбор.Добавить("Статус");
//	КонецЕсли;
//	
//	ОтборСтатус.Использование = Истина;
//	ОтборСтатус.ВидСравнения  = ВидСравнения.Равно;
//	ОтборСтатус.Значение      = Перечисления.МеркурийСтатусыВСД.Оформлен;
//	
//	ЭлементыФормы.СписокВСД.НастройкаОтбора.Статус.Доступность = Ложь;
//	
//КонецПроцедуры


//&НаКлиенте
//Процедура СписокВСДПередНачаломДобавления1(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
//	
//	Отказ = Истина;
//	
//КонецПроцедуры

