// Модуль менеджера обработки "ПечатьКонвертовНаклеек"

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ ФОРМИРОВАНИЯ ПЕЧАТНЫХ ФОРМ

// Процедура обработки печати.
Процедура Печать(МассивОбъектов, ПараметрыПечати, КоллекцияПечатныхФорм, ОбъектыПечати, ПараметрыВывода) Экспорт
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ПФ_MXL_КарточкаКлиента") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"ПФ_MXL_КарточкаКлиента",
															НСтр("ru = 'Карточка клиента'"),
															СформироватьТабличныйДокумент(МассивОбъектов, ОбъектыПечати, ПараметрыПечати.СтруктураНастроек));
	КонецЕсли;													
КонецПроцедуры

Функция СформироватьТабличныйДокумент(МассивОбъектов, ОбъектыПечати ,СтруктураНастроек = Неопределено)

	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	// Добавим установку параметров печати
	ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ПечатьКарточкаКлиента";
	УправлениеПечатьюПлатформа.УстановитьСтандартныеПараметрыПечати(ИмяПараметровПечати,ТабличныйДокумент);
	ТабличныйДокумент.ИмяПараметровПечати = ИмяПараметровПечати;
	
	ПервыйДокумент = Истина;
	Для Каждого ТекущийОбъект Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		
		// Зададим параметры макета
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
		
		// Получим данные из документа
		Если ЗначениеЗаполнено(СтруктураНастроек) Тогда
			Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьКарточкаКлиента.ПФ_MXL_КарточкаКлиента");
			
			ОбластьОтступа0 = Макет.ПолучитьОбласть("СтрокаОтступа0");
			ОбластьОтступа1 = Макет.ПолучитьОбласть("СтрокаОтступа1");
			ОбластьЗаголовкаРаздела = Макет.ПолучитьОбласть("НазваниеРаздела");
			
			// Построим заголовок
			ОбластьЗаголовка = Макет.ПолучитьОбласть("Заголовок");
			ОбластьЗаголовка.Параметры.Контрагент = ТекущийОбъект.Ссылка;
			
			ТабличныйДокумент.Вывести(ОбластьЗаголовка);
			ТабличныйДокумент.Вывести(ОбластьОтступа0);
			
			// Основные сведения
			СписокОсновныхСведений = Новый СписокЗначений();
			СписокОсновныхСведений.Добавить(ТекущийОбъект.Ссылка.НаименованиеПолное, НСтр("ru = 'Полное наименование'"));
			СписокОсновныхСведений.Добавить(ТекущийОбъект.Ссылка.ВидКонтрагента, НСтр("ru = 'Вид контрагента'"));
			СписокОсновныхСведений.Добавить(ТекущийОбъект.Ссылка.ФормаСобственности, НСтр("ru = 'Форма собственности'"));
			СписокОсновныхСведений.Добавить(ТекущийОбъект.Ссылка.Сотрудник, "Сотрудник");
			СписокОсновныхСведений.Добавить(ТекущийОбъект.Ссылка.Филиал, "Филиал");
			
			ОбластьЗаголовка = Макет.ПолучитьОбласть("СтрокаОсновныеСведения");
			Для Каждого ЭлементСпискаОсновныхСведений Из СписокОсновныхСведений Цикл
				Если ЗначениеЗаполнено(ЭлементСпискаОсновныхСведений.Значение) Тогда
					Если ЭлементСпискаОсновныхСведений.Представление = НСтр("ru = 'Полное наименование'") Тогда
						ОбластьЗаголовка.Параметры.ПараметрРасшифровки = ТекущийОбъект.Ссылка;	
					Иначе
						ОбластьЗаголовка.Параметры.ПараметрРасшифровки = Неопределено;	
					КонецЕсли;	
					ОбластьЗаголовка.Параметры.НазваниеОсновногоПараметра = ЭлементСпискаОсновныхСведений.Представление;
					ОбластьЗаголовка.Параметры.ЗначениеОсновногоПараметра = ЭлементСпискаОсновныхСведений.Значение;
					ТабличныйДокумент.Вывести(ОбластьЗаголовка);
					           
					// Получим таблицу кураторов контрагента
					ВыводитьКонтактнуюИнформациюКонтактныхЛиц = СтруктураНастроек.СписокНастроек.НайтиПоЗначению("КонтактнаяИнформацияКЛ").Пометка;
					ВыводитьПодтверждающиеДокументыКонтактныхЛиц = СтруктураНастроек.СписокНастроек.НайтиПоЗначению("ПодтверждающиеДокументыКЛ").Пометка;
					ВыводитьДополнительнуюИнформациюКонтактныхЛиц = СтруктураНастроек.СписокНастроек.НайтиПоЗначению("ДополнительнаяИнформацияКЛ").Пометка;
				КонецЕсли;
			КонецЦикла;
			ТабличныйДокумент.НачатьАвтогруппировкуСтрок();	
			Для Каждого ЭлементСпискаНастроек Из СтруктураНастроек.СписокНастроек Цикл
				ТекущийРаздел = ЭлементСпискаНастроек.Значение;
				Если (ТекущийРаздел = "КонтактнаяИнформацияКЛ")
				 Или (ТекущийРаздел = "ПодтверждающиеДокументыКЛ")
			 	 Или (ТекущийРаздел = "ДополнительнаяИнформацияКЛ") Тогда		 
					Продолжить;
				КонецЕсли;	
				Если ЭлементСпискаНастроек.Пометка Тогда
					
					Если ((ТекущийРаздел = "РегистрационнаяИнформация") И (ТекущийОбъект.Ссылка.ФормаСобственности = Перечисления.ФормыСобственности.ЧастноеЛицо)) Тогда
						ТекущийРаздел = "РегистрационнаяИнформацияЧастноеЛицо";
					КонецЕсли;	
					ТаблицаДанных = ТаблицаДанныхРаздела(ТекущийРаздел, ТекущийОбъект.Ссылка, СтруктураНастроек.ВыводитьТолькоОсновные);
					Если ТаблицаДанных.Количество() > 0 Тогда
						ТабличныйДокумент.Вывести(ОбластьОтступа0, 0);
						ОбластьЗаголовкаРаздела.Параметры.НаименованиеРаздела = ЭлементСпискаНастроек.Представление;
						ТабличныйДокумент.Вывести(ОбластьЗаголовкаРаздела, 0);
						
						ТабличныйДокумент.Вывести(ОбластьОтступа1, 1);
						Если (СтрНайти(ТекущийРаздел, "РегистрационнаяИнформация") = 1) Или (ТекущийРаздел = "ДополнительнаяИнформация") Тогда
							ВывестиТаблицей(ТаблицаДанных, ТабличныйДокумент, Макет, ТекущийРаздел, 1, Ложь);
						ИначеЕсли ТекущийРаздел = "КонтактнаяИнформация" Тогда
							ВывестиТаблицей(ТаблицаДанных, ТабличныйДокумент, Макет, ТекущийРаздел, 1, Ложь);
						ИначеЕсли ТекущийРаздел = "КонтактныеЛица" Тогда
				    		ОбластьШапкаКонтактногоЛица = Макет.ПолучитьОбласть("ШапкаКонтактныеЛица");
				    		ОбластьОсновныеДанныеКонтактногоЛица = Макет.ПолучитьОбласть("ОсновныеДанныеКонтактныеЛица");
				    		ВывестиОбластьПробела = Ложь;
							Для Каждого КонтактныеЛицаКонтрагента Из ТаблицаДанных Цикл
				    			Если ВывестиОбластьПробела Тогда
				    				ТабличныйДокумент.Вывести(ОбластьОтступа1, 2);	
				    			Иначе
				    				ВывестиОбластьПробела = Истина;
				    			КонецЕсли;
				    			ОбластьШапкаКонтактногоЛица.Параметры.Ссылка = КонтактныеЛицаКонтрагента.Ссылка;
				    			ТабличныйДокумент.Вывести(ОбластьШапкаКонтактногоЛица, 1);	
				    			
				    			ОбластьОсновныеДанныеКонтактногоЛица.Параметры.Заполнить(КонтактныеЛицаКонтрагента);
				    			ТабличныйДокумент.Вывести(ОбластьОсновныеДанныеКонтактногоЛица, 2);
								
								Если ВыводитьКонтактнуюИнформациюКонтактныхЛиц Тогда
									ТаблицаДанныхКонтактногоЛица = ТаблицаДанныхРаздела("КонтактнаяИнформация", ТекущийОбъект.Ссылка,
																								СтруктураНастроек.ВыводитьТолькоОсновные, КонтактныеЛицаКонтрагента.Ссылка);
									Если ТаблицаДанныхКонтактногоЛица.Количество() > 0 Тогда 
										ТабличныйДокумент.Вывести(ОбластьОтступа1, 2);
										ВывестиТаблицей(ТаблицаДанныхКонтактногоЛица, ТабличныйДокумент, Макет, "КонтактнаяИнформацияКЛ", 2);
									КонецЕсли;
									БылаВыведенаКонтактнаяИнформация = (ТаблицаДанныхКонтактногоЛица.Количество() > 0);
				    			КонецЕсли;
				    			Если ВыводитьПодтверждающиеДокументыКонтактныхЛиц Тогда
									ТаблицаДанныхКонтактногоЛица = ТаблицаДанныхРаздела("ПодтверждающиеДокументы", ТекущийОбъект.Ссылка, 
																								СтруктураНастроек.ВыводитьТолькоОсновные, КонтактныеЛицаКонтрагента.Ссылка);
									Если ТаблицаДанныхКонтактногоЛица.Количество() > 0 Тогда 
										Если (Не ВыводитьКонтактнуюИнформациюКонтактныхЛиц) Или (БылаВыведенаКонтактнаяИнформация) Тогда
											ТабличныйДокумент.Вывести(ОбластьОтступа1, 2);						
										КонецЕсли;
										ВывестиТаблицей(ТаблицаДанныхКонтактногоЛица, ТабличныйДокумент, Макет, "ПодтверждающиеДокументыКЛ", 2);
									КонецЕсли;	
				    			КонецЕсли;
				    			Если ВыводитьДополнительнуюИнформациюКонтактныхЛиц Тогда
				    				ТаблицаДанныхКонтактногоЛица = ТаблицаДанныхРаздела("ДополнительнаяИнформация", ТекущийОбъект.Ссылка, 
																								СтруктураНастроек.ВыводитьТолькоОсновные ,КонтактныеЛицаКонтрагента.Ссылка);
									Если ТаблицаДанныхКонтактногоЛица.Количество() > 0 Тогда 
										ТабличныйДокумент.Вывести(ОбластьОтступа1, 2);
										ВывестиТаблицей(ТаблицаДанныхКонтактногоЛица, ТабличныйДокумент, Макет, "ДополнительнаяИнформацияКЛ", 2);
									КонецЕсли;	
				    			КонецЕсли;	
				    		КонецЦикла;
						ИначеЕсли ТекущийРаздел = "ПодтверждающиеДокументы"	Тогда
							ВывестиТаблицей(ТаблицаДанных, ТабличныйДокумент, Макет, ТекущийРаздел, 1, Ложь);
						Иначе
							ВывестиТаблицей(ТаблицаДанных, ТабличныйДокумент, Макет, ТекущийРаздел, 1);
						КонецЕсли;
			 		КонецЕсли;
			    КонецЕсли;	
			КонецЦикла;
			ТабличныйДокумент.ОтображатьСетку = Ложь;
			ТабличныйДокумент.ПолеСверху = 10;
			ТабличныйДокумент.ПолеСлева = 20;
			ТабличныйДокумент.ПолеСнизу = 10;
			ТабличныйДокумент.ПолеСправа = 10;
			ТабличныйДокумент.ОтображатьЗаголовки = Ложь;
			ТабличныйДокумент.ЗакончитьАвтогруппировкуСтрок();
		КонецЕсли;
		// Отметим конец области документа.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, ТекущийОбъект);
	КонецЦикла;	
	
	Возврат ТабличныйДокумент;	
КонецФункции // СформироватьТабличныйДокумент()

// Формирует на основе текста запроса таблицу и выводит ее в табличный документ.
// Параметры:
//	ТаблицаДанных - ТаблицаЗначений - таблица значений, на основании которой формируется табличный документ;
//	ТабличныйДокумент - табличный документ, в который выводится таблица;
//	Макет - макет, с помощью которого формируются строки таблицы;
//	Раздел - Строка - название раздела информации о контрагенте, в который выводится таблица;
//	Уровень - Число - уровень группировки таблицы в документе;
//	ЕстьШапка - Булево - указывает, есть ли в таблице шапка;
//  РаскраситьТаблицу - Число - указывает, какие части таблицы раскрасить. Имеет значения:
//                    "0" - не раскрашивать ни шапку, ни строки таблицы;
//                    "1" - раскрасить шапку таблицы;
//                    "2" - раскрасить строки таблицы;
//                    "3" - раскрасить и шапку, и строки таблицы.
Процедура ВывестиТаблицей(ТаблицаДанных, ТабличныйДокумент, Макет, Раздел, Уровень, ЕстьШапка = Истина)
	Если ЕстьШапка Тогда
		ОбластьШапка = Макет.ПолучитьОбласть("Шапка"+Раздел);
		ТабличныйДокумент.Вывести(ОбластьШапка, Уровень);
	КонецЕсли;
	
	Для Каждого СтрокаТаблицыДанных Из ТаблицаДанных Цикл
		Попытка
			ПоУмолчанию = СтрокаТаблицыДанных.ЗначениеПоУмолчанию;
		Исключение
			ПоУмолчанию = Ложь;
		КонецПопытки;

		ОбластьСтроки = Макет.ПолучитьОбласть("Строка" + Раздел + ?(ПоУмолчанию, "ПоУмолчанию", ""));
		ОбластьСтроки.Параметры.Заполнить(СтрокаТаблицыДанных);
		
		ТабличныйДокумент.Вывести(ОбластьСтроки, Уровень);
	КонецЦикла;	
КонецПроцедуры// ВывестиТаблицей()	

// Выбирает данные по разделу информации.
// Параметры:
//  Раздел - Строка - НазваниеРаздела
//  Контрагент - СправочникСсылка.Контрагент - контрагент, по которому собирается информация;
//  КонтактноеЛицо - СправочникСсылка.КонтактноеЛицо - контактное лицо, по которому собирается информация;
// Возвращаемое значение
//  ТаблицаЗначений - таблица, в которую выгружены результаты запроса.
Функция ТаблицаДанныхРаздела(Раздел, Контрагент, ВыводитьТолькоОсновные, КонтактноеЛицо = Неопределено)
	ВозвращаемоеЗначение = Новый ТаблицаЗначений();
	Если Раздел = "РегистрационнаяИнформация" Тогда
		НетСтрок = Истина;
		ВозвращаемоеЗначение.Колонки.Добавить("ИННКПП");
		ВозвращаемоеЗначение.Колонки.Добавить("КодИМНС");
		ВозвращаемоеЗначение.Колонки.Добавить("КодПоОКПО");
		СтрокаВозвращаемоеЗначение = ВозвращаемоеЗначение.Добавить();
		Если ЗначениеЗаполнено(Контрагент.ИНН) Тогда
			ИННКПП = СокрЛП(Контрагент.ИНН);
			Если ЗначениеЗаполнено(Контрагент.КПП) Тогда
				ИННКПП = ИННКПП + " / "+ СокрЛП(Контрагент.КПП);
			КонецЕсли;
			НетСтрок = Ложь;
		КонецЕсли;
		Если ЗначениеЗаполнено(Контрагент.КодИМНС) Тогда
			НетСтрок = Ложь;
		КонецЕсли;
		Если ЗначениеЗаполнено(Контрагент.КодПоОКПО) Тогда
			НетСтрок = Ложь;
		КонецЕсли;
		Если НетСтрок Тогда
			ВозвращаемоеЗначение.Очистить();
		Иначе
			СтрокаВозвращаемоеЗначение.ИННКПП = ИННКПП;
			СтрокаВозвращаемоеЗначение.КодИМНС = Контрагент.КодИМНС;
			СтрокаВозвращаемоеЗначение.КодПоОКПО = Контрагент.КодПоОКПО;
		КонецЕсли;
	ИначеЕсли Раздел = "РегистрационнаяИнформацияЧастноеЛицо" Тогда
		НетСтрок = Истина;
		ВозвращаемоеЗначение.Колонки.Добавить("ДатаРождения");
		Если ЗначениеЗаполнено(Контрагент.ДатаРождения) Тогда
			СтрокаВозвращаемоеЗначение = ВозвращаемоеЗначение.Добавить();
			СтрокаВозвращаемоеЗначение.ДатаРождения = Формат(Контрагент.ДатаРождения, "ДЛФ = Д");
		КонецЕсли;
	ИначеЕсли Раздел = "ДополнительнаяИнформация" Тогда
		ВозвращаемоеЗначение.Колонки.Добавить("Комментарий");
		Если КонтактноеЛицо = Неопределено Тогда
			Комментарий = Контрагент.Комментарий;
		Иначе
			Комментарий = КонтактноеЛицо.Комментарий;
		КонецЕсли;
		Если ЗначениеЗаполнено(Комментарий) Тогда
			СтрокаВозвращаемоеЗначение = ВозвращаемоеЗначение.Добавить();
			СтрокаВозвращаемоеЗначение.Комментарий = Комментарий;
		КонецЕсли;
	Иначе	
		Запрос = Новый Запрос;
		// Создадим текст запроса
		Если Раздел = "КонтактнаяИнформация" Тогда
			ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	КонтрагентыКонтактнаяИнформация.Ссылка,
			|	КонтрагентыКонтактнаяИнформация.Тип,
			|	КонтрагентыКонтактнаяИнформация.Вид,
			|	КонтрагентыКонтактнаяИнформация.Представление
			|ИЗ
			|	Справочник.Контрагенты.КонтактнаяИнформация КАК КонтрагентыКонтактнаяИнформация
			|ГДЕ
			|	КонтрагентыКонтактнаяИнформация.Ссылка = &Объект";
		ИначеЕсли Раздел = "КонтактныеЛица" Тогда
			ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	ВЫБОР
			|		КОГДА Контрагенты.ВидКонтрагента = ЗНАЧЕНИЕ(Перечисление.ВидыКонтрагентов.КонтактноеЛицо)
			|				И Контрагенты.ФормаСобственности = ЗНАЧЕНИЕ(Перечисление.ФормыСобственности.ЧастноеЛицо)
			|			ТОГДА Контрагенты.Должность
			|		ИНАЧЕ ЗНАЧЕНИЕ(Справочник.Должности.Неопределена)
			|	КОНЕЦ КАК Должность,
			|	Контрагенты.ДатаРождения КАК ДатаРождения,
			|	КонтактныеЛица.КонтактноеЛицо КАК Ссылка
			|ИЗ
			|	РегистрСведений.КонтактныеЛица КАК КонтактныеЛица
			|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК Контрагенты
			|		ПО КонтактныеЛица.КонтактноеЛицо = Контрагенты.Ссылка
			|ГДЕ
			|	КонтактныеЛица.Владелец = &Объект";
			Запрос.УстановитьПараметр("Объект", Контрагент);
		ИначеЕсли Раздел = "Кураторы" Тогда
			ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	Кураторы.Куратор,
			|	Кураторы.Договор
			|ИЗ
			|	РегистрСведений.Кураторы КАК Кураторы
			|ГДЕ
			|	Кураторы.Контрагент = &Контрагент";
			Запрос.УстановитьПараметр("Контрагент", Контрагент);
		ИначеЕсли Раздел = "БанковскиеСчета" Тогда
			ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	БанковскиеСчета.Ссылка,
			|	БанковскиеСчета.ВалютаДенежныхСредств,
			|	БанковскиеСчета.НомерСчета,
			|	БанковскиеСчета.Банк.Ссылка КАК БанкСсылка,
			|	БанковскиеСчета.Банк.КоррСчет КАК КоррСчет,
			|	БанковскиеСчета.Банк.Код КАК БИК,
			|	ВЫБОР
			|		КОГДА БанковскиеСчета.Ссылка = &ОсновнойРеквизит
			|			ТОГДА ИСТИНА
			|		ИНАЧЕ ЛОЖЬ
			|	КОНЕЦ КАК ЗначениеПоУмолчанию
			|ИЗ
			|	Справочник.БанковскиеСчета КАК БанковскиеСчета
			|ГДЕ
			|	БанковскиеСчета.Владелец = &Контрагент
			|	ОтборПоОсновномуРеквизиту
			|УПОРЯДОЧИТЬ ПО
			|	ЗначениеПоУмолчанию УБЫВ";
			Запрос.УстановитьПараметр("ОсновнойРеквизит", Контрагент.ОсновнойБанковскийСчет);
		ИначеЕсли Раздел = "ДоговорыКонтрагента" Тогда
			ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	ДоговорыВзаиморасчетов.Ссылка,
			|	ДоговорыВзаиморасчетов.Наименование,
			|	ДоговорыВзаиморасчетов.ВалютаВзаиморасчетов,
			|	ДоговорыВзаиморасчетов.ПроцентПредоплаты,
			|	ДоговорыВзаиморасчетов.Основной КАК ЗначениеПоУмолчанию
			|ИЗ
			|	Справочник.ДоговорыВзаиморасчетов КАК ДоговорыВзаиморасчетов
			|ГДЕ
			|	ДоговорыВзаиморасчетов.Владелец = &Контрагент
			|	ОтборПоОсновномуРеквизиту
			|
			|УПОРЯДОЧИТЬ ПО
			|	ЗначениеПоУмолчанию УБЫВ";
		ИначеЕсли Раздел = "ПодтверждающиеДокументы" Тогда
			ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	ПодтверждающиеДокументы.Ссылка,
			|	ПодтверждающиеДокументы.Наименование,
			|	ПодтверждающиеДокументы.Серия,
			|	ПодтверждающиеДокументы.Номер,
			|	ПодтверждающиеДокументы.КемВыдан,
			|	ПодтверждающиеДокументы.ДатаВыдачи
			|ИЗ
			|	Справочник.ПодтверждающиеДокументы КАК ПодтверждающиеДокументы
			|ГДЕ
			|	ПодтверждающиеДокументы.Владелец = &Объект";
		ИначеЕсли Раздел = "ДисконтныеКарты" Тогда
			ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	Карточки.Ссылка КАК Карточка,
			|	СУММА(ЕСТЬNULL(НакопленияПоКарточкамКонтрагентовОбороты.СуммаОборот, 0)) КАК Сумма
			|ИЗ
			|	Справочник.Карточки КАК Карточки
			|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрНакопления.НакопленияПоКарточкамКонтрагентов.Обороты КАК НакопленияПоКарточкамКонтрагентовОбороты
			|		ПО (НакопленияПоКарточкамКонтрагентовОбороты.Карточка = Карточки.Ссылка)
			|ГДЕ
			|	Карточки.Объект = &Контрагент
			|
			|СГРУППИРОВАТЬ ПО
			|	Карточки.Ссылка";
		КонецЕсли;
		
		// Установим параметры запроса и при необходимости корректируем его текст.
		
		// Если есть отбор по основным значениям раздела.
		Если ВыводитьТолькоОсновные Тогда
			Если СтрНайти(ТекстЗапроса, "ОтборПоОсновномуРеквизиту")>0 Тогда
				Если Раздел = "БанковскиеСчета" Тогда
					ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ОтборПоОсновномуРеквизиту", "И БанковскиеСчета.Ссылка = &ОсновнойРеквизит");
				ИначеЕсли Раздел = "ДоговорыКонтрагента" Тогда
					ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ОтборПоОсновномуРеквизиту", "И ДоговорыВзаиморасчетов.Ссылка = &ОсновнойРеквизит");
				КонецЕсли;
			КонецЕсли;
		Иначе
			ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ОтборПоОсновномуРеквизиту", "");
		КонецЕсли;
		
		// У контактной информации и подтверждающих документов
		// может быть отбор или по контрагенту, или по контактной информации.
		Если (Раздел = "КонтактнаяИнформация") ИЛИ (Раздел = "ПодтверждающиеДокументы") Тогда
			Если КонтактноеЛицо = Неопределено Тогда
				Запрос.УстановитьПараметр("Объект", Контрагент);
			Иначе
				Запрос.УстановитьПараметр("Объект", КонтактноеЛицо);
			КонецЕсли;	
		Иначе
			Запрос.УстановитьПараметр("Контрагент", Контрагент);					   
		КонецЕсли;
		
		Запрос.Текст = ТекстЗапроса;
		
		ВозвращаемоеЗначение = Запрос.Выполнить().Выгрузить();
		
		// Дополнительные преобразования 
		Если ВозвращаемоеЗначение.Количество() > 0 Тогда
			// Для подтверждающих документов сформируем дополнительную колонку "Строка", куда будет добавлено представление.
			Если Раздел = "ПодтверждающиеДокументы" Тогда
				ВозвращаемоеЗначение.Колонки.Добавить("Строка");
				Для Каждого СтрокаТаблицы Из ВозвращаемоеЗначение Цикл
					СтрокаТаблицы.Строка = " " + (ВозвращаемоеЗначение.Индекс(СтрокаТаблицы)+1) + ". " + СокрЛП(СтрокаТаблицы.Наименование)
															  + ?(НЕ ЗначениеЗаполнено(СтрокаТаблицы.Серия),""," " + "серия" + " " +СокрЛП(СтрокаТаблицы.Серия))
															  + ?(НЕ ЗначениеЗаполнено(СтрокаТаблицы.Номер),""," " + "№"+СокрЛП(СтрокаТаблицы.Номер))
															  + ?(НЕ ЗначениеЗаполнено(СтрокаТаблицы.КемВыдан),""," " + "выдан" + " " +СокрЛП(СтрокаТаблицы.КемВыдан))
															  + ?(НЕ ЗначениеЗаполнено(СтрокаТаблицы.ДатаВыдачи),"", " "+ НСтр("ru = 'дата выдачи'") + " " 
															  													+Формат(СтрокаТаблицы.ДатаВыдачи,"ДЛФ = Д"));
				КонецЦикла;
			// Для контактных лиц соберем кураторов.
			ИначеЕсли Раздел = "КонтактныеЛица" Тогда
				ВозвращаемоеЗначение.Колонки.Добавить("СтрокаКураторы");
				Для Каждого СтрокаТаблицы Из ВозвращаемоеЗначение Цикл
					ТаблицаКураторов = ТаблицаДанныхРаздела("Кураторы", Контрагент, ВыводитьТолькоОсновные, СтрокаТаблицы.Ссылка);
	    			СтрокаКураторов = "";
	    			Для Каждого СтрокаТаблицыКураторов Из ТаблицаКураторов Цикл
    					Если Не СтрокаКураторов = "" Тогда
    						СтрокаКураторов = СтрокаКураторов + ", ";
    					КонецЕсли;
    					СтрокаКураторов = СтрокаКураторов + СокрЛП(СтрокаТаблицыКураторов.Куратор.Наименование);
					КонецЦикла;
					СтрокаТаблицы.СтрокаКураторы = СтрокаКураторов;
				КонецЦикла;										  
			КонецЕсли;		
		КонецЕсли;
	КонецЕсли;
	Возврат ВозвращаемоеЗначение;
КонецФункции// ТаблицаДанныхРаздела()	

#КонецЕсли