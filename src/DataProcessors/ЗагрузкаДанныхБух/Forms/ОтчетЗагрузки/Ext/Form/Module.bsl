&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Обработки.ЗагрузкаДанныхБух.СформироватьОтчет(ТабличныйДокумент, Параметры);
	ВремяЗавершения = Параметры.ОкончаниеРаботы;
	ИмяФайла        = Параметры.ИмяФайла;
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьКакExcel(Команда)
	
	ФайлЗагрузкиИсходный = Новый Файл(ИмяФайла);
	
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Сохранение);
	Диалог.Заголовок		= НСтр("ru = 'Сохранение табличного документа'");
	Диалог.Фильтр			= "Табличный документ (*.mxl)|*.mxl";
	Диалог.Расширение		= "mxl";
	Диалог.ПолноеИмяФайла	= ФайлЗагрузкиИсходный.ИмяБезРасширения + " "+СтрЗаменить(Формат(ВремяЗавершения,"ДЛФ=ДВ"),":", "");
	Если Диалог.Выбрать() Тогда
		Если НЕ ПустаяСтрока(Диалог.ПолноеИмяФайла) Тогда
			ТабличныйДокумент.Записать(Диалог.ПолноеИмяФайла, ТипФайлаТабличногоДокумента.MXL);
		КонецЕсли;
	КонецЕсли;	

КонецПроцедуры

&НаКлиенте
Процедура СохранитьКакTXT(Команда)
	
	ФайлЗагрузкиИсходный = Новый Файл(ИмяФайла);
	
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Сохранение);
	Диалог.Заголовок		= НСтр("ru = 'Сохранение табличного документа'");
	Диалог.Фильтр			= "Табличный документ (*.txt)|*.txt";
	Диалог.Расширение		= "txt";
	Диалог.ПолноеИмяФайла	= ФайлЗагрузкиИсходный.ИмяБезРасширения + " "+СтрЗаменить(Формат(ВремяЗавершения,"ДЛФ=ДВ"),":", "");
	Если Диалог.Выбрать() Тогда
		Если НЕ ПустаяСтрока(Диалог.ПолноеИмяФайла) Тогда
			ТабличныйДокумент.Записать(Диалог.ПолноеИмяФайла, ТипФайлаТабличногоДокумента.TXT);
		КонецЕсли;
	КонецЕсли;	
КонецПроцедуры
