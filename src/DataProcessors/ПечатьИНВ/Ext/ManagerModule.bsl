#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

// Процедура обработки печати
Процедура Печать(МассивОбъектов, ПараметрыПечати, КоллекцияПечатныхФорм, ОбъектыПечати, ПараметрыВывода) Экспорт
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ПФ_MXL_ИНВ3") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"ПФ_MXL_ИНВ3",
															НСтр("ru = 'ИНВ3 (Инвентаризационная опись)'"),
															ПечатьИНВ3Общая(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ПФ_MXL_ИНВ19") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"ПФ_MXL_ИНВ19",
															НСтр("ru = 'ИНВ19 (Сличительная ведомость)'"),
															ПечатьИНВ19Общая(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;		
														
	//Мальцев А.Ю. +++
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "итПФ_MXL_ИНВ3") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"итПФ_MXL_ИНВ3",
															НСтр("ru = 'ИНВ3 (Инвентаризационная опись)'"),
															итПечатьИНВ3Общая(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;	
	
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "итПФ_MXL_ИНВ19") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"итПФ_MXL_ИНВ19",
															НСтр("ru = 'ИНВ19 (Сличительная ведомость)'"),
															итПечатьИНВ19Общая(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;	
	
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "итОбъяснительнаяПоНедостаче") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"итОбъяснительнаяПоНедостаче",
															НСтр("ru = 'Объяснительная по недостаче'"),
															итПечатьОбъяснительнаяПоНедостаче(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;		
	
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "итПриказОбУдержании") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"итПриказОбУдержании",
															НСтр("ru = 'Приказ об удержании'"),
															итПечатьПриказОбУдержании(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;	
														
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "итЗаявлениеНаУдержание") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"итЗаявлениеНаУдержание",
															НСтр("ru = 'Заявление на удержание'"),
															итПечатьЗаявлениеНаУдержание(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;	
	
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "итПриказОбИнвентаризации") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
															"итПриказОбИнвентаризации",
															НСтр("ru = 'Приказ об инвентаризации'"),
															итПечатьПриказОбИнвентаризации(МассивОбъектов, ОбъектыПечати));
	КонецЕсли;	
	//Мальцев А.Ю. ---
	
КонецПроцедуры

Функция ПечатьИНВ3Общая(МассивОбъектов, ОбъектыПечати)
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	// Добавим установку параметров печати
	ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ИНВ3Общая";
	УправлениеПечатьюПлатформа.УстановитьСтандартныеПараметрыПечати(ИмяПараметровПечати,ТабличныйДокумент);
	ТабличныйДокумент.ИмяПараметровПечати = ИмяПараметровПечати;

	ПервыйДокумент = Истина;
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
		ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
		
		// Зададим параметры макета.
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		
		// Получим данные из документа.
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).ПолучитьДанныеДляПечатиИНВ3(Документ);
		
		Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.ПФ_MXL_ИНВ3");
		
		ОбластьЗаголовок        = Макет.ПолучитьОбласть("Шапка");
		ОбластьЗаголовокТаблицы	= Макет.ПолучитьОбласть("ЗаголовокТаблицы");
		ОбластьСтрока			= Макет.ПолучитьОбласть("Строка");
		ОбластьПодвалСтраницы	= Макет.ПолучитьОбласть("ПодвалСтраницы");
		ОбластьПодвалОписи      = Макет.ПолучитьОбласть("ПодвалОписи");
		
		ОбластьЗаголовок.Параметры.Заполнить(ДанныеДокумента);
		
		СтруктураПредставления = Новый Структура;
		СтруктураПредставления.Вставить("Наименование",     "");
		СтруктураПредставления.Вставить("ИНН",              "ИНН ");
		СтруктураПредставления.Вставить("АдресЮридический", "");
		СтруктураПредставления.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПредставления.Вставить("БанковскийСчет",   "р/с ");
		СтруктураПредставления.Вставить("Банк",             НСтр("ru = 'в банке'") + " ");
		СтруктураПредставления.Вставить("БИК",              "БИК ");
		СтруктураПредставления.Вставить("КоррСчет",         "к/с ");
		
		// Дата документа
		ДатаДокумента = Документ.Дата;
		ДополнительныеПараметры = Новый Структура("ПодразделениеКомпании", Документ.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПредставление = 
		УправлениеПечатьюПлатформа.ПолучитьПредставлениеСправочника(ОбластьЗаголовок.Параметры.Организация, 
																		СтруктураПредставления,,,ДатаДокумента,ДополнительныеПараметры);
		
		ОбластьЗаголовок.Параметры.ПодразделениеКомпании = 
		УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПоОКПО			= ОбластьЗаголовок.Параметры.Организация.КодПоОКПО;
		ОбластьЗаголовок.Параметры.ВидДеятельностиПоОКДП		= ОбластьЗаголовок.Параметры.Организация.КодПоОКДП;
		ОбластьЗаголовок.Параметры.МОЛДолжность					= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		ОбластьЗаголовок.Параметры.МОЛПредставление				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
		
		ОбластьЗаголовок.Параметры.Номер = УправлениеПечатьюПлатформа.ПолучитьНомерДляПечати(Документ);
		
		Если ЕстьРеквизит(Документ, "ДатаНачалаИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаНачалаИнвентаризации = Документ.ДатаНачалаИнвентаризации;
		КонецЕсли;
		Если ЕстьРеквизит(Документ, "ДатаОкончанияИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаОкончанияИнвентаризации = Документ.ДатаОкончанияИнвентаризации;
		КонецЕсли;
		
		ТабличныйДокумент.Вывести(ОбластьЗаголовок);
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();

		СтрокНаСтранице	= 24;
		СтрокШапки		= 5;
		СтрокПодвала	= 5;
		НомерСтраницы	= 2;
		Ном				= 1;
		
		ИтогКоличество		= 0;
		ИтогКоличествоУчет	= 0;
		ИтогСумма			= 0;
		ИтогСуммаУчет		= 0;

		
		КолвоСтрокПоСтранице	= 0;
		КолвоПоСтранице			= 0;
		СуммаЛиста				= 0;
		
		// Выводим заголовок таблицы.
		ОбластьЗаголовокТаблицы.Параметры.Заполнить(ДанныеДокумента);
		ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница" + " " + НомерСтраницы; 
		ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);

		ВыборкаСтрокТовары = ДанныеДокумента.ВыборкаТабличнойЧасти;
		КоличествоСтрок = ВыборкаСтрокТовары.Количество();

		Если КоличествоСтрок = 1 Тогда
			ПереноситьПоследнююСтроку = 0;
		Иначе
			ЦелыхСтраницСПодвалом     = Цел((СтрокШапки + КоличествоСтрок + СтрокПодвала) / СтрокНаСтранице);
			ЦелыхСтраницБезПодвала    = Цел((СтрокШапки + КоличествоСтрок - 1) / СтрокНаСтранице);
			ПереноситьПоследнююСтроку = ЦелыхСтраницСПодвалом - ЦелыхСтраницБезПодвала;
		КонецЕсли;

		// Определим, что показывать в колонке кода - код или артикул.
		ИмяКолонкиКода = УправлениеПечатьюПлатформа.ПолучитьПараметрыРежимаВыводаКодаВДокументах().Имя;
		
		РозничныеЦены = ПолучитьЗначениеПараметраСтруктуры(ДанныеДокумента, "РозничныеЦены", Ложь);
		
		// Выводим многострочную часть документа.
		Для Каждого СтрокаТоваров Из ВыборкаСтрокТовары Цикл
		
			
			// Начинаем новую страницу, если предыдущая строка была последней на странице
			// или пора переносить последнюю строку на последнюю страницу с подвалом.
			ЦелаяСтраница	= (СтрокШапки + Ном - 1) / СтрокНаСтранице;

			Если (ЦелаяСтраница = Цел(ЦелаяСтраница))
			 ИЛИ ((ПереноситьПоследнююСтроку = 1) И (Ном = КоличествоСтрок)) Тогда

				ОбластьПодвалСтраницы.Параметры.ИтогоКоличество     = Формат(ИтогКоличество,ФорматВыводаКоличества);
				ОбластьПодвалСтраницы.Параметры.ИтогоКоличествоУчет = Формат(ИтогКоличествоУчет,ФорматВыводаКоличества);
				ОбластьПодвалСтраницы.Параметры.ИтогоСумма          = Формат(ИтогСумма,ФорматВыводаСуммы);
				ОбластьПодвалСтраницы.Параметры.ИтогоСуммаУчет      = Формат(ИтогСуммаУчет,ФорматВыводаСуммы);

				ОбластьПодвалСтраницы.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью     = ЧислоПрописью(КолвоСтрокПоСтранице, ,",,,,,,,,0");
				ОбластьПодвалСтраницы.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ЧислоПрописью(КолвоПоСтранице);
				ОбластьПодвалСтраницы.Параметры.СуммаФактическиНаСтраницеПрописью              	  = ЧислоПрописью(ИтогСуммаУчет,ДанныеДокумента.Валюта);
				ТабличныйДокумент.Вывести(ОбластьПодвалСтраницы);
				
				НомерСтраницы = НомерСтраницы + 1;
				ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();

				ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница" + " " + НомерСтраницы;
				ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);
				Ном =1;

				ИтогКоличество 		= 0;
				ИтогКоличествоУчет  = 0;
				ИтогСумма     		= 0;
				ИтогСуммаУчет       = 0;

				КолвоСтрокПоСтранице = 0;
				КолвоПоСтранице      = 0;
				СуммаЛиста           = 0;

			КонецЕсли;
			
			ОбластьСтрока.Параметры.Заполнить(СтрокаТоваров);
			ОбластьСтрока.Параметры.КоличествоУчет					= Формат(СтрокаТоваров.КоличествоФакт,ФорматВыводаКоличества);
			ОбластьСтрока.Параметры.Количество						= Формат(СтрокаТоваров.КоличествоКнижн,ФорматВыводаКоличества);
			Если ЕстьРеквизит(ВыборкаСтрокТовары, "ХарактеристикаНоменклатуры") Тогда
				Характеристика										= Строка(СтрокаТоваров.ХарактеристикаНоменклатуры);
			Иначе
				Характеристика										= "";
			КонецЕсли;
			ОбластьСтрока.Параметры.ТоварНаименование				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(СтрокаТоваров.Номенклатура) + ?(Характеристика = "", "",", " + Характеристика);
			Если ЕстьРеквизит(ВыборкаСтрокТовары, "ТоварКод") Тогда
				ОбластьСтрока.Параметры.ТоварКод					= СтрокаТоваров.ТоварКод;
			Иначе
				ОбластьСтрока.Параметры.ТоварКод					= УправлениеПечатьюПлатформа.ПолучитьЗначениеКолонкиКода(СтрокаТоваров.Номенклатура);
			КонецЕсли;
			Если ЕстьРеквизит(СтрокаТоваров.Номенклатура, "БазоваяЕдиницаИзмерения") Тогда
				ОбластьСтрока.Параметры.ЕдиницаИзмеренияНаименование= СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения;
				ОбластьСтрока.Параметры.ЕдиницаИзмеренияКодПоОКЕИ 	= СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения.Код;
			КонецЕсли;
			ОбластьСтрока.Параметры.Номер							= СтрокаТоваров.НомерСтроки;
			Если РозничныеЦены Тогда
				ОбластьСтрока.Параметры.Цена							= ?(СтрокаТоваров.ЦенаРозничная <> 0,Формат(СтрокаТоваров.ЦенаРозничная,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.СуммаУчет	    				= ?(СтрокаТоваров.СуммаРозничнаяФакт <> 0,Формат(СтрокаТоваров.СуммаРозничнаяФакт,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.Сумма       					= ?(СтрокаТоваров.СуммаРозничнаяКнижн <> 0,Формат(СтрокаТоваров.СуммаРозничнаяКнижн,ФорматВыводаСуммы),"-");
			Иначе
				ОбластьСтрока.Параметры.Цена							= ?(СтрокаТоваров.Цена <> 0,Формат(СтрокаТоваров.Цена,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.СуммаУчет	    				= ?(СтрокаТоваров.СуммаФакт <> 0,Формат(СтрокаТоваров.СуммаФакт,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.Сумма       					= ?(СтрокаТоваров.СуммаКнижн <> 0,Формат(СтрокаТоваров.СуммаКнижн,ФорматВыводаСуммы),"-");
			КонецЕсли;

			ТабличныйДокумент.Вывести(ОбластьСтрока);
			Ном = Ном + 1;

			ИтогКоличество     = ИтогКоличество     + СтрокаТоваров.КоличествоКнижн;
			ИтогКоличествоУчет = ИтогКоличествоУчет + СтрокаТоваров.КоличествоФакт;
			Если РозничныеЦены Тогда
				ИтогСумма          = ИтогСумма          + СтрокаТоваров.СуммаРозничнаяКнижн;
				ИтогСуммаУчет      = ИтогСуммаУчет      + СтрокаТоваров.СуммаРозничнаяФакт;
			Иначе
				ИтогСумма          = ИтогСумма          + СтрокаТоваров.СуммаКнижн;
				ИтогСуммаУчет      = ИтогСуммаУчет      + СтрокаТоваров.СуммаФакт;
			КонецЕсли;


			КолвоСтрокПоСтранице = КолвоСтрокПоСтранице + 1;
			КолвоПоСтранице      = КолвоПоСтранице      + СтрокаТоваров.КоличествоФакт;
			СуммаЛиста           = СуммаЛиста           + СтрокаТоваров.СуммаФакт;

		КонецЦикла;

		// Выводим итоги по последней странице.
		ОбластьПодвалСтраницы.Параметры.ИтогоКоличество     = Формат(ИтогКоличество,ФорматВыводаКоличества);
		ОбластьПодвалСтраницы.Параметры.ИтогоКоличествоУчет = Формат(ИтогКоличествоУчет,ФорматВыводаКоличества);
		ОбластьПодвалСтраницы.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью     = ЧислоПрописью(КолвоСтрокПоСтранице, ,",,,,,,,,0");
		ОбластьПодвалСтраницы.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ЧислоПрописью(КолвоПоСтранице);
		ОбластьПодвалСтраницы.Параметры.ИтогоСумма          = ?(ИтогСумма <> 0,Формат(ИтогСумма,ФорматВыводаСуммы), "-");
		ОбластьПодвалСтраницы.Параметры.ИтогоКоличествоУчет = Формат(ИтогКоличествоУчет,ФорматВыводаКоличества);
		ОбластьПодвалСтраницы.Параметры.ИтогоСуммаУчет      = ?(ИтогСуммаУчет <> 0,Формат(ИтогСуммаУчет,ФорматВыводаСуммы),"-");
		ОбластьПодвалСтраницы.Параметры.СуммаФактическиНаСтраницеПрописью                 = ?(ИтогСуммаУчет <> 0,ЧислоПрописью(ИтогСуммаУчет,ДанныеДокумента.Валюта),"");

		ТабличныйДокумент.Вывести(ОбластьПодвалСтраницы);

		// Выводим подвал документа.
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента);
		ОбластьПодвалОписи.Параметры.НачальныйНомерПоПорядку = 1;
		ОбластьПодвалОписи.Параметры.НомерКонца              = ВыборкаСтрокТовары.Количество();
		ОбластьПодвалОписи.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ЧислоПрописью(ВыборкаСтрокТовары.Итог("КоличествоФакт"));
		ОбластьПодвалОписи.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью     = ЧислоПрописью(ВыборкаСтрокТовары.Количество(), ,",,,,,,,,0");

		// Если нет движений по регистрам, то печатаем сумму документа.
		Если РозничныеЦены Тогда
			СуммаНаПечать = ВыборкаСтрокТовары.Итог("СуммаРозничнаяФакт");
		Иначе
			СуммаНаПечать = ВыборкаСтрокТовары.Итог("СуммаФакт");
		КонецЕсли;
		ОбластьПодвалОписи.Параметры.СуммаФактическиНаСтраницеПрописью	= ?(СуммаНаПечать <> 0,ЧислоПрописью(СуммаНаПечать,ДанныеДокумента.Валюта),"");
		
		// Заполним информацию о руководителях и ответственных.
		ОбластьПодвалОписи.Параметры.МОЛДолжность					= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		ОбластьПодвалОписи.Параметры.МОЛПредставление				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
	
		ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента.ПредседательКомиссии);
		ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента.ЧленКомиссии1);
		ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента.ЧленКомиссии2);
		ТабличныйДокумент.Вывести(ОбластьПодвалОписи);
	
		// Отметим конец области документа.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
	КонецЦикла;
	
	Возврат ТабличныйДокумент;	
КонецФункции

Функция ПечатьИНВ19Общая(МассивОбъектов, ОбъектыПечати)
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	// Добавим установку параметров печати
	ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ИНВ19Общая";
	УправлениеПечатьюПлатформа.УстановитьСтандартныеПараметрыПечати(ИмяПараметровПечати,ТабличныйДокумент);
	ТабличныйДокумент.ИмяПараметровПечати = ИмяПараметровПечати;
	
	ПервыйДокумент = Истина; 
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
		ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
				
		// Зададим параметры макета.
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		
		// Получим данные из документа.
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).ПолучитьДанныеДляПечатиИНВ19(Документ);
		
		Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.ПФ_MXL_ИНВ19");
		
		ОбластьЗаголовок        		    = Макет.ПолучитьОбласть("Шапка");
		ОбластьЗаголовокТаблицы				= Макет.ПолучитьОбласть("ЗаголовокТаблицы");
		ОбластьСтрокаТаблицы				= Макет.ПолучитьОбласть("СтрокаТаблицы");
		ОбластьИтогоТаблицы					= Макет.ПолучитьОбласть("ИтогоТаблицы");
		ОбластьИтогиПоСтранице  			= Макет.ПолучитьОбласть("ИтогоТаблицыИтогиПоСтранице");
		ОбластьИтогоТаблицы2				= Макет.ПолучитьОбласть("ИтогоТаблицы2");
		ОбластьПодвал		   				= Макет.ПолучитьОбласть("Подвал");
		
		ОбластьЗаголовок.Параметры.Заполнить(ДанныеДокумента);
		
		СтруктураПредставления = Новый Структура;
		СтруктураПредставления.Вставить("Наименование",     "");
		СтруктураПредставления.Вставить("ИНН",              "ИНН ");
		СтруктураПредставления.Вставить("АдресЮридический", "");
		СтруктураПредставления.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПредставления.Вставить("БанковскийСчет",   "р/с ");
		СтруктураПредставления.Вставить("Банк",             НСтр("ru = 'в банке'") + " ");
		СтруктураПредставления.Вставить("БИК",              "БИК ");
		СтруктураПредставления.Вставить("КоррСчет",         "к/с ");
		
		// Дата документа
		ДатаДокумента = Документ.Дата;
		ДополнительныеПараметры = Новый Структура("ПодразделениеКомпании", Документ.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПредставление = 
		УправлениеПечатьюПлатформа.ПолучитьПредставлениеСправочника(ОбластьЗаголовок.Параметры.Организация, СтруктураПредставления,,,
																			ДатаДокумента,ДополнительныеПараметры);
		
		ОбластьЗаголовок.Параметры.ПодразделениеКомпании = 
		УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПоОКПО			= ОбластьЗаголовок.Параметры.Организация.КодПоОКПО;
		ОбластьЗаголовок.Параметры.ВидДеятельностиПоОКДП		= ОбластьЗаголовок.Параметры.Организация.КодПоОКДП;
		ОбластьЗаголовок.Параметры.МОЛДолжность					= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		ОбластьЗаголовок.Параметры.МОЛПредставление				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
		
		ОбластьЗаголовок.Параметры.Номер = УправлениеПечатьюПлатформа.ПолучитьНомерДляПечати(Документ);
		ОбластьЗаголовок.Параметры.Основание = "приказ, постановление, распоряжение";
		
		ТабличныйДокумент.Вывести(ОбластьЗаголовок);
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();

		СтрокНаСтранице = 27;
		СтрокШапки      = 5;
		СтрокПодвала    = 5;
		НомерСтраницы   = 2;
		Ном             = 1;
		Сч              = 1;
		
		ИтогоРезультатИзлишекКолво   		= 0;
		ИтогоРезультатИзлишекСумма   		= 0;
		ИтогоРезультатНедостачаКолво 		= 0;
		ИтогоРезультатНедостачаСумма 		= 0;
		ИтогоПриходИзлишковКолво 	 		= 0;
		ИтогоПриходИзлишковСумма 	 		= 0;
		ИтогоСписаниеНедостачКолонка1Колво  = 0;
		ИтогоСписаниеНедостачКолонка1Сумма  = 0;


		
		// Выводим заголовок таблицы
		ОбластьЗаголовокТаблицы.Параметры.Заполнить(ДанныеДокумента);
		ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы; 
		ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);

		ВыборкаСтрокТовары = ДанныеДокумента.ВыборкаТабличнойЧасти;
		КоличествоСтрок = ВыборкаСтрокТовары.Количество();

		Если КоличествоСтрок = 1 Тогда
			ПереноситьПоследнююСтроку = 0;
		Иначе
			ЦелыхСтраницСПодвалом     = Цел((СтрокШапки + КоличествоСтрок + СтрокПодвала) / СтрокНаСтранице);
			ЦелыхСтраницБезПодвала    = Цел((СтрокШапки + КоличествоСтрок - 1) / СтрокНаСтранице);
			ПереноситьПоследнююСтроку = ЦелыхСтраницСПодвалом - ЦелыхСтраницБезПодвала;
		КонецЕсли;
		
		// Определим, что показывать в колонке кода - код или артикул.
		ИмяКолонкиКода = УправлениеПечатьюПлатформа.ПолучитьПараметрыРежимаВыводаКодаВДокументах().Имя;
		
		// Структура итогов по странице
		СтруктураИтогов = Новый Структура;
		СтруктураИтогов.Вставить("ИтогоРезультатИзлишекКолво",         0);
		СтруктураИтогов.Вставить("ИтогоРезультатИзлишекСумма",         0);
		СтруктураИтогов.Вставить("ИтогоРезультатНедостачаКолво",       0);
		СтруктураИтогов.Вставить("ИтогоРезультатНедостачаСумма",       0);
		СтруктураИтогов.Вставить("ИтогоПриходИзлишковКолво",           0);
		СтруктураИтогов.Вставить("ИтогоПриходИзлишковСумма",           0);
		СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Колво", 0);
		СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Сумма", 0);
		
		мсвДопОбластиПодвала = Новый Массив;
		мсвДопОбластиПодвала.Добавить(ОбластьИтогоТаблицы);
		мсвДопОбластиПодвала.Добавить(ОбластьПодвал);
			
		// Выводим многострочную часть документа.
		Для Каждого СтрокаТоваров Из ВыборкаСтрокТовары Цикл
		
			// Начинаем новую страницу, если предыдущая строка была последней на странице
			// или пора переносить последнюю строку на последнюю страницу с подвалом.
			ЦелаяСтраница	= (СтрокШапки + Ном - 1) / СтрокНаСтранице;
			
			Если (ЦелаяСтраница = Цел(ЦелаяСтраница))
				ИЛИ ((ПереноситьПоследнююСтроку = 1) И (Ном = КоличествоСтрок)) Тогда
			 
				УправлениеПечатьюПлатформа.ВывестиИтогиПоСтранице(ТабличныйДокумент,ОбластьИтогиПоСтранице,СтруктураИтогов,Документ);
				СтруктураИтогов = Новый Структура;
				СтруктураИтогов.Вставить("ИтогоРезультатИзлишекКолво",         0);
				СтруктураИтогов.Вставить("ИтогоРезультатИзлишекСумма",         0);
				СтруктураИтогов.Вставить("ИтогоРезультатНедостачаКолво",       0);
				СтруктураИтогов.Вставить("ИтогоРезультатНедостачаСумма",       0);
				СтруктураИтогов.Вставить("ИтогоПриходИзлишковКолво",           0);
				СтруктураИтогов.Вставить("ИтогоПриходИзлишковСумма",           0);
				СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Колво", 0);
				СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Сумма", 0);
				
				ТабличныйДокумент.Вывести(ОбластьИтогиПоСтранице);
				
				НомерСтраницы = НомерСтраницы + 1;
				ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
				ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы;
				ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);
				Ном =1;
			КонецЕсли;
			
			ОбластьСтрокаТаблицы.Параметры.Заполнить(СтрокаТоваров);
			
			Если ЕстьРеквизит(ВыборкаСтрокТовары, "ХарактеристикаНоменклатуры") Тогда
				Характеристика											= Строка(СтрокаТоваров.ХарактеристикаНоменклатуры);
			Иначе
				Характеристика											= "";
			КонецЕсли;
			ОбластьСтрокаТаблицы.Параметры.ТоварНаименование			= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(СтрокаТоваров.Номенклатура) + ?(Характеристика = "", "",", " + Характеристика);
			ОбластьСтрокаТаблицы.Параметры.ТоварКод						= УправлениеПечатьюПлатформа.ПолучитьЗначениеКолонкиКода(СтрокаТоваров.Номенклатура);
			ОбластьСтрокаТаблицы.Параметры.ЕдиницаИзмеренияНаименование = СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения;
			ОбластьСтрокаТаблицы.Параметры.ЕдиницаИзмеренияКодПоОКЕИ 	= СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения.Код;
			ОбластьСтрокаТаблицы.Параметры.Номер = Сч;
			
			Разница     = 0;
			РазницаСумм = 0;
			
			НеТребуетсяПересчетИзУпр = (ДанныеДокумента.Валюта = Константы.ВалютаУправленческогоУчетаКомпании.Получить());
			НеТребуетсяПересчетИзРегл = (ДанныеДокумента.Валюта = Константы.ВалютаРегламентированногоУчетаОрганизаций.Получить());
			
			Если ДанныеДокумента.ЕстьДвиженияПоРегистру Тогда
				Если ДанныеДокумента.ПростоеСписание Тогда
					Если СтрокаТоваров.СкладКомпании.Розничный Тогда
						РазницаСумм = СтрокаТоваров.СуммаРозн;
						Если Не НеТребуетсяПересчетИзРегл Тогда
							РазницаСумм = РаботаСКурсамиВалютПлатформа.ПересчетПоВалюте(РазницаСумм, 
												Константы.ВалютаРегламентированногоУчетаОрганизаций.Получить(), Документ.Дата, 
												ДанныеДокумента.ВалютаДокумента, Документ.КурсДокумента);
						КонецЕсли;
					Иначе
						Если НеТребуетсяПересчетИзРегл Тогда
							РазницаСумм = СтрокаТоваров.Сумма;
						Иначе
							РазницаСумм = СтрокаТоваров.СуммаУпр;
							Если НЕ НеТребуетсяПересчетИзУпр Тогда
								РазницаСумм = РаботаСКурсамиВалютПлатформа.ПересчетПоВалюте(РазницаСумм, 
												Константы.ВалютаУправленческогоУчетаКомпании.Получить(), Документ.Дата, 
												ДанныеДокумента.ВалютаДокумента, Документ.КурсДокумента);
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
				Иначе
					Если НеТребуетсяПересчетИзРегл Тогда
						РазницаСумм = СтрокаТоваров.Сумма;
					Иначе
						РазницаСумм = СтрокаТоваров.СуммаУпр;
						Если НЕ НеТребуетсяПересчетИзУпр Тогда
							РазницаСумм = РаботаСКурсамиВалютПлатформа.ПересчетПоВалюте(РазницаСумм, 
											Константы.ВалютаУправленческогоУчетаКомпании.Получить(), Документ.Дата, 
											ДанныеДокумента.ВалютаДокумента, Документ.КурсДокумента);
						КонецЕсли;
					КонецЕсли;
				КонецЕсли; 
				Разница = СтрокаТоваров.Количество;
				Если СтрокаТоваров.ВидДвижения = ВидДвиженияНакопления.Расход Тогда
					Разница     = -Разница;
					РазницаСумм = -РазницаСумм;
				КонецЕсли; 
		Иначе
			Разница     = СтрокаТоваров.КоличествоФакт - СтрокаТоваров.КоличествоКнижн;
			РазницаСумм = СтрокаТоваров.СуммаФакт - СтрокаТоваров.СуммаКнижн;
		КонецЕсли;
			
			Если Разница = 0 Тогда
				Продолжить;
			КонецЕсли;
			
			Если Разница < 0 Тогда
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекКолво   = 0;
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекСумма   = 0;
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаКолво = Формат(-Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаСумма = Формат(-РазницаСумм,ФорматВыводаСуммы);
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковКолво = 0;
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковСумма = 0;
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Колво   = Формат(-Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Сумма   = Формат(-РазницаСумм,ФорматВыводаСуммы);
			
			ИтогоРезультатИзлишекКолво   		= ИтогоРезультатИзлишекКолво   + 0;
			ИтогоРезультатИзлишекСумма   		= ИтогоРезультатИзлишекСумма   + 0;
			ИтогоРезультатНедостачаКолво 		= ИтогоРезультатНедостачаКолво + (-Разница);
			ИтогоРезультатНедостачаСумма 		= ИтогоРезультатНедостачаСумма + (-РазницаСумм);
			ИтогоПриходИзлишковКолво 	 		= ИтогоПриходИзлишковКолво 	+ 0;
			ИтогоПриходИзлишковСумма 	 		= ИтогоПриходИзлишковСумма + 0;
			ИтогоСписаниеНедостачКолонка1Колво  = ИтогоСписаниеНедостачКолонка1Колво   + (-Разница);
			ИтогоСписаниеНедостачКолонка1Сумма  = ИтогоСписаниеНедостачКолонка1Сумма   + (-РазницаСумм);
		Иначе
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекКолво   = Формат(Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекСумма   = Формат(РазницаСумм,ФорматВыводаСуммы);
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаКолво = 0;
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаСумма = 0;
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковКолво = Формат(Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковСумма = Формат(РазницаСумм,ФорматВыводаСуммы);
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Колво   = 0;
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Сумма   = 0;
			
			ИтогоРезультатИзлишекКолво   		= ИтогоРезультатИзлишекКолво  + Разница;
			ИтогоРезультатИзлишекСумма  		= ИтогоРезультатИзлишекСумма  + РазницаСумм;
			ИтогоРезультатНедостачаКолво 		= ИтогоРезультатНедостачаКолво + 0;
			ИтогоРезультатНедостачаСумма		= ИтогоРезультатНедостачаСумма + 0;
			ИтогоПриходИзлишковКолво 	 		= ИтогоПриходИзлишковКолво + Разница;
			ИтогоПриходИзлишковСумма 	 		= ИтогоПриходИзлишковСумма + РазницаСумм;
			ИтогоСписаниеНедостачКолонка1Колво  = ИтогоСписаниеНедостачКолонка1Колво   + 0;
			ИтогоСписаниеНедостачКолонка1Сумма  = ИтогоСписаниеНедостачКолонка1Сумма   + 0;
		КонецЕсли; 
			Ном = Ном + 1;
		
		СтраницаДоВывода = НомерСтраницы;
		Области = Новый Структура;
		Области.Вставить("Шапка",  ОбластьЗаголовокТаблицы);
		Области.Вставить("Подвал", ОбластьИтогиПоСтранице);
		НомерСтраницы = ВывестиГоризонтальнуюОбластьИНВ19(ТабличныйДокумент, ОбластьСтрокаТаблицы, Области, Документ, НомерСтраницы, СтруктураИтогов, мсвДопОбластиПодвала);
		Если СтраницаДоВывода <> НомерСтраницы Тогда
			СтруктураИтогов = Новый Структура;
			СтруктураИтогов.Вставить("ИтогоРезультатИзлишекКолво",         0);
			СтруктураИтогов.Вставить("ИтогоРезультатИзлишекСумма",         0);
			СтруктураИтогов.Вставить("ИтогоРезультатНедостачаКолво",       0);
			СтруктураИтогов.Вставить("ИтогоРезультатНедостачаСумма",       0);
			СтруктураИтогов.Вставить("ИтогоПриходИзлишковКолво",           0);
			СтруктураИтогов.Вставить("ИтогоПриходИзлишковСумма",           0);
			СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Колво", 0);
			СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Сумма", 0);
		КонецЕсли;
		
		Если Разница < 0 Тогда
			СтруктураИтогов.ИтогоРезультатИзлишекКолво   		= СтруктураИтогов.ИтогоРезультатИзлишекКолво + 0;
			СтруктураИтогов.ИтогоРезультатИзлишекСумма  		= СтруктураИтогов.ИтогоРезультатИзлишекСумма + 0;
			СтруктураИтогов.ИтогоРезультатНедостачаКолво 		= СтруктураИтогов.ИтогоРезультатНедостачаКолво + (-Разница);
			СтруктураИтогов.ИтогоРезультатНедостачаСумма 		= СтруктураИтогов.ИтогоРезультатНедостачаСумма + (-РазницаСумм);
			СтруктураИтогов.ИтогоПриходИзлишковКолво   			= СтруктураИтогов.ИтогоПриходИзлишковКолво + 0;
			СтруктураИтогов.ИтогоПриходИзлишковСумма   			= СтруктураИтогов.ИтогоПриходИзлишковСумма + 0;
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво + (-Разница);
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма + (-РазницаСумм);
		Иначе
			СтруктураИтогов.ИтогоРезультатИзлишекКолво   		= СтруктураИтогов.ИтогоРезультатИзлишекКолво + Разница;
			СтруктураИтогов.ИтогоРезультатИзлишекСумма  		= СтруктураИтогов.ИтогоРезультатИзлишекСумма + РазницаСумм;
			СтруктураИтогов.ИтогоРезультатНедостачаКолво 		= СтруктураИтогов.ИтогоРезультатНедостачаКолво + 0;
			СтруктураИтогов.ИтогоРезультатНедостачаСумма 		= СтруктураИтогов.ИтогоРезультатНедостачаСумма + 0;
			СтруктураИтогов.ИтогоПриходИзлишковКолво   			= СтруктураИтогов.ИтогоПриходИзлишковКолво + Разница;
			СтруктураИтогов.ИтогоПриходИзлишковСумма   			= СтруктураИтогов.ИтогоПриходИзлишковСумма + РазницаСумм;
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво + 0;
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма + 0;
		КонецЕсли;	
			Сч = Сч + 1;
	КонецЦикла;
	
		// Выведем область итогов по странице.
		Если НомерСтраницы > 2 Тогда
			УправлениеПечатьюПлатформа.ВывестиИтогиПоСтранице(ТабличныйДокумент,ОбластьИтогиПоСтранице,СтруктураИтогов,Документ);
			ОбластьИтогиПоСтранице.Параметры.Заполнить(СтруктураИтогов);
			ТабличныйДокумент.Вывести(ОбластьИтогиПоСтранице);
		КонецЕсли;
	
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатИзлишекКолво   		= Формат(ИтогоРезультатИзлишекКолво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатИзлишекСумма   		= Формат(ИтогоРезультатИзлишекСумма,ФорматВыводаСуммы);
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатНедостачаКолво 		= Формат(ИтогоРезультатНедостачаКолво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатНедостачаСумма 		= Формат(ИтогоРезультатНедостачаСумма,ФорматВыводаСуммы);
		ОбластьИтогоТаблицы.Параметры.ИтогоПриходИзлишковКолво   			= Формат(ИтогоПриходИзлишковКолво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоПриходИзлишковСумма   		 	= Формат(ИтогоПриходИзлишковСумма,ФорматВыводаСуммы);
		ОбластьИтогоТаблицы.Параметры.ИтогоСписаниеНедостачКолонка1Колво 	= Формат(ИтогоСписаниеНедостачКолонка1Колво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоСписаниеНедостачКолонка1Сумма 	= Формат(ИтогоСписаниеНедостачКолонка1Сумма,ФорматВыводаСуммы);
		ТабличныйДокумент.Вывести(ОбластьИтогоТаблицы);
		
		ОбластьПодвал.Параметры.Заполнить(ДанныеДокумента);
		
		// Заполним информацию о руководителях и ответственных.
	    ОбластьПодвал.Параметры.Заполнить(ДанныеДокумента.ГлавныйБухгалтер);
		ОбластьПодвал.Параметры.МОЛДолжность		= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		ОбластьПодвал.Параметры.МОЛПредставление	= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
	
		ТабличныйДокумент.Вывести(ОбластьПодвал);
	
		// Отметим конец области документа.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
	КонецЦикла;
	
	Возврат ТабличныйДокумент;
КонецФункции

// Функция проверяет попадание текущей области и области подвала на страницу.
//
// Параметры:
//  ТабДокумент 			 - табличный документ, в который выводится информация, подготовленная к печати,
//  Область 				 - текущая или массив текущих областей, предназначенных для вывода в данный момент,
//  Области 				 - Структура - области шапки и подвала:
// 	* Шапка 			 - область или массив областей, выступающих в качестве шапки и
// 								которые будут выводиться на каждой странице.
// 	* Подвал 			 - область, которая будет выводиться в качестве подвала на каждой странице
//  Документ 				 - печатаемый документ,
//  НомерСтраницы 			 - номер печатаемой страницы
//  СтруктураИтоговПоСтранице - структура содержащая, накопленные итоги по текущей странице
//					 	 	 значение ключа структуры должно совпадать с идентификатором реквизита табличной части документа
//  мсвДопОбластиПодвала  - подвальная часть макета. Проверка попадания последней строки таблицы на страницу
//							общего подвала Если = Неопределено, то проверка не осуществляется
//
// Возвращаемое значение:
//  Число - текущий номер страницы.
//
Функция ВывестиГоризонтальнуюОбластьИНВ19(ТабДокумент, Область, Области, Документ, НомерСтраницы = 1,
	СтруктураИтоговПоСтранице = Неопределено, мсвДопОбластиПодвала = Неопределено) Экспорт
	
	ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
	ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
	
	ИспользоватьРазбиениеНаСтраницы = ПравоПользователя("ИспользоватьРазбиениеНаСтраницы");
	
	// Если не разбиваем, выводим и все.
	Если Не ИспользоватьРазбиениеНаСтраницы Тогда
		ТабДокумент.Вывести(Область);
		Возврат НомерСтраницы;
	КонецЕсли;
	
	// Запишем их в один массив
	МассивОбластейДляПроверки = Новый Массив;
	МассивОбластейДляПроверки.Добавить(Область);

	Если Области.Подвал <> Неопределено Тогда
		МассивОбластейДляПроверки.Добавить(Области.Подвал);
	КонецЕсли;
	Если мсвДопОбластиПодвала <> Неопределено Тогда
		Для Каждого ТекОбласть Из мсвДопОбластиПодвала Цикл
			МассивОбластейДляПроверки.Добавить(ТекОбласть);
		КонецЦикла;
	КонецЕсли;
	
	Попытка
		// Проверим, помещаются ли на странице.
		РезультатПроверки = ТабДокумент.ПроверитьВывод(МассивОбластейДляПроверки);
		
		// Если не помещаются, то ...
		Если НЕ РезультатПроверки Тогда
			// Выводим итог по странице
			ВывестиИтогиПоСтраницеИНВ19(ТабДокумент,Области.Подвал,СтруктураИтоговПоСтранице,Документ); 
			ТабДокумент.ВывестиГоризонтальныйРазделительСтраниц(); // переходим на следующую страницу
			НомерСтраницы = НомерСтраницы + 1;
			Если Области.Шапка <> Неопределено Тогда
				Области.Шапка.Параметры.НомерСтраницы = "Страница " + НомерСтраницы;
				ТабДокумент.Вывести(Области.Шапка); // выводим шапку таблицы
			КонецЕсли;
		КонецЕсли;
	Исключение
		ЗаписьЖурналаРегистрации(НСтр("ru = 'Возникли проблемы с получением информации о текущем принтере'", ОбщегоНазначенияКлиентСервер.КодОсновногоЯзыка()), УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
	КонецПопытки;
	
	// Выводим текущую область
	ТабДокумент.Вывести(Область);
	
	Возврат НомерСтраницы;
КонецФункции

// Вывод итогов по странице печатной формы документа.
//
// Параметры:
// ТабДокумент - табличный документ, печатная форма документа
// ОбластьПодвал - область табличного документа, область куда выводятся итоги
// СтруктураИтоговПоСтранице - структура, сравнит данные для вывода
// Права   - структура, права текущего пользователя или объект документа.
//
Процедура ВывестиИтогиПоСтраницеИНВ19(ТабДокумент,ОбластьПодвал,СтруктураИтоговПоСтранице,Документ) Экспорт 
	
	ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
	ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
	Если ОбластьПодвал <> Неопределено Тогда
		Если СтруктураИтоговПоСтранице <> Неопределено Тогда
			Для Каждого ЭлементИтога Из СтруктураИтоговПоСтранице Цикл
				Если СтрНайти(ЭлементИтога.Ключ,"Сум") <> 0 Тогда
					ЗначениеПараметра = Формат(ЭлементИтога.Значение,ФорматВыводаСуммы);
				Иначе
					ЗначениеПараметра = Формат(ЭлементИтога.Значение,ФорматВыводаКоличества);
				КонецЕсли;
				Попытка
					ОбластьПодвал.Параметры[ЭлементИтога.Ключ] = ЗначениеПараметра;
				Исключение
				КонецПопытки;
			КонецЦикла;
		КонецЕсли;
		
		ТабДокумент.Вывести(ОбластьПодвал);
	КонецЕсли;
КонецПроцедуры 

//Мальцев А.Ю. +++
Функция итПечатьИНВ3Общая(МассивОбъектов, ОбъектыПечати)
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	// Добавим установку параметров печати
	ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ИНВ3Общая";
	УправлениеПечатьюПлатформа.УстановитьСтандартныеПараметрыПечати(ИмяПараметровПечати,ТабличныйДокумент);
	ТабличныйДокумент.ИмяПараметровПечати = ИмяПараметровПечати;

	ПервыйДокумент = Истина;
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
		ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
		
		// Зададим параметры макета.
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		
		// Получим данные из документа.
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).итПолучитьДанныеДляПечатиИНВ3(Документ);
		
		Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итПФ_MXL_ИНВ3");
		
		ОбластьЗаголовок        	= Макет.ПолучитьОбласть("Шапка");
		ОбластьЗаголовокМол        	= Макет.ПолучитьОбласть("ШапкаМол");
		ОбластьЗаголовокОкончание   = Макет.ПолучитьОбласть("ШапкаОкончание");
		
		ОбластьЗаголовокТаблицы		= Макет.ПолучитьОбласть("ЗаголовокТаблицы");
		ОбластьСтрока				= Макет.ПолучитьОбласть("Строка");
		ОбластьПодвалСтраницы		= Макет.ПолучитьОбласть("ПодвалСтраницы");
		ОбластьПодвалОписи      	= Макет.ПолучитьОбласть("ПодвалОписи");
		ОбластьПодвалОписиМол      	= Макет.ПолучитьОбласть("ПодвалОписиМол");
		ОбластьПодвалОписиОкончание	= Макет.ПолучитьОбласть("ПодвалОписиОкончание");
		
		ОбластьЗаголовок.Параметры.Заполнить(ДанныеДокумента);
		
		СтруктураПредставления = Новый Структура;
		СтруктураПредставления.Вставить("Наименование",     "");
		СтруктураПредставления.Вставить("ИНН",              "ИНН ");
		СтруктураПредставления.Вставить("АдресЮридический", "");
		СтруктураПредставления.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПредставления.Вставить("БанковскийСчет",   "р/с ");
		СтруктураПредставления.Вставить("Банк",             НСтр("ru = 'в банке'") + " ");
		СтруктураПредставления.Вставить("БИК",              "БИК ");
		СтруктураПредставления.Вставить("КоррСчет",         "к/с ");
		
		// Дата документа
		ДатаДокумента = Документ.Дата;
		ДополнительныеПараметры = Новый Структура("ПодразделениеКомпании", Документ.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПредставление = 
		УправлениеПечатьюПлатформа.ПолучитьПредставлениеСправочника(ОбластьЗаголовок.Параметры.Организация, 
																		СтруктураПредставления,,,ДатаДокумента,ДополнительныеПараметры);
		
		ОбластьЗаголовок.Параметры.ПодразделениеКомпании = 
		УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПоОКПО			= ОбластьЗаголовок.Параметры.Организация.КодПоОКПО;
		ОбластьЗаголовок.Параметры.ВидДеятельностиПоОКДП		= ОбластьЗаголовок.Параметры.Организация.КодПоОКДП;
		//ОбластьЗаголовок.Параметры.МОЛДолжность					= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		//ОбластьЗаголовок.Параметры.МОЛПредставление				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
		
		ОбластьЗаголовок.Параметры.Номер = УправлениеПечатьюПлатформа.ПолучитьНомерДляПечати(Документ);
		
		Если ЕстьРеквизит(Документ, "ДатаНачалаИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаНачалаИнвентаризации = Документ.ДатаНачалаИнвентаризации;
		КонецЕсли;
		Если ЕстьРеквизит(Документ, "ДатаОкончанияИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаОкончанияИнвентаризации = Документ.ДатаОкончанияИнвентаризации;
		КонецЕсли;
		
		ВидЦенностей = "товарно-материальные ценности";
		Если ДанныеДокумента.Локальная Тогда
			ВидЦенностей = "товарно-материальные ценности по группам товаров: " + ДанныеДокумента.ОписаниеЛокальныхФильтров;
		КонецЕсли;		
		ОбластьЗаголовок.Параметры.ВидЦенностей = ВидЦенностей;
		
		
		ТабличныйДокумент.Вывести(ОбластьЗаголовок);
		
		Если ДанныеДокумента.МассивМОЛ.Количество() Тогда
			Для Каждого стрМОЛ Из ДанныеДокумента.МассивМОЛ Цикл
				ОбластьЗаголовокМол.Параметры.МОЛДолжность     = стрМОЛ.Должность;
				ОбластьЗаголовокМол.Параметры.МОЛПредставление = стрМОЛ;
				ТабличныйДокумент.Вывести(ОбластьЗаголовокМол);				
			КонецЦикла;
		Иначе
			ТабличныйДокумент.Вывести(ОбластьЗаголовокМол);
		КонецЕсли;	
		
		ОбластьЗаголовокОкончание.Параметры.ДатаСнятияОстатков = ДанныеДокумента.ДатаСнятияОстатков;
		ТабличныйДокумент.Вывести(ОбластьЗаголовокОкончание);
		
		
		
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();

		СтрокНаСтранице	= 24;
		СтрокШапки		= 5;
		СтрокПодвала	= 5;
		НомерСтраницы	= 2;
		Ном				= 1;
		
		ИтогКоличество		= 0;
		ИтогКоличествоУчет	= 0;
		ИтогСумма			= 0;
		ИтогСуммаУчет		= 0;

		
		КолвоСтрокПоСтранице	= 0;
		КолвоПоСтранице			= 0;
		СуммаЛиста				= 0;
		
		// Выводим заголовок таблицы.
		ОбластьЗаголовокТаблицы.Параметры.Заполнить(ДанныеДокумента);
		ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница" + " " + НомерСтраницы; 
		ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);

		ВыборкаСтрокТовары = ДанныеДокумента.ВыборкаТабличнойЧасти;
		КоличествоСтрок = ВыборкаСтрокТовары.Количество();

		Если КоличествоСтрок = 1 Тогда
			ПереноситьПоследнююСтроку = 0;
		Иначе
			ЦелыхСтраницСПодвалом     = Цел((СтрокШапки + КоличествоСтрок + СтрокПодвала) / СтрокНаСтранице);
			ЦелыхСтраницБезПодвала    = Цел((СтрокШапки + КоличествоСтрок - 1) / СтрокНаСтранице);
			ПереноситьПоследнююСтроку = ЦелыхСтраницСПодвалом - ЦелыхСтраницБезПодвала;
		КонецЕсли;

		// Определим, что показывать в колонке кода - код или артикул.
		ИмяКолонкиКода = УправлениеПечатьюПлатформа.ПолучитьПараметрыРежимаВыводаКодаВДокументах().Имя;
		
		РозничныеЦены = ПолучитьЗначениеПараметраСтруктуры(ДанныеДокумента, "РозничныеЦены", Ложь);
		
		// Выводим многострочную часть документа.
		Для Каждого СтрокаТоваров Из ВыборкаСтрокТовары Цикл
		
			
			// Начинаем новую страницу, если предыдущая строка была последней на странице
			// или пора переносить последнюю строку на последнюю страницу с подвалом.
			ЦелаяСтраница	= (СтрокШапки + Ном - 1) / СтрокНаСтранице;

			Если (ЦелаяСтраница = Цел(ЦелаяСтраница))
			 ИЛИ ((ПереноситьПоследнююСтроку = 1) И (Ном = КоличествоСтрок)) Тогда

				ОбластьПодвалСтраницы.Параметры.ИтогоКоличество     = Формат(ИтогКоличество,ФорматВыводаКоличества);
				ОбластьПодвалСтраницы.Параметры.ИтогоКоличествоУчет = Формат(ИтогКоличествоУчет,ФорматВыводаКоличества);
				ОбластьПодвалСтраницы.Параметры.ИтогоСумма          = Формат(ИтогСумма,ФорматВыводаСуммы);
				ОбластьПодвалСтраницы.Параметры.ИтогоСуммаУчет      = Формат(ИтогСуммаУчет,ФорматВыводаСуммы);

				ОбластьПодвалСтраницы.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью     = ЧислоПрописью(КолвоСтрокПоСтранице, ,",,,,,,,,0");
				ОбластьПодвалСтраницы.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ЧислоПрописью(КолвоПоСтранице);
				ОбластьПодвалСтраницы.Параметры.СуммаФактическиНаСтраницеПрописью              	  = ЧислоПрописью(ИтогСуммаУчет,ДанныеДокумента.Валюта);
				ТабличныйДокумент.Вывести(ОбластьПодвалСтраницы);
				
				НомерСтраницы = НомерСтраницы + 1;
				ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();

				ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница" + " " + НомерСтраницы;
				ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);
				Ном =1;

				ИтогКоличество 		= 0;
				ИтогКоличествоУчет  = 0;
				ИтогСумма     		= 0;
				ИтогСуммаУчет       = 0;

				КолвоСтрокПоСтранице = 0;
				КолвоПоСтранице      = 0;
				СуммаЛиста           = 0;

			КонецЕсли;
			
			ОбластьСтрока.Параметры.Заполнить(СтрокаТоваров);
			ОбластьСтрока.Параметры.КоличествоУчет					= Формат(СтрокаТоваров.КоличествоФакт,ФорматВыводаКоличества);
			ОбластьСтрока.Параметры.Количество						= Формат(СтрокаТоваров.КоличествоКнижн,ФорматВыводаКоличества);
			Если ЕстьРеквизит(ВыборкаСтрокТовары, "ХарактеристикаНоменклатуры") Тогда
				Характеристика										= Строка(СтрокаТоваров.ХарактеристикаНоменклатуры);
			Иначе
				Характеристика										= "";
			КонецЕсли;
			ОбластьСтрока.Параметры.ТоварНаименование				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(СтрокаТоваров.Номенклатура) + ?(Характеристика = "", "",", " + Характеристика);
			Если ЕстьРеквизит(ВыборкаСтрокТовары, "ТоварКод") Тогда
				ОбластьСтрока.Параметры.ТоварКод					= СтрокаТоваров.ТоварКод;
			Иначе
				ОбластьСтрока.Параметры.ТоварКод					= УправлениеПечатьюПлатформа.ПолучитьЗначениеКолонкиКода(СтрокаТоваров.Номенклатура);
			КонецЕсли;
			Если ЕстьРеквизит(СтрокаТоваров.Номенклатура, "БазоваяЕдиницаИзмерения") Тогда
				ОбластьСтрока.Параметры.ЕдиницаИзмеренияНаименование= СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения;
				ОбластьСтрока.Параметры.ЕдиницаИзмеренияКодПоОКЕИ 	= СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения.Код;
			КонецЕсли;
			ОбластьСтрока.Параметры.Номер							= СтрокаТоваров.НомерСтроки;
			Если РозничныеЦены Тогда
				ОбластьСтрока.Параметры.Цена							= ?(СтрокаТоваров.ЦенаРозничная <> 0,Формат(СтрокаТоваров.ЦенаРозничная,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.СуммаУчет	    				= ?(СтрокаТоваров.СуммаРозничнаяФакт <> 0,Формат(СтрокаТоваров.СуммаРозничнаяФакт,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.Сумма       					= ?(СтрокаТоваров.СуммаРозничнаяКнижн <> 0,Формат(СтрокаТоваров.СуммаРозничнаяКнижн,ФорматВыводаСуммы),"-");
			Иначе
				ОбластьСтрока.Параметры.Цена							= ?(СтрокаТоваров.Цена <> 0,Формат(СтрокаТоваров.Цена,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.СуммаУчет	    				= ?(СтрокаТоваров.СуммаФакт <> 0,Формат(СтрокаТоваров.СуммаФакт,ФорматВыводаСуммы),"-");
				ОбластьСтрока.Параметры.Сумма       					= ?(СтрокаТоваров.СуммаКнижн <> 0,Формат(СтрокаТоваров.СуммаКнижн,ФорматВыводаСуммы),"-");
			КонецЕсли;

			ТабличныйДокумент.Вывести(ОбластьСтрока);
			Ном = Ном + 1;

			ИтогКоличество     = ИтогКоличество     + СтрокаТоваров.КоличествоКнижн;
			ИтогКоличествоУчет = ИтогКоличествоУчет + СтрокаТоваров.КоличествоФакт;
			Если РозничныеЦены Тогда
				ИтогСумма          = ИтогСумма          + СтрокаТоваров.СуммаРозничнаяКнижн;
				ИтогСуммаУчет      = ИтогСуммаУчет      + СтрокаТоваров.СуммаРозничнаяФакт;
			Иначе
				ИтогСумма          = ИтогСумма          + СтрокаТоваров.СуммаКнижн;
				ИтогСуммаУчет      = ИтогСуммаУчет      + СтрокаТоваров.СуммаФакт;
			КонецЕсли;


			КолвоСтрокПоСтранице = КолвоСтрокПоСтранице + 1;
			КолвоПоСтранице      = КолвоПоСтранице      + СтрокаТоваров.КоличествоФакт;
			СуммаЛиста           = СуммаЛиста           + СтрокаТоваров.СуммаФакт;

		КонецЦикла;

		// Выводим итоги по последней странице.
		ОбластьПодвалСтраницы.Параметры.ИтогоКоличество     = Формат(ИтогКоличество,ФорматВыводаКоличества);
		ОбластьПодвалСтраницы.Параметры.ИтогоКоличествоУчет = Формат(ИтогКоличествоУчет,ФорматВыводаКоличества);
		ОбластьПодвалСтраницы.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью     = ЧислоПрописью(КолвоСтрокПоСтранице, ,",,,,,,,,0");
		ОбластьПодвалСтраницы.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ЧислоПрописью(КолвоПоСтранице);
		ОбластьПодвалСтраницы.Параметры.ИтогоСумма          = ?(ИтогСумма <> 0,Формат(ИтогСумма,ФорматВыводаСуммы), "-");
		ОбластьПодвалСтраницы.Параметры.ИтогоКоличествоУчет = Формат(ИтогКоличествоУчет,ФорматВыводаКоличества);
		ОбластьПодвалСтраницы.Параметры.ИтогоСуммаУчет      = ?(ИтогСуммаУчет <> 0,Формат(ИтогСуммаУчет,ФорматВыводаСуммы),"-");
		ОбластьПодвалСтраницы.Параметры.СуммаФактическиНаСтраницеПрописью                 = ?(ИтогСуммаУчет <> 0,ЧислоПрописью(ИтогСуммаУчет,ДанныеДокумента.Валюта),"");

		ТабличныйДокумент.Вывести(ОбластьПодвалСтраницы);

		// Выводим подвал документа.
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента);
		ОбластьПодвалОписи.Параметры.НачальныйНомерПоПорядку = 1;
		ОбластьПодвалОписи.Параметры.НомерКонца              = ВыборкаСтрокТовары.Количество();
		ОбластьПодвалОписи.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ЧислоПрописью(ВыборкаСтрокТовары.Итог("КоличествоФакт"));
		ОбластьПодвалОписи.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью     = ЧислоПрописью(ВыборкаСтрокТовары.Количество(), ,",,,,,,,,0");

		// Если нет движений по регистрам, то печатаем сумму документа.
		Если РозничныеЦены Тогда
			СуммаНаПечать = ВыборкаСтрокТовары.Итог("СуммаРозничнаяФакт");
		Иначе
			СуммаНаПечать = ВыборкаСтрокТовары.Итог("СуммаФакт");
		КонецЕсли;
		ОбластьПодвалОписи.Параметры.СуммаФактическиНаСтраницеПрописью	= ?(СуммаНаПечать <> 0,ЧислоПрописью(СуммаНаПечать,ДанныеДокумента.Валюта),"");
		
		// Заполним информацию о руководителях и ответственных.
		//	ОбластьПодвалОписи.Параметры.МОЛДолжность					= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		//	ОбластьПодвалОписи.Параметры.МОЛПредставление				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
		//
		//	ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента.ПредседательКомиссии);
		//	ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента.ЧленКомиссии1);
		//	ОбластьПодвалОписи.Параметры.Заполнить(ДанныеДокумента.ЧленКомиссии2);
	
		//Заполнить членов комиссии
		ОбластьПодвалОписи.Параметры.ПредседательКомиссииДолжность      = ДанныеДокумента.ПредседательКомиссии.Должность;
		ОбластьПодвалОписи.Параметры.ПредседательКомиссииПредставление  = ДанныеДокумента.ПредседательКомиссии;
		
		сч = 1;
		Для Каждого стрКомиссии Из ДанныеДокумента.МассивЧленовКомиссии Цикл
			
			Если сч > 3 Тогда
				Прервать;
			КонецЕсли;
			
			ОбластьПодвалОписи.Параметры["ЧленКомиссии" + Строка(сч) + "Должность"]      = стрКомиссии.Должность;
			ОбластьПодвалОписи.Параметры["ЧленКомиссии" + Строка(сч) + "Представление"]  = стрКомиссии;	
			
			сч = сч + 1;
			
		КонецЦикла;	
		
		ТабличныйДокумент.Вывести(ОбластьПодвалОписи);
				
		Если ДанныеДокумента.МассивМОЛ.Количество() Тогда
			Для Каждого стрМОЛ Из ДанныеДокумента.МассивМОЛ Цикл
				ОбластьПодвалОписиМол.Параметры.МОЛДолжность     = стрМОЛ.Должность;
				ОбластьПодвалОписиМол.Параметры.МОЛПредставление = стрМОЛ;
				ТабличныйДокумент.Вывести(ОбластьПодвалОписиМол);				
			КонецЦикла;
		Иначе
			ТабличныйДокумент.Вывести(ОбластьПодвалОписиМол);
		КонецЕсли;	
		
		ТабличныйДокумент.Вывести(ОбластьПодвалОписиОкончание);
		
		
		// Отметим конец области документа.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
	КонецЦикла;
	
	Возврат ТабличныйДокумент;	
КонецФункции

Функция итПечатьИНВ19Общая(МассивОбъектов, ОбъектыПечати)
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	// Добавим установку параметров печати
	ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ИНВ19Общая";
	УправлениеПечатьюПлатформа.УстановитьСтандартныеПараметрыПечати(ИмяПараметровПечати,ТабличныйДокумент);
	ТабличныйДокумент.ИмяПараметровПечати = ИмяПараметровПечати;
	
	ПервыйДокумент = Истина; 
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
		ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
				
		// Зададим параметры макета.
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		
		// Получим данные из документа.
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).итПолучитьДанныеДляПечатиИНВ19(Документ);
		
		Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итПФ_MXL_ИНВ19");
		
		ОбластьЗаголовок        		    = Макет.ПолучитьОбласть("Шапка");
		ОбластьЗаголовокМол        		    = Макет.ПолучитьОбласть("ШапкаМол");
		ОбластьЗаголовокОкончание  		    = Макет.ПолучитьОбласть("ШапкаОкончание");
		
		ОбластьЗаголовокТаблицы				= Макет.ПолучитьОбласть("ЗаголовокТаблицы");
		ОбластьСтрокаТаблицы				= Макет.ПолучитьОбласть("СтрокаТаблицы");
		ОбластьИтогоТаблицы					= Макет.ПолучитьОбласть("ИтогоТаблицы");
		ОбластьИтогиПоСтранице  			= Макет.ПолучитьОбласть("ИтогоТаблицыИтогиПоСтранице");
		ОбластьИтогоТаблицы2				= Макет.ПолучитьОбласть("ИтогоТаблицы2");
		ОбластьПодвал		   				= Макет.ПолучитьОбласть("Подвал");
		ОбластьПодвалМол		   				= Макет.ПолучитьОбласть("ПодвалМол");
		
		ОбластьЗаголовок.Параметры.Заполнить(ДанныеДокумента);
		
		СтруктураПредставления = Новый Структура;
		СтруктураПредставления.Вставить("Наименование",     "");
		СтруктураПредставления.Вставить("ИНН",              "ИНН ");
		СтруктураПредставления.Вставить("АдресЮридический", "");
		СтруктураПредставления.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПредставления.Вставить("БанковскийСчет",   "р/с ");
		СтруктураПредставления.Вставить("Банк",             НСтр("ru = 'в банке'") + " ");
		СтруктураПредставления.Вставить("БИК",              "БИК ");
		СтруктураПредставления.Вставить("КоррСчет",         "к/с ");
		
		// Дата документа
		ДатаДокумента = Документ.Дата;
		ДополнительныеПараметры = Новый Структура("ПодразделениеКомпании", Документ.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПредставление = 
		УправлениеПечатьюПлатформа.ПолучитьПредставлениеСправочника(ОбластьЗаголовок.Параметры.Организация, СтруктураПредставления,,,
																			ДатаДокумента,ДополнительныеПараметры);
		
		ОбластьЗаголовок.Параметры.ПодразделениеКомпании = 
		УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПоОКПО			= ОбластьЗаголовок.Параметры.Организация.КодПоОКПО;
		ОбластьЗаголовок.Параметры.ВидДеятельностиПоОКДП		= ОбластьЗаголовок.Параметры.Организация.КодПоОКДП;
		//ОбластьЗаголовок.Параметры.МОЛДолжность					= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		//ОбластьЗаголовок.Параметры.МОЛПредставление				= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
		
		ОбластьЗаголовок.Параметры.Номер = УправлениеПечатьюПлатформа.ПолучитьНомерДляПечати(Документ);
		//ОбластьЗаголовок.Параметры.Основание = "приказ, постановление, распоряжение";
		
		ТабличныйДокумент.Вывести(ОбластьЗаголовок);
		
		
		Если ДанныеДокумента.МассивМОЛ.Количество() Тогда
			Для Каждого стрМОЛ Из ДанныеДокумента.МассивМОЛ Цикл
				ОбластьЗаголовокМол.Параметры.МОЛДолжность     = стрМОЛ.Должность;
				ОбластьЗаголовокМол.Параметры.МОЛПредставление = стрМОЛ;
				ТабличныйДокумент.Вывести(ОбластьЗаголовокМол);				
			КонецЦикла;
		Иначе
			ТабличныйДокумент.Вывести(ОбластьЗаголовокМол);
		КонецЕсли;	
		
		ОбластьЗаголовокОкончание.Параметры.ДатаНачалаИнвентаризации = ДанныеДокумента.ДатаСнятияОстатков;
		ТабличныйДокумент.Вывести(ОбластьЗаголовокОкончание);
				
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();

		СтрокНаСтранице = 27;
		СтрокШапки      = 5;
		СтрокПодвала    = 5;
		НомерСтраницы   = 2;
		Ном             = 1;
		Сч              = 1;
		
		ИтогоРезультатИзлишекКолво   		= 0;
		ИтогоРезультатИзлишекСумма   		= 0;
		ИтогоРезультатНедостачаКолво 		= 0;
		ИтогоРезультатНедостачаСумма 		= 0;
		ИтогоПриходИзлишковКолво 	 		= 0;
		ИтогоПриходИзлишковСумма 	 		= 0;
		ИтогоСписаниеНедостачКолонка1Колво  = 0;
		ИтогоСписаниеНедостачКолонка1Сумма  = 0;


		
		// Выводим заголовок таблицы
		ОбластьЗаголовокТаблицы.Параметры.Заполнить(ДанныеДокумента);
		ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы; 
		ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);

		ВыборкаСтрокТовары = ДанныеДокумента.ВыборкаТабличнойЧасти;
		КоличествоСтрок = ВыборкаСтрокТовары.Количество();

		Если КоличествоСтрок = 1 Тогда
			ПереноситьПоследнююСтроку = 0;
		Иначе
			ЦелыхСтраницСПодвалом     = Цел((СтрокШапки + КоличествоСтрок + СтрокПодвала) / СтрокНаСтранице);
			ЦелыхСтраницБезПодвала    = Цел((СтрокШапки + КоличествоСтрок - 1) / СтрокНаСтранице);
			ПереноситьПоследнююСтроку = ЦелыхСтраницСПодвалом - ЦелыхСтраницБезПодвала;
		КонецЕсли;
		
		// Определим, что показывать в колонке кода - код или артикул.
		ИмяКолонкиКода = УправлениеПечатьюПлатформа.ПолучитьПараметрыРежимаВыводаКодаВДокументах().Имя;
		
		// Структура итогов по странице
		СтруктураИтогов = Новый Структура;
		СтруктураИтогов.Вставить("ИтогоРезультатИзлишекКолво",         0);
		СтруктураИтогов.Вставить("ИтогоРезультатИзлишекСумма",         0);
		СтруктураИтогов.Вставить("ИтогоРезультатНедостачаКолво",       0);
		СтруктураИтогов.Вставить("ИтогоРезультатНедостачаСумма",       0);
		СтруктураИтогов.Вставить("ИтогоПриходИзлишковКолво",           0);
		СтруктураИтогов.Вставить("ИтогоПриходИзлишковСумма",           0);
		СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Колво", 0);
		СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Сумма", 0);
		
		мсвДопОбластиПодвала = Новый Массив;
		мсвДопОбластиПодвала.Добавить(ОбластьИтогоТаблицы);
		мсвДопОбластиПодвала.Добавить(ОбластьПодвал);
			
		// Выводим многострочную часть документа.
		Для Каждого СтрокаТоваров Из ВыборкаСтрокТовары Цикл
		
			// Начинаем новую страницу, если предыдущая строка была последней на странице
			// или пора переносить последнюю строку на последнюю страницу с подвалом.
			ЦелаяСтраница	= (СтрокШапки + Ном - 1) / СтрокНаСтранице;
			
			Если (ЦелаяСтраница = Цел(ЦелаяСтраница))
				ИЛИ ((ПереноситьПоследнююСтроку = 1) И (Ном = КоличествоСтрок)) Тогда
			 
				УправлениеПечатьюПлатформа.ВывестиИтогиПоСтранице(ТабличныйДокумент,ОбластьИтогиПоСтранице,СтруктураИтогов,Документ);
				СтруктураИтогов = Новый Структура;
				СтруктураИтогов.Вставить("ИтогоРезультатИзлишекКолво",         0);
				СтруктураИтогов.Вставить("ИтогоРезультатИзлишекСумма",         0);
				СтруктураИтогов.Вставить("ИтогоРезультатНедостачаКолво",       0);
				СтруктураИтогов.Вставить("ИтогоРезультатНедостачаСумма",       0);
				СтруктураИтогов.Вставить("ИтогоПриходИзлишковКолво",           0);
				СтруктураИтогов.Вставить("ИтогоПриходИзлишковСумма",           0);
				СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Колво", 0);
				СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Сумма", 0);
				
				ТабличныйДокумент.Вывести(ОбластьИтогиПоСтранице);
				
				НомерСтраницы = НомерСтраницы + 1;
				ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
				ОбластьЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы;
				ТабличныйДокумент.Вывести(ОбластьЗаголовокТаблицы);
				Ном =1;
			КонецЕсли;
			
			ОбластьСтрокаТаблицы.Параметры.Заполнить(СтрокаТоваров);
			
			Если ЕстьРеквизит(ВыборкаСтрокТовары, "ХарактеристикаНоменклатуры") Тогда
				Характеристика											= Строка(СтрокаТоваров.ХарактеристикаНоменклатуры);
			Иначе
				Характеристика											= "";
			КонецЕсли;
			ОбластьСтрокаТаблицы.Параметры.ТоварНаименование			= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(СтрокаТоваров.Номенклатура) + ?(Характеристика = "", "",", " + Характеристика);
			ОбластьСтрокаТаблицы.Параметры.ТоварКод						= УправлениеПечатьюПлатформа.ПолучитьЗначениеКолонкиКода(СтрокаТоваров.Номенклатура);
			ОбластьСтрокаТаблицы.Параметры.ЕдиницаИзмеренияНаименование = СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения;
			ОбластьСтрокаТаблицы.Параметры.ЕдиницаИзмеренияКодПоОКЕИ 	= СтрокаТоваров.Номенклатура.БазоваяЕдиницаИзмерения.Код;
			ОбластьСтрокаТаблицы.Параметры.Номер = Сч;
			
			Разница     = 0;
			РазницаСумм = 0;
			
			НеТребуетсяПересчетИзУпр = (ДанныеДокумента.Валюта = Константы.ВалютаУправленческогоУчетаКомпании.Получить());
			НеТребуетсяПересчетИзРегл = (ДанныеДокумента.Валюта = Константы.ВалютаРегламентированногоУчетаОрганизаций.Получить());
			
			Если ДанныеДокумента.ЕстьДвиженияПоРегистру Тогда
				Если ДанныеДокумента.ПростоеСписание Тогда
					Если СтрокаТоваров.СкладКомпании.Розничный Тогда
						РазницаСумм = СтрокаТоваров.СуммаРозн;
						Если Не НеТребуетсяПересчетИзРегл Тогда
							РазницаСумм = РаботаСКурсамиВалютПлатформа.ПересчетПоВалюте(РазницаСумм, 
												Константы.ВалютаРегламентированногоУчетаОрганизаций.Получить(), Документ.Дата, 
												ДанныеДокумента.ВалютаДокумента, Документ.КурсДокумента);
						КонецЕсли;
					Иначе
						Если НеТребуетсяПересчетИзРегл Тогда
							РазницаСумм = СтрокаТоваров.Сумма;
						Иначе
							РазницаСумм = СтрокаТоваров.СуммаУпр;
							Если НЕ НеТребуетсяПересчетИзУпр Тогда
								РазницаСумм = РаботаСКурсамиВалютПлатформа.ПересчетПоВалюте(РазницаСумм, 
												Константы.ВалютаУправленческогоУчетаКомпании.Получить(), Документ.Дата, 
												ДанныеДокумента.ВалютаДокумента, Документ.КурсДокумента);
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
				Иначе
					Если НеТребуетсяПересчетИзРегл Тогда
						РазницаСумм = СтрокаТоваров.Сумма;
					Иначе
						РазницаСумм = СтрокаТоваров.СуммаУпр;
						Если НЕ НеТребуетсяПересчетИзУпр Тогда
							РазницаСумм = РаботаСКурсамиВалютПлатформа.ПересчетПоВалюте(РазницаСумм, 
											Константы.ВалютаУправленческогоУчетаКомпании.Получить(), Документ.Дата, 
											ДанныеДокумента.ВалютаДокумента, Документ.КурсДокумента);
						КонецЕсли;
					КонецЕсли;
				КонецЕсли; 
				Разница = СтрокаТоваров.Количество;
				Если СтрокаТоваров.ВидДвижения = ВидДвиженияНакопления.Расход Тогда
					Разница     = -Разница;
					РазницаСумм = -РазницаСумм;
				КонецЕсли; 
		Иначе
			Разница     = СтрокаТоваров.КоличествоФакт - СтрокаТоваров.КоличествоКнижн;
			РазницаСумм = СтрокаТоваров.СуммаФакт - СтрокаТоваров.СуммаКнижн;
		КонецЕсли;
			
			Если Разница = 0 Тогда
				Продолжить;
			КонецЕсли;
			
			Если Разница < 0 Тогда
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекКолво   = 0;
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекСумма   = 0;
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаКолво = Формат(-Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаСумма = Формат(-РазницаСумм,ФорматВыводаСуммы);
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковКолво = 0;
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковСумма = 0;
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Колво   = Формат(-Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Сумма   = Формат(-РазницаСумм,ФорматВыводаСуммы);
			
			ИтогоРезультатИзлишекКолво   		= ИтогоРезультатИзлишекКолво   + 0;
			ИтогоРезультатИзлишекСумма   		= ИтогоРезультатИзлишекСумма   + 0;
			ИтогоРезультатНедостачаКолво 		= ИтогоРезультатНедостачаКолво + (-Разница);
			ИтогоРезультатНедостачаСумма 		= ИтогоРезультатНедостачаСумма + (-РазницаСумм);
			ИтогоПриходИзлишковКолво 	 		= ИтогоПриходИзлишковКолво 	+ 0;
			ИтогоПриходИзлишковСумма 	 		= ИтогоПриходИзлишковСумма + 0;
			ИтогоСписаниеНедостачКолонка1Колво  = ИтогоСписаниеНедостачКолонка1Колво   + (-Разница);
			ИтогоСписаниеНедостачКолонка1Сумма  = ИтогоСписаниеНедостачКолонка1Сумма   + (-РазницаСумм);
		Иначе
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекКолво   = Формат(Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.РезультатИзлишекСумма   = Формат(РазницаСумм,ФорматВыводаСуммы);
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаКолво = 0;
			ОбластьСтрокаТаблицы.Параметры.РезультатНедостачаСумма = 0;
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковКолво = Формат(Разница,ФорматВыводаКоличества);
			ОбластьСтрокаТаблицы.Параметры.ПриходИзлишковСумма = Формат(РазницаСумм,ФорматВыводаСуммы);
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Колво   = 0;
			ОбластьСтрокаТаблицы.Параметры.СписаниеНедостачКолонка1Сумма   = 0;
			
			ИтогоРезультатИзлишекКолво   		= ИтогоРезультатИзлишекКолво  + Разница;
			ИтогоРезультатИзлишекСумма  		= ИтогоРезультатИзлишекСумма  + РазницаСумм;
			ИтогоРезультатНедостачаКолво 		= ИтогоРезультатНедостачаКолво + 0;
			ИтогоРезультатНедостачаСумма		= ИтогоРезультатНедостачаСумма + 0;
			ИтогоПриходИзлишковКолво 	 		= ИтогоПриходИзлишковКолво + Разница;
			ИтогоПриходИзлишковСумма 	 		= ИтогоПриходИзлишковСумма + РазницаСумм;
			ИтогоСписаниеНедостачКолонка1Колво  = ИтогоСписаниеНедостачКолонка1Колво   + 0;
			ИтогоСписаниеНедостачКолонка1Сумма  = ИтогоСписаниеНедостачКолонка1Сумма   + 0;
		КонецЕсли; 
			Ном = Ном + 1;
		
		СтраницаДоВывода = НомерСтраницы;
		Области = Новый Структура;
		Области.Вставить("Шапка",  ОбластьЗаголовокТаблицы);
		Области.Вставить("Подвал", ОбластьИтогиПоСтранице);
		НомерСтраницы = ВывестиГоризонтальнуюОбластьИНВ19(ТабличныйДокумент, ОбластьСтрокаТаблицы, Области, Документ, НомерСтраницы, СтруктураИтогов, мсвДопОбластиПодвала);
		Если СтраницаДоВывода <> НомерСтраницы Тогда
			СтруктураИтогов = Новый Структура;
			СтруктураИтогов.Вставить("ИтогоРезультатИзлишекКолво",         0);
			СтруктураИтогов.Вставить("ИтогоРезультатИзлишекСумма",         0);
			СтруктураИтогов.Вставить("ИтогоРезультатНедостачаКолво",       0);
			СтруктураИтогов.Вставить("ИтогоРезультатНедостачаСумма",       0);
			СтруктураИтогов.Вставить("ИтогоПриходИзлишковКолво",           0);
			СтруктураИтогов.Вставить("ИтогоПриходИзлишковСумма",           0);
			СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Колво", 0);
			СтруктураИтогов.Вставить("ИтогоСписаниеНедостачКолонка1Сумма", 0);
		КонецЕсли;
		
		Если Разница < 0 Тогда
			СтруктураИтогов.ИтогоРезультатИзлишекКолво   		= СтруктураИтогов.ИтогоРезультатИзлишекКолво + 0;
			СтруктураИтогов.ИтогоРезультатИзлишекСумма  		= СтруктураИтогов.ИтогоРезультатИзлишекСумма + 0;
			СтруктураИтогов.ИтогоРезультатНедостачаКолво 		= СтруктураИтогов.ИтогоРезультатНедостачаКолво + (-Разница);
			СтруктураИтогов.ИтогоРезультатНедостачаСумма 		= СтруктураИтогов.ИтогоРезультатНедостачаСумма + (-РазницаСумм);
			СтруктураИтогов.ИтогоПриходИзлишковКолво   			= СтруктураИтогов.ИтогоПриходИзлишковКолво + 0;
			СтруктураИтогов.ИтогоПриходИзлишковСумма   			= СтруктураИтогов.ИтогоПриходИзлишковСумма + 0;
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво + (-Разница);
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма + (-РазницаСумм);
		Иначе
			СтруктураИтогов.ИтогоРезультатИзлишекКолво   		= СтруктураИтогов.ИтогоРезультатИзлишекКолво + Разница;
			СтруктураИтогов.ИтогоРезультатИзлишекСумма  		= СтруктураИтогов.ИтогоРезультатИзлишекСумма + РазницаСумм;
			СтруктураИтогов.ИтогоРезультатНедостачаКолво 		= СтруктураИтогов.ИтогоРезультатНедостачаКолво + 0;
			СтруктураИтогов.ИтогоРезультатНедостачаСумма 		= СтруктураИтогов.ИтогоРезультатНедостачаСумма + 0;
			СтруктураИтогов.ИтогоПриходИзлишковКолво   			= СтруктураИтогов.ИтогоПриходИзлишковКолво + Разница;
			СтруктураИтогов.ИтогоПриходИзлишковСумма   			= СтруктураИтогов.ИтогоПриходИзлишковСумма + РазницаСумм;
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Колво + 0;
			СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма  = СтруктураИтогов.ИтогоСписаниеНедостачКолонка1Сумма + 0;
		КонецЕсли;	
			Сч = Сч + 1;
	КонецЦикла;
	
		// Выведем область итогов по странице.
		Если НомерСтраницы > 2 Тогда
			УправлениеПечатьюПлатформа.ВывестиИтогиПоСтранице(ТабличныйДокумент,ОбластьИтогиПоСтранице,СтруктураИтогов,Документ);
			ОбластьИтогиПоСтранице.Параметры.Заполнить(СтруктураИтогов);
			ТабличныйДокумент.Вывести(ОбластьИтогиПоСтранице);
		КонецЕсли;
	
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатИзлишекКолво   		= Формат(ИтогоРезультатИзлишекКолво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатИзлишекСумма   		= Формат(ИтогоРезультатИзлишекСумма,ФорматВыводаСуммы);
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатНедостачаКолво 		= Формат(ИтогоРезультатНедостачаКолво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоРезультатНедостачаСумма 		= Формат(ИтогоРезультатНедостачаСумма,ФорматВыводаСуммы);
		ОбластьИтогоТаблицы.Параметры.ИтогоПриходИзлишковКолво   			= Формат(ИтогоПриходИзлишковКолво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоПриходИзлишковСумма   		 	= Формат(ИтогоПриходИзлишковСумма,ФорматВыводаСуммы);
		ОбластьИтогоТаблицы.Параметры.ИтогоСписаниеНедостачКолонка1Колво 	= Формат(ИтогоСписаниеНедостачКолонка1Колво,ФорматВыводаКоличества);
		ОбластьИтогоТаблицы.Параметры.ИтогоСписаниеНедостачКолонка1Сумма 	= Формат(ИтогоСписаниеНедостачКолонка1Сумма,ФорматВыводаСуммы);
		ТабличныйДокумент.Вывести(ОбластьИтогоТаблицы);
		
		ОбластьПодвал.Параметры.Заполнить(ДанныеДокумента);
		
		// Заполним информацию о руководителях и ответственных.
	    ОбластьПодвал.Параметры.Заполнить(ДанныеДокумента.ГлавныйБухгалтер);
		//ОбластьПодвал.Параметры.МОЛДолжность		= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		//ОбластьПодвал.Параметры.МОЛПредставление	= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
	
		ТабличныйДокумент.Вывести(ОбластьПодвал);
		Если ДанныеДокумента.МассивМОЛ.Количество() Тогда
			Для Каждого стрМОЛ Из ДанныеДокумента.МассивМОЛ Цикл
				ОбластьПодвалМол.Параметры.МОЛДолжность     = стрМОЛ.Должность;
				ОбластьПодвалМол.Параметры.МОЛПредставление = стрМОЛ;
				ТабличныйДокумент.Вывести(ОбластьПодвалМол);				
			КонецЦикла;
		Иначе
			ТабличныйДокумент.Вывести(ОбластьПодвалМол);
		КонецЕсли;	
		
		// Отметим конец области документа.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
	КонецЦикла;
	
	Возврат ТабличныйДокумент;
КонецФункции

Функция итПечатьОбъяснительнаяПоНедостаче(МассивОбъектов, ОбъектыПечати)
	
	// Подготовка документа
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТабличныйДокумент.ИмяПараметровПечати	= "ПАРАМЕТРЫ_ПЕЧАТИ_ОбъяснительнаяПоНедостаче";
	ТабличныйДокумент.АвтоМасштаб			= Истина;
	ТабличныйДокумент.ОриентацияСтраницы	= ОриентацияСтраницы.Портрет;
	
	// Подготовка областей
	Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итОбъяснительнаяПоНедостаче");
	Область_ПустаяСтрока		= Макет.ПолучитьОбласть("ПустаяСтрока");
	Область_Шапка_ФММР			= Макет.ПолучитьОбласть("Шапка_ФММР");
	Область_Шапка_ИП			= Макет.ПолучитьОбласть("Шапка_ИП");
	Область_Тело_1				= Макет.ПолучитьОбласть("Тело_1"); 
	Область_Тело_2				= Макет.ПолучитьОбласть("Тело_2");
	Область_ПустаяСтрокаВвода	= Макет.ПолучитьОбласть("ПустаяСтрокаВвода");
	Область_Подвал				= Макет.ПолучитьОбласть("Подвал");
	КомплекснаяОбласть			= Новый Массив();
	КомплекснаяОбласть.Добавить(Область_ПустаяСтрока);
	КомплекснаяОбласть.Добавить(Область_ПустаяСтрокаВвода);
		
	ПервыйДокумент = Истина; 
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).итПолучитьОбщиеДанныеДляПечати(Документ);
		
		Если Не ДанныеДокумента.МассивМОЛ.Количество() Тогда		
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не указаны МОЛы по документу " + Строка(Документ) + ". Печатная форма не сформирована.");
		КонецЕсли;	
		
		// Вывод данных в табличный документ
		Для Каждого Мол Из ДанныеДокумента.МассивМОЛ Цикл
					
			//Определим итоговое отклонение инвентаризации
			СуммаОтклонения = ДанныеДокумента.ВыборкаТабличнойЧасти.Итог("СуммаФакт") - ДанныеДокумента.ВыборкаТабличнойЧасти.Итог("Сумма");
			
			НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
				
			// Заполнение данных
			Если ДанныеДокумента.ЭтоФММР Тогда  
				Область_Шапка = Область_Шапка_ФММР;
			Иначе
				Область_Шапка_ИП.Параметры.ЮрЛицо_Представление_1 = СокрЛП(ДанныеДокумента.Организация.НаименованиеСокращенное);
				Область_Шапка = Область_Шапка_ИП;
			КонецЕсли;	
			
			Область_Шапка.Параметры.ДолжностьПродавца = Строка(Мол.Должность);
			Область_Шапка.Параметры.Продавец_Представление_1 = Строка(Мол);
			
			Область_Тело_1.Параметры.Магазин_Адрес					= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ДанныеДокумента.СкладКомпании);			
			Область_Тело_1.Параметры.ДатаИнвентаризации				= Формат(ДанныеДокумента.Дата, "ДЛФ=DD");
			Область_Тело_1.Параметры.СуммаИнвентаризации			= Формат(?(СуммаОтклонения < 0, -СуммаОтклонения, СуммаОтклонения), "ЧГ=0");
			Область_Тело_1.Параметры.СуммаИнвентаризацииПрописью	= РаботаСКурсамиВалют.СформироватьСуммуПрописью(СуммаОтклонения, Константы.ВалютаРегламентированногоУчетаОрганизаций.Получить());
				
			// Вывод данных
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 3);
			ТабличныйДокумент.Вывести(Область_Шапка);
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 3);
			ТабличныйДокумент.Вывести(Область_Тело_1);
			ТабличныйДокумент.Вывести(Область_Тело_2);
			МногократноВывестиОбласть(ТабличныйДокумент, КомплекснаяОбласть, 3);
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 2);
			ТабличныйДокумент.Вывести(Область_Подвал);
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		
		КонецЦикла;	
			
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
		
	КонецЦикла;
		
	// Возврат
	Возврат ТабличныйДокумент;
	
КонецФункции

Функция итПечатьПриказОбУдержании(МассивОбъектов, ОбъектыПечати)
	
	// Подготовка документа
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТабличныйДокумент.ИмяПараметровПечати	= "ПАРАМЕТРЫ_ПЕЧАТИ_ОбъяснительнаяПоНедостаче";
	ТабличныйДокумент.АвтоМасштаб			= Истина;
	ТабличныйДокумент.ОриентацияСтраницы	= ОриентацияСтраницы.Портрет;
	
	// Подготовка областей
	Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итПриказОбУдержании");
	Область_ПустаяСтрока		= Макет.ПолучитьОбласть("ПустаяСтрока");
	Область_Шапка_ФММР			= Макет.ПолучитьОбласть("Шапка");
	Область_Шапка_ИП			= Макет.ПолучитьОбласть("Шапка");
	Область_Тело_1				= Макет.ПолучитьОбласть("Тело_1"); 
	Область_Тело_2				= Макет.ПолучитьОбласть("Тело_2_ФММР");
	Область_ПустаяСтрокаВвода	= Макет.ПолучитьОбласть("ПустаяСтрока");
	Область_Подвал				= Макет.ПолучитьОбласть("Подвал_2");
	КомплекснаяОбласть			= Новый Массив();
	КомплекснаяОбласть.Добавить(Область_ПустаяСтрока);
	КомплекснаяОбласть.Добавить(Область_ПустаяСтрокаВвода);
		
	ПервыйДокумент = Истина; 
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).итПолучитьОбщиеДанныеДляПечати(Документ);
		
		Если Не ДанныеДокумента.МассивМОЛ.Количество() Тогда		
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не указаны МОЛы по документу " + Строка(Документ) + ". Печатная форма не сформирована.");
		КонецЕсли;	
		
		// Вывод данных в табличный документ
		Для Каждого Мол Из ДанныеДокумента.МассивМОЛ Цикл
					
			//Определим итоговое отклонение инвентаризации
			СуммаОтклонения = ДанныеДокумента.ВыборкаТабличнойЧасти.Итог("СуммаФакт") - ДанныеДокумента.ВыборкаТабличнойЧасти.Итог("Сумма");
			
			НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
				
			// Заполнение данных
			Если ДанныеДокумента.ЭтоФММР Тогда  
				Область_Шапка = Область_Шапка_ФММР;
			Иначе
				Область_Шапка_ИП.Параметры.ЮрЛицо_Представление_1 = СокрЛП(ДанныеДокумента.Организация.НаименованиеСокращенное);
				Область_Шапка = Область_Шапка_ИП;
			КонецЕсли;	
			
			Область_Шапка.Параметры.ДолжностьПродавца = Строка(Мол.Должность);
			Область_Шапка.Параметры.Продавец_Представление_1 = Строка(Мол);
			
			Область_Тело_1.Параметры.Магазин_Адрес					= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ДанныеДокумента.СкладКомпании);			
			Область_Тело_1.Параметры.ДатаИнвентаризации				= Формат(ДанныеДокумента.Дата, "ДЛФ=DD");
			Область_Тело_1.Параметры.СуммаИнвентаризации			= Формат(?(СуммаОтклонения < 0, -СуммаОтклонения, СуммаОтклонения), "ЧГ=0");
			Область_Тело_1.Параметры.СуммаИнвентаризацииПрописью	= РаботаСКурсамиВалют.СформироватьСуммуПрописью(СуммаОтклонения, Константы.ВалютаРегламентированногоУчетаОрганизаций.Получить());
				
			// Вывод данных
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 3);
			ТабличныйДокумент.Вывести(Область_Шапка);
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 3);
			ТабличныйДокумент.Вывести(Область_Тело_1);
			ТабличныйДокумент.Вывести(Область_Тело_2);
			МногократноВывестиОбласть(ТабличныйДокумент, КомплекснаяОбласть, 3);
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 2);
			ТабличныйДокумент.Вывести(Область_Подвал);
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		
		КонецЦикла;	
			
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
		
	КонецЦикла;
		
	// Возврат
	Возврат ТабличныйДокумент;

КонецФункции

Функция итПечатьЗаявлениеНаУдержание(МассивОбъектов, ОбъектыПечати)
	
	// Подготовка документа
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТабличныйДокумент.ИмяПараметровПечати	= "ПАРАМЕТРЫ_ПЕЧАТИ_ЗаявлениеНаУдержание";
	ТабличныйДокумент.АвтоМасштаб			= Истина;
	ТабличныйДокумент.ОриентацияСтраницы	= ОриентацияСтраницы.Портрет;
	
	// Подготовка областей
	Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итЗаявлениеНаУдержание");
	Область_ПустаяСтрока		= Макет.ПолучитьОбласть("ПустаяСтрока");
	Область_Шапка_ФММР			= Макет.ПолучитьОбласть("Шапка_ФММР");
	Область_Шапка_ИП			= Макет.ПолучитьОбласть("Шапка_ИП");
	Область_Тело				= Макет.ПолучитьОбласть("Тело"); 
	Область_Подвал				= Макет.ПолучитьОбласть("Подвал");
		
	ПервыйДокумент = Истина; 
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).итПолучитьОбщиеДанныеДляПечати(Документ);
		
		Если Не ДанныеДокумента.МассивМОЛ.Количество() Тогда		
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не указаны МОЛы по документу " + Строка(Документ) + ". Печатная форма не сформирована.");
		КонецЕсли;	
		
		// Вывод данных в табличный документ
		Для Каждого Мол Из ДанныеДокумента.МассивМОЛ Цикл
						
			НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
			
			// Заполнение данных
			Если ДанныеДокумента.ЭтоФММР Тогда  
				Область_Шапка = Область_Шапка_ФММР;
			Иначе
				Область_Шапка_ИП.Параметры.ЮрЛицо_Представление_1 = СокрЛП(ДанныеДокумента.Организация.НаименованиеСокращенное);				
				Область_Шапка = Область_Шапка_ИП;
			КонецЕсли;	
			
			Область_Шапка.Параметры.ДолжностьПродавца = Строка(Мол.Должность);
			Область_Шапка.Параметры.Продавец_Представление_1 = Строка(Мол);
		
			Область_Тело.Параметры.ДатаИнвентаризации	= Формат(ДанныеДокумента.Дата, "ДЛФ=DD");
			Область_Тело.Параметры.МесяцУдержания		= ПолучитьМесяцУдержанияСтрокой(ДанныеДокумента.Дата);
			
			СуммаКУдержанию = 0;
			СуммаКУдержанию = ?(СуммаКУдержанию < 0, -СуммаКУдержанию, СуммаКУдержанию);
			
			Область_Тело.Параметры.СуммаИнвентаризации			= Формат(СуммаКУдержанию, "ЧГ=0");
			Область_Тело.Параметры.СуммаИнвентаризацииПрописью	= РаботаСКурсамиВалют.СформироватьСуммуПрописью(СуммаКУдержанию, Константы.ВалютаРегламентированногоУчетаОрганизаций.Получить());
			
			Область_Подвал.Параметры.ДатаЗаявления = Формат(ДанныеДокумента.Дата, "ДЛФ=DD");
			// Вывод данных
			ТабличныйДокумент.Вывести(Область_Шапка);
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 3);
			ТабличныйДокумент.Вывести(Область_Тело);
			МногократноВывестиОбласть(ТабличныйДокумент, Область_ПустаяСтрока, 3);
			ТабличныйДокумент.Вывести(Область_Подвал);
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();

		
		КонецЦикла;	
			
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
		
	КонецЦикла;
		
	// Возврат
	Возврат ТабличныйДокумент;

КонецФункции

Функция итПечатьПриказОбИнвентаризации(МассивОбъектов, ОбъектыПечати) 
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	//ТабличныйДокумент.ВысотаТаблицы = 1;
	// Добавим установку параметров печати
	ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ПриказОбИнвентаризации";
	УправлениеПечатьюПлатформа.УстановитьСтандартныеПараметрыПечати(ИмяПараметровПечати,ТабличныйДокумент);
	ТабличныйДокумент.ИмяПараметровПечати = ИмяПараметровПечати;

	ПервыйДокумент = Истина;
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
		ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
		
		// Зададим параметры макета.
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		
		// Получим данные из документа.
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).итПолучитьДанныеДляПечатиИНВ3(Документ);
		
		Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итПриказОбИнвентаризации");
				
		ОбластьЗаголовок        	= Макет.ПолучитьОбласть("Шапка");
		ОбластьОзнакомлен        	= Макет.ПолучитьОбласть("ОбластьОзнакомлен");
		
		СтруктураПредставления = Новый Структура;
		СтруктураПредставления.Вставить("Наименование",     "");
		СтруктураПредставления.Вставить("ИНН",              "ИНН ");
		СтруктураПредставления.Вставить("АдресЮридический", "");
		СтруктураПредставления.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПредставления.Вставить("БанковскийСчет",   "р/с ");
		СтруктураПредставления.Вставить("Банк",             НСтр("ru = 'в банке'") + " ");
		СтруктураПредставления.Вставить("БИК",              "БИК ");
		СтруктураПредставления.Вставить("КоррСчет",         "к/с ");
		
		// Дата документа
		ДатаДокумента = Документ.Дата;
		ДополнительныеПараметры = Новый Структура("ПодразделениеКомпании", Документ.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПредставление = 
		УправлениеПечатьюПлатформа.ПолучитьПредставлениеСправочника(Документ.Организация, 
																		СтруктураПредставления,,,ДатаДокумента,ДополнительныеПараметры);
		
		ОбластьЗаголовок.Параметры.ПодразделениеКомпании = 
		УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(Документ.ПодразделениеКомпании);
		
		//ОбластьЗаголовок.Параметры.ОрганизацияПоОКПО			= ОбластьЗаголовок.Параметры.Организация.КодПоОКПО;
		//ОбластьЗаголовок.Параметры.ВидДеятельностиПоОКДП		= ОбластьЗаголовок.Параметры.Организация.КодПоОКДП;
		//ОбластьЗаголовок.Параметры.МОЛДолжность				= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		//ОбластьЗаголовок.Параметры.МОЛПредставление			= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
		
		ОбластьЗаголовок.Параметры.Номер = УправлениеПечатьюПлатформа.ПолучитьНомерДляПечати(Документ);
		
		Если ЕстьРеквизит(Документ, "ДатаНачалаИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаНачалаИнвентаризации = Документ.ДатаНачалаИнвентаризации;
		КонецЕсли;
		Если ЕстьРеквизит(Документ, "ДатаОкончанияИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаОкончанияИнвентаризации = Документ.ДатаОкончанияИнвентаризации;
		КонецЕсли;
		
		ВидЦенностей = "товарно-материальные ценности";
		Если ДанныеДокумента.Локальная Тогда
			ВидЦенностей = "товарно-материальные ценности по группам товаров: " + ДанныеДокумента.ОписаниеЛокальныхФильтров;
		КонецЕсли;		
		ОбластьЗаголовок.Параметры.ВидЦенностей  = ВидЦенностей;
		ОбластьЗаголовок.Параметры.ВидЦенностей1 = ВидЦенностей;
		
		ОбластьЗаголовок.Параметры.ПредседательКомиссииДолжность      = ДанныеДокумента.ПредседательКомиссии.Должность;
		ОбластьЗаголовок.Параметры.ПредседательКомиссииПредставление  = ДанныеДокумента.ПредседательКомиссии;
		
		сч = 1;
		Для Каждого стрКомиссии Из ДанныеДокумента.МассивЧленовКомиссии Цикл
			
			Если сч > 3 Тогда
				Прервать;
			КонецЕсли;
			
			ОбластьЗаголовок.Параметры["ЧленКомиссии" + Строка(сч) + "Должность"]      = стрКомиссии.Должность;
			ОбластьЗаголовок.Параметры["ЧленКомиссии" + Строка(сч) + "Представление"]  = стрКомиссии;	
			
			сч = сч + 1;
			
		КонецЦикла;		
		
		СтруктураОтбора=Новый Структура("Организация,Объект",ДанныеДокумента.Организация,Перечисления.ВидыОбъектовСведений.Руководитель);
		СтруктураСведений=РегистрыСведений.СведенияКомпании.ПолучитьПоследнее(НачалоДня(ТекущаяДатаСеанса()),СтруктураОтбора);
		Руководитель=СтруктураСведений.Значение;
		ОбластьЗаголовок.Параметры.РуководительДолжность      = Руководитель.Должность;
		ОбластьЗаголовок.Параметры.РуководительПредставление  = Руководитель;
			
		ТабличныйДокумент.Вывести(ОбластьЗаголовок);
				
		Если ДанныеДокумента.МассивМОЛ.Количество() Тогда
			Для Каждого стрМОЛ Из ДанныеДокумента.МассивМОЛ Цикл
				ОбластьОзнакомлен.Параметры.ОзнакомленДолжность     = стрМОЛ.Должность;
				ОбластьОзнакомлен.Параметры.ОзнакомленПредставление = стрМОЛ;
				ТабличныйДокумент.Вывести(ОбластьОзнакомлен);				
			КонецЦикла;
		Иначе
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);
		КонецЕсли;			
		
		ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = ДанныеДокумента.ПредседательКомиссии.Должность;
		ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = ДанныеДокумента.ПредседательКомиссии;
		ТабличныйДокумент.Вывести(ОбластьОзнакомлен);
		
		Для Каждого стрКомиссии Из ДанныеДокумента.МассивЧленовКомиссии Цикл
			
			ОбластьОзнакомлен.Параметры.ОзнакомленДолжность       = стрКомиссии.Должность;
			ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = стрКомиссии;	
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);
			
		КонецЦикла;			
		
		// Отметим конец области документа.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Документ);
	КонецЦикла;
	
	Возврат ТабличныйДокумент;	

КонецФункции

Процедура итПечатьПриказОбИнвентаризацииВТабличныйДокумент(ТабличныйДокумент, МассивОбъектов, ОбъектыПечати, Пакет = НЕОПРЕДЕЛЕНО) Экспорт
	
	ПервыйДокумент = Истина;
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
		ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
		
		// Зададим параметры макета.
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		
		// Получим данные из документа.
		ДанныеДокумента = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Документ).итПолучитьДанныеДляПечатиИНВ3(Документ);
		
		Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итПриказОбИнвентаризации");
				
		ОбластьЗаголовок        	= Макет.ПолучитьОбласть("Шапка");
		ОбластьОзнакомлен        	= Макет.ПолучитьОбласть("ОбластьОзнакомлен");
		
		СтруктураПредставления = Новый Структура;
		СтруктураПредставления.Вставить("Наименование",     "");
		СтруктураПредставления.Вставить("ИНН",              "ИНН ");
		СтруктураПредставления.Вставить("АдресЮридический", "");
		СтруктураПредставления.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПредставления.Вставить("БанковскийСчет",   "р/с ");
		СтруктураПредставления.Вставить("Банк",             НСтр("ru = 'в банке'") + " ");
		СтруктураПредставления.Вставить("БИК",              "БИК ");
		СтруктураПредставления.Вставить("КоррСчет",         "к/с ");
		
		// Дата документа
		ДатаДокумента = Документ.Дата;
		ДополнительныеПараметры = Новый Структура("ПодразделениеКомпании", Документ.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПредставление = 
		УправлениеПечатьюПлатформа.ПолучитьПредставлениеСправочника(Документ.Организация, 
																		СтруктураПредставления,,,ДатаДокумента,ДополнительныеПараметры);
		
		ОбластьЗаголовок.Параметры.ПодразделениеКомпании = 
		УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(Документ.ПодразделениеКомпании);
		
		//ОбластьЗаголовок.Параметры.ОрганизацияПоОКПО			= ОбластьЗаголовок.Параметры.Организация.КодПоОКПО;
		//ОбластьЗаголовок.Параметры.ВидДеятельностиПоОКДП		= ОбластьЗаголовок.Параметры.Организация.КодПоОКДП;
		//ОбластьЗаголовок.Параметры.МОЛДолжность				= ОбластьЗаголовок.Параметры.МОЛ.Должность;
		//ОбластьЗаголовок.Параметры.МОЛПредставление			= УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(ОбластьЗаголовок.Параметры.МОЛ);
		
		ОбластьЗаголовок.Параметры.Номер = УправлениеПечатьюПлатформа.ПолучитьНомерДляПечати(Документ);
		
		Если ЕстьРеквизит(Документ, "ДатаНачалаИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаНачалаИнвентаризации = Формат(Документ.ДатаНачалаИнвентаризации, "ДЛФ=DD");
		КонецЕсли;
		Если ЕстьРеквизит(Документ, "ДатаОкончанияИнвентаризации") Тогда
			ОбластьЗаголовок.Параметры.ДатаОкончанияИнвентаризации = Формат(Документ.ДатаОкончанияИнвентаризации, "ДЛФ=DD");
		КонецЕсли;
		
		ВидЦенностей = "товарно-материальные ценности";
		Если ДанныеДокумента.Локальная Тогда
			ВидЦенностей = "товарно-материальные ценности по группам товаров: " + ДанныеДокумента.ОписаниеЛокальныхФильтров;
		КонецЕсли;		
		ОбластьЗаголовок.Параметры.ВидЦенностей  = ВидЦенностей;
		ОбластьЗаголовок.Параметры.ВидЦенностей1 = ВидЦенностей;
		
		Если Пакет <> НЕОПРЕДЕЛЕНО Тогда 
			
			Если ЗначениеЗаполнено(Пакет.ПредседательКомиссии) Тогда
				ОбластьЗаголовок.Параметры.ПредседательКомиссииДолжность      = Пакет.ПредседательКомиссии.Должность;
				ОбластьЗаголовок.Параметры.ПредседательКомиссииПредставление  = Пакет.ПредседательКомиссии;
			КонецЕсли;	
			Если ЗначениеЗаполнено(Пакет.ЧленКомиссии_ЛинейныйМенеджер) Тогда
				ОбластьЗаголовок.Параметры.ЧленКомиссии1Должность      = Пакет.ЧленКомиссии_ЛинейныйМенеджер.Должность;
				ОбластьЗаголовок.Параметры.ЧленКомиссии1Представление  = Пакет.ЧленКомиссии_ЛинейныйМенеджер;
			КонецЕсли;	
			Если ЗначениеЗаполнено(Пакет.ЧленКомиссии_СтаршийМенеджер) Тогда
				ОбластьЗаголовок.Параметры.ЧленКомиссии2Должность      = Пакет.ЧленКомиссии_СтаршийМенеджер.Должность;
				ОбластьЗаголовок.Параметры.ЧленКомиссии2Представление  = Пакет.ЧленКомиссии_СтаршийМенеджер;
			КонецЕсли;
			Если ЗначениеЗаполнено(Пакет.ЧленКомиссии_СотрудникСлужбыБезопасности) Тогда
				ОбластьЗаголовок.Параметры.ЧленКомиссии3Должность      = Пакет.ЧленКомиссии_СотрудникСлужбыБезопасности.Должность;
				ОбластьЗаголовок.Параметры.ЧленКомиссии3Представление  = Пакет.ЧленКомиссии_СотрудникСлужбыБезопасности;
			КонецЕсли;
			
		Иначе
			
			ОбластьЗаголовок.Параметры.ПредседательКомиссииДолжность      = ДанныеДокумента.ПредседательКомиссии.Должность;
			ОбластьЗаголовок.Параметры.ПредседательКомиссииПредставление  = ДанныеДокумента.ПредседательКомиссии;
			
			сч = 1;
			Для Каждого стрКомиссии Из ДанныеДокумента.МассивЧленовКомиссии Цикл
				
				Если сч > 3 Тогда
					Прервать;
				КонецЕсли;
				
				ОбластьЗаголовок.Параметры["ЧленКомиссии" + Строка(сч) + "Должность"]      = стрКомиссии.Должность;
				ОбластьЗаголовок.Параметры["ЧленКомиссии" + Строка(сч) + "Представление"]  = стрКомиссии;	
				
				сч = сч + 1;
				
			КонецЦикла;		
		КонецЕсли;	
			
		СтруктураОтбора=Новый Структура("Организация,Объект",ДанныеДокумента.Организация,Перечисления.ВидыОбъектовСведений.Руководитель);
		СтруктураСведений=РегистрыСведений.СведенияКомпании.ПолучитьПоследнее(НачалоДня(ТекущаяДатаСеанса()),СтруктураОтбора);
		Руководитель=СтруктураСведений.Значение;
		ОбластьЗаголовок.Параметры.РуководительДолжность      = Руководитель.Должность;
		ОбластьЗаголовок.Параметры.РуководительПредставление  = Руководитель;
			
		ТабличныйДокумент.Вывести(ОбластьЗаголовок);
						
		
		Если Пакет <> НЕОПРЕДЕЛЕНО Тогда 
			
			Если ЗначениеЗаполнено(Пакет.ПредседательКомиссии) Тогда
				ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Пакет.ПредседательКомиссии.Должность;
				ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Пакет.ПредседательКомиссии;
				ТабличныйДокумент.Вывести(ОбластьОзнакомлен);			
			КонецЕсли;	
			Если ЗначениеЗаполнено(Пакет.ЧленКомиссии_ЛинейныйМенеджер) Тогда
				ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Пакет.ЧленКомиссии_ЛинейныйМенеджер.Должность;
				ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Пакет.ЧленКомиссии_ЛинейныйМенеджер;
				ТабличныйДокумент.Вывести(ОбластьОзнакомлен);					
			КонецЕсли;	
			Если ЗначениеЗаполнено(Пакет.ЧленКомиссии_СтаршийМенеджер) Тогда
				ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Пакет.ЧленКомиссии_СтаршийМенеджер.Должность;
				ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Пакет.ЧленКомиссии_СтаршийМенеджер;
				ТабличныйДокумент.Вывести(ОбластьОзнакомлен);	
			КонецЕсли;
			Если ЗначениеЗаполнено(Пакет.ЧленКомиссии_СотрудникСлужбыБезопасности) Тогда
				ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Пакет.ЧленКомиссии_СотрудникСлужбыБезопасности.Должность;
				ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Пакет.ЧленКомиссии_СотрудникСлужбыБезопасности;
				ТабличныйДокумент.Вывести(ОбластьОзнакомлен);	
			КонецЕсли;
			
			ВыборкаПродавцы = Пакет.Инвентаризации.НайтиСтроки(Новый Структура("ДокументИнвентаризация",Документ));
			
			Для Каждого стрПродавец Из ВыборкаПродавцы Цикл		
				ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = стрПродавец.Продавец.Должность;
				ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = стрПродавец.Продавец;
				ТабличныйДокумент.Вывести(ОбластьОзнакомлен);	
			КонецЦикла;				
		Иначе		
		
			ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = ДанныеДокумента.ПредседательКомиссии.Должность;
			ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = ДанныеДокумента.ПредседательКомиссии;
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);
			
			Для Каждого стрКомиссии Из ДанныеДокумента.МассивЧленовКомиссии Цикл
				
				ОбластьОзнакомлен.Параметры.ОзнакомленДолжность       = стрКомиссии.Должность;
				ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = стрКомиссии;	
				ТабличныйДокумент.Вывести(ОбластьОзнакомлен);
				
			КонецЦикла;			
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры

Процедура итПечатьПриказОбИнвентаризацииИзПриказаВТабличныйДокумент(ТабличныйДокумент, МассивОбъектов, ОбъектыПечати) Экспорт
	
	ПервыйДокумент = Истина;
	Для Каждого Документ Из МассивОбъектов Цикл
		Если НЕ ПервыйДокумент Тогда // новый документ должен быть на отдельной странице
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		ФорматВыводаСуммы      = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаСуммы(Документ);
		ФорматВыводаКоличества = УправлениеПечатьюПлатформа.ПолучитьФорматВыводаКоличества(Документ);
		
		// Зададим параметры макета.
		ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		
		// Получим данные из документа.		
		Макет = УправлениеПечатью.МакетПечатнойФормы("Обработка.ПечатьИНВ.итПриказОбИнвентаризации");
				
		ОбластьЗаголовок        	= Макет.ПолучитьОбласть("Шапка");
		ОбластьОзнакомлен        	= Макет.ПолучитьОбласть("ОбластьОзнакомлен");
		
		СтруктураПредставления = Новый Структура;
		СтруктураПредставления.Вставить("Наименование",     "");
		СтруктураПредставления.Вставить("ИНН",              "ИНН ");
		СтруктураПредставления.Вставить("АдресЮридический", "");
		СтруктураПредставления.Вставить("ТелефонРабочий",   "тел.: ");
		СтруктураПредставления.Вставить("БанковскийСчет",   "р/с ");
		СтруктураПредставления.Вставить("Банк",             НСтр("ru = 'в банке'") + " ");
		СтруктураПредставления.Вставить("БИК",              "БИК ");
		СтруктураПредставления.Вставить("КоррСчет",         "к/с ");
		
		// Дата документа
		ДатаДокумента = Документ.Дата;
		ДополнительныеПараметры = Новый Структура("ПодразделениеКомпании", Документ.ПодразделениеКомпании);
		
		ОбластьЗаголовок.Параметры.ОрганизацияПредставление = 
		УправлениеПечатьюПлатформа.ПолучитьПредставлениеСправочника(Документ.Организация, 
																		СтруктураПредставления,,,ДатаДокумента,ДополнительныеПараметры);
		
		ОбластьЗаголовок.Параметры.ПодразделениеКомпании = 
		УправлениеПечатьюПлатформа.ПолучитьНаименованиеСправочника(Документ.ПодразделениеКомпании);
				
		ОбластьЗаголовок.Параметры.Номер = УправлениеПечатьюПлатформа.ПолучитьНомерДляПечати(Документ);
		ОбластьЗаголовок.Параметры.Дата  = Формат(ДатаДокумента, "ДЛФ=D");
		
		Если ЕстьРеквизит(Документ, "ДатаНачала") Тогда
			ОбластьЗаголовок.Параметры.ДатаНачалаИнвентаризации = Формат(Документ.ДатаНачала, "ДЛФ=DD");
		КонецЕсли;
		Если ЕстьРеквизит(Документ, "ДатаОкончания") Тогда
			ОбластьЗаголовок.Параметры.ДатаОкончанияИнвентаризации = Формат(Документ.ДатаОкончания, "ДЛФ=DD");
		КонецЕсли;
		
		ВидЦенностей = "товарно-материальные ценности";
		//Если ДанныеДокумента.Локальная Тогда
		//	ВидЦенностей = "товарно-материальные ценности по группам товаров: " + ДанныеДокумента.ОписаниеЛокальныхФильтров;
		//КонецЕсли;		
		ОбластьЗаголовок.Параметры.ВидЦенностей  = ВидЦенностей;
		ОбластьЗаголовок.Параметры.ВидЦенностей1 = ВидЦенностей;
		
		Если ЗначениеЗаполнено(Документ.ПредседательКомиссии) Тогда
			ОбластьЗаголовок.Параметры.ПредседательКомиссииДолжность      = Документ.ПредседательКомиссии.Должность;
			ОбластьЗаголовок.Параметры.ПредседательКомиссииПредставление  = Документ.ПредседательКомиссии;
		КонецЕсли;	
		Если ЗначениеЗаполнено(Документ.ЧленКомиссии_ЛинейныйМенеджер) Тогда
			ОбластьЗаголовок.Параметры.ЧленКомиссии1Должность      = Документ.ЧленКомиссии_ЛинейныйМенеджер.Должность;
			ОбластьЗаголовок.Параметры.ЧленКомиссии1Представление  = Документ.ЧленКомиссии_ЛинейныйМенеджер;
		КонецЕсли;	
		Если ЗначениеЗаполнено(Документ.ЧленКомиссии_СтаршийМенеджер) Тогда
			ОбластьЗаголовок.Параметры.ЧленКомиссии2Должность      = Документ.ЧленКомиссии_СтаршийМенеджер.Должность;
			ОбластьЗаголовок.Параметры.ЧленКомиссии2Представление  = Документ.ЧленКомиссии_СтаршийМенеджер;
		КонецЕсли;
		Если ЗначениеЗаполнено(Документ.ЧленКомиссии_СотрудникСлужбыБезопасности) Тогда
			ОбластьЗаголовок.Параметры.ЧленКомиссии3Должность      = Документ.ЧленКомиссии_СотрудникСлужбыБезопасности.Должность;
			ОбластьЗаголовок.Параметры.ЧленКомиссии3Представление  = Документ.ЧленКомиссии_СотрудникСлужбыБезопасности;
		КонецЕсли;
		
		СтруктураОтбора=Новый Структура("Организация,Объект",Документ.Организация,Перечисления.ВидыОбъектовСведений.Руководитель);
		СтруктураСведений=РегистрыСведений.СведенияКомпании.ПолучитьПоследнее(НачалоДня(ТекущаяДатаСеанса()),СтруктураОтбора);
		Руководитель=СтруктураСведений.Значение;
		ОбластьЗаголовок.Параметры.РуководительДолжность      = Руководитель.Должность;
		ОбластьЗаголовок.Параметры.РуководительПредставление  = Руководитель;
			
		ТабличныйДокумент.Вывести(ОбластьЗаголовок);
						
				
		Если ЗначениеЗаполнено(Документ.ПредседательКомиссии) Тогда
			ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Документ.ПредседательКомиссии.Должность;
			ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Документ.ПредседательКомиссии;
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);			
		КонецЕсли;	
		Если ЗначениеЗаполнено(Документ.ЧленКомиссии_ЛинейныйМенеджер) Тогда
			ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Документ.ЧленКомиссии_ЛинейныйМенеджер.Должность;
			ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Документ.ЧленКомиссии_ЛинейныйМенеджер;
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);					
		КонецЕсли;	
		Если ЗначениеЗаполнено(Документ.ЧленКомиссии_СтаршийМенеджер) Тогда
			ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Документ.ЧленКомиссии_СтаршийМенеджер.Должность;
			ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Документ.ЧленКомиссии_СтаршийМенеджер;
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);	
		КонецЕсли;
		Если ЗначениеЗаполнено(Документ.ЧленКомиссии_СотрудникСлужбыБезопасности) Тогда
			ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = Документ.ЧленКомиссии_СотрудникСлужбыБезопасности.Должность;
			ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = Документ.ЧленКомиссии_СотрудникСлужбыБезопасности;
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);	
		КонецЕсли;
		
		Для Каждого стрПродавец Из Документ.Продавцы Цикл
			ОбластьОзнакомлен.Параметры.ОзнакомленДолжность      = стрПродавец.МОЛ.Должность;
			ОбластьОзнакомлен.Параметры.ОзнакомленПредставление  = стрПродавец.МОЛ;
			ТабличныйДокумент.Вывести(ОбластьОзнакомлен);				
		КонецЦикла;
		
	КонецЦикла;

КонецПроцедуры

Функция МногократноВывестиОбласть(ТабличныйДокумент, ОбластьМакета, КоличествоВыводов)
	
	Для НомерВывода = 1 По КоличествоВыводов Цикл 
		Если ТипЗнч(ОбластьМакета) = Тип("Массив") Тогда 
			Для Каждого ОбластьМассива Из ОбластьМакета Цикл 
				ТабличныйДокумент.Вывести(ОбластьМассива);	
			КонецЦикла;	
		Иначе	
			ТабличныйДокумент.Вывести(ОбластьМакета);
		КонецЕсли;	
	КонецЦикла;	
	
КонецФункции	

Функция ПолучитьМесяцУдержанияСтрокой(Дата)
	
	ДатаСтрокой		= Формат(Дата, "ДФ='dd MMMM yyyy ""г.""'");
	МесяцУдержания	= Сред(ДатаСтрокой, 4);
	
	Возврат МесяцУдержания;
	
КонецФункции

//Мальцев А.Ю. ---


#КонецЕсли